# Round 5 · Recon findings (87) — HEAD 0b8befa

sev={'Medium': 26, 'Low': 36, 'High': 25} | status={'CONFIRMED': 35, 'CHANGED': 5, 'RESOLVED': 1, 'NEW': 46} | type={'security': 12, 'bug': 18, 'performance': 16, 'quality': 18, 'test': 9, 'completeness': 14}

Generated from workflow wf_ece69bd5-3e7 (13 scouts).


| id | sev | type | status | conf | location | problem |
|---|---|---|---|---|---|---|
| NEW-completeness-backend-1 | High | completeness | NEW | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_b/onboard.py:689` | current: POST /admin/onboard/finalize-image-provider with choice="reuse" ALWAYS returns 409 CONFLICT and never confirms image capability, even for genuinely image-capable providers (e.g. one exposing dall-e-3). expected: the route probes the provider and accepts when image generation is supported. The first-run wizard's 'reuse my current API key for image generation' step is broken for every provider. |
| NEW-completeness-backend-2 | High | completeness | NEW | high | `python/packages/corlinman-providers/src/corlinman_providers/openai_provider.py:280; python/packages/corlinman-episodes/src/corlinman_episodes/embed.py:24` | current: the entire embeddings / dense-retrieval feature is dead. The [embedding] config block is parsed (specs.py EmbeddingSpec, lifecycle/py_config.py:157), seeded by the onboarding wizard (onboard.py:329), and exposed via /admin/embedding, but NO production code path ever constructs an embedder or calls .embed(). Every real provider's embed() raises NotImplementedError (OpenAIProvider.embed line 281 'lands in M3', google_provider.py:178, bedrock_provider.py:349). The episodes embedding sweep  |
| NEW-verify-functional-1 | High | completeness | CONFIRMED | high | `ui/lib/mocks/nodes.ts:24-26 + ui/app/(admin)/nodes/page.tsx:24,52-57` | current: /nodes admin page (shipped in sidebar nav at sidebar.tsx:139, label nav.nodes, isDeveloper) is permanently empty — it polls fetchRunnersMock() every 5s, which is `async function fetchRunnersMock(): Promise<Runner[]> { return []; }` (a hardcoded empty array). expected: it should fetch the live runner registry. No real backend endpoint exists: the UI/i18n strings (en.ts:1565,1568; zh-CN.ts:1530,1532) and the page TODO(B4-BE3) all reference a future GET /wstool/runners endpoint that is NOT |
| NEW-verify-functional-2 | High | completeness | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes/register.py:123-136 + lifecycle/entrypoint.py:1064-1070 + grpc/plugin_invoker.py:198-206` | current: the plugin async-callback surface (POST /v1/plugins/callback/{task_id} and legacy /plugin-callback/{task_id}) is PERMANENTLY 501 in production, AND the parking side is also unwired, so the whole async-plugin (accepted_for_later/task_id) feature is a no-op end to end. expected: an async plugin returns {task_id}, the gateway parks the tool call on a shared AsyncTaskRegistry, the plugin POSTs the real result to the callback route, and the parked tool call wakes. Two independent gaps: (1) r |
| NEW-fresh-sec-channels-deploy-1 | High | security | NEW | high | `deploy/install.sh:709-728` | Native systemd install runs the internet-facing gateway as root. The generated corlinman.service unit has NO User=/Group= directive, so a systemd system unit defaults to running as root. The gateway binds BIND=0.0.0.0 (line 721), serves the admin REST API, and drives channel/agent/plugin code paths. Current vs expected: should run under a dedicated unprivileged system user (as the Docker path already does via `useradd --system corlinman` + `USER corlinman` in docker/Dockerfile:114-116,154); nati |
| NEW-fresh-sec-channels-deploy-2 | High | security | NEW | high | `deploy/install.sh:682 + deploy/install.sh:748-765` | Local privilege escalation via writable root-executed upgrade scripts. install.sh chowns the ENTIRE prefix (`sudo chown -R "$(id -u):$(id -g)" "$PREFIX"`, lines 481 and 682) to the non-root install user, so $PREFIX/repo/deploy/corlinman-upgrader.sh and $PREFIX/repo/deploy/install.sh become owned/writable by that unprivileged user. The corlinman-upgrader.service unit (User=root) does `ExecStart=/bin/bash ${PREFIX}/repo/deploy/corlinman-upgrader.sh` (line 760) and the script in turn runs `bash ${I |
| NEW-fresh-sec-gateway-1 | High | security | NEW | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:423 (and entrypoint.py:1074-1078, middleware/auth.py:145)` | The /v1/voice WebSocket is exposed with NO authentication. The gateway's only /v1 gate is ApiKeyAuthMiddleware, a Starlette BaseHTTPMiddleware, which is never invoked for WebSocket scopes. The voice router is mounted unconditionally at boot (entrypoint.py:1078 -> routes_voice.mod.router()), and run_voice_session performs subprotocol negotiation + a per-tenant daily budget check but never verifies a bearer/API key. When an operator turns voice on ([voice] enabled=true with budget_minutes_per_tena |
| NEW-fresh-bug-server-1 | High | bug | NEW | high | `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:1019-1130 (append_message / append_messages BEGIN IMMEDIATE) vs 873-953,961-974,1578-1584 (complete_turn/error_turn/append_event autocommit)` | SqliteJournalBackend shares ONE aiosqlite.Connection across all sessions, but mixes two transaction models on it: append_message/append_messages/delete_session/append_events_batch open an explicit `BEGIN IMMEDIATE`...COMMIT envelope, while complete_turn, error_turn, _populate_turn_aggregates, append_event, update_turn_cost issue bare autocommit `execute(...)` + `commit()`. The per-session asyncio lock in agent_servicer (_lock_for/_session_locks) only serialises RPCs of the SAME session_key; two  |
| NEW-fresh-ui-1 | High | bug | NEW | high | `ui/lib/chat/use-chat-stream.ts:442-468 (editAndRerun) + 308-313 (runTurn payload build)` | edit-and-rerun sends STALE, un-truncated history to the model. Expected: after editing user message N, the model receives history[0..N-1] + edited content. Actual: it receives the full ORIGINAL history (including the messages that were dropped and the pre-edit version of message N) plus the edited content appended at the end. |
| NEW-fresh-ui-2 | High | bug | NEW | high | `ui/app/(admin)/chat/page.tsx:302-308 (ChatArea render) and 149-151,317-331 (pickerOpen/ChatModelPicker)` | The chat composer's model-picker pill is a dead/no-op button on /chat, and the ChatModelPicker can never open. Expected: clicking the model pill (or the /model slash command) opens the picker so operators can override the LLM/image model; the override state (llmOverride/imageOverride, persisted to localStorage) is plumbed but unreachable. |
| NEW-providers-1 | High | bug | NEW | high | `python/packages/corlinman-providers/src/corlinman_providers/anthropic_provider.py:433-464 (_split_system)` | AnthropicProvider drops assistant `tool_calls` and never emits Anthropic tool_use/tool_result blocks on the multi-round INPUT path. An assistant message produced by the reasoning loop's _extend_with_tool_round (content="", tool_calls=[...]) is collapsed to {"role":"assistant","content":""} (tool_calls silently discarded), and the following {"role":"tool",...} message becomes a bare {"role":"user","content":<result>}. Anthropic rejects empty assistant text content and a tool_result with no matchi |
| NEW-providers-2 | High | bug | NEW | high | `python/packages/corlinman-providers/src/corlinman_providers/bedrock_provider.py:406-423 (_split_system) + _build_anthropic_body:374-403` | Bedrock (Anthropic-on-Bedrock) has the identical multi-round tool-input defect as the native Anthropic adapter: assistant `tool_calls` are dropped and `role="tool"` results are turned into bare user messages with no tool_use/tool_result correlation. Multi-round tool calling against Anthropic-on-Bedrock breaks after the first tool call. |
| NEW-fresh-perf-1 | High | performance | NEW | high | `python/packages/corlinman-server/src/corlinman_server/system/subagent/store.py:250-275, 296, 313, 403, 424, 443` | PersistentSubagentStore rewrites its ENTIRE state to disk on every mutation and never prunes terminal rows, giving unbounded memory growth + O(N) write amplification per mutation (O(N^2) over process lifetime). current: _flush_locked() serializes the full _requests + _statuses dicts to JSON and does a synchronous tmp-write + os.replace, and is called from begin()/update()/append_log()/set_killed()/set_summary() after EVERY change; _statuses/_requests are never pruned and are reloaded from disk o |
| PERF-005 | High | performance | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/services/direct_backend.py:294,302,313` | O(K^2) string accumulation in the streaming tool-call argument assembler. Each provider arguments_delta fragment is appended to an immutable Python str via `+=`, reallocating the whole accumulated string every delta. |
| PERF-006 | High | performance | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:1310-1321` | list_session_summaries (powers /admin/sessions) still uses three correlated subqueries per grouped session row. R4-D6 only added the `turn_id DESC` tie-breaker for preview/status; it did not remove the correlated-subquery cost. |
| NEW-fresh-test-1 | High | test | NEW | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/cost.py:240 (evaluate_budget), :310 (SessionMeter.poll), :133 (InMemoryVoiceSpend.add_seconds) + budget.py:110 (BudgetEnforcer.tick), :132 (finalize), :140 (_checkpoint_delta)` | current: the entire per-tenant voice money/quota subsystem has ZERO test coverage. expected: the budget admission gate, mid-session termination, and delta-checkpoint billing arithmetic should each have unit tests since this is a money path and a live WS gate. |
| NEW-fresh-test-2 | High | test | NEW | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes/channels.py:73 (_verify_secret), :88 (_handle), :156 (POST /v1/channels/telegram/webhook)` | current: the Telegram-webhook route's secret-token authentication gate (`_verify_secret`) and the inbound update-injection handler have NO test. expected: the security gate that decides whether an unauthenticated caller may inject a Telegram Update into the agent loop must be covered (mismatch -> 401, empty-config -> open, valid secret -> 200). |
| NEW-verifyqualtest-2 | High | test | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:119-132` | current: token_config_to_acl translates per-token MCP ACLs (tools_allowlist, resources_allowed, prompts_allowed, tenant_id with DEFAULT_TENANT_ID fallback via TokenAcl.effective_tenant) — i.e. the cross-tenant authorization boundary for MCP tokens — with zero tests. expected: tests proving an empty/missing tenant_id falls back to DEFAULT_TENANT_ID and a set tenant_id is preserved (cross-tenant ACL not leaked). A regression here silently widens or mis-scopes tenant access on the MCP surface. |
| TEST-006 | High | test | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:1-209` | current: 209-line MCP server module with public funcs token_config_to_acl (:119), build_server_config (:135), build_dispatcher (:144), build_mcp_server (:170) and config dataclasses — zero tests. The tests/gateway/mcp/ directory exists but is EMPTY (0 files). expected: tests covering config->ACL translation, dispatcher build, server build. token_config_to_acl is security-relevant (see token_config_to_acl finding). |
| TEST-008 | High | test | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/home_channel_store.py:1-335` | current: 335-line /sethome SQLite store (set_home/get_home/list_all_homes/mark_tip_shown/resolve_user_id, idempotent PK on user_id) actively used by channels/commands.py, gateway/lifecycle/entrypoint.py, and gateway/services/chat_bootstrap.py — zero tests. expected: tests for set/get round-trip, idempotent re-/sethome on user_id PK, resolve_user_id channel+sender mapping, mark_tip_shown. |
| TEST-009 | High | test | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/middleware/admin_session.py:84-145` | current: AdminSessionStore TTL expiry (validate, :95), sliding-refresh (replace last_used=now, :101-102), gc() eviction (:119) and the background sweep task start_gc (:140, tick ttl/4 min 60s) have NO direct unit test. Many test files reference admin sessions but only as auth fixtures (mint via _admin_auth.py) — none exercise expiry/gc/sliding behavior. expected: unit tests on AdminSessionStore lifecycle. |
| NEW-fresh-quality-1 | High | quality | NEW | high | `.importlinter:21-23,31 (root) + .github/workflows/ci.yml:266` | The layering contract names corlinman_embedding as a root package and as a layer, but that package was DELETED in commit 50515f6 ('refactor: delete the corlinman-embedding package'). import-linter aborts before evaluating any contract, so the layering guard is silently 100% disabled. |
| NEW-fresh-quality-2 | High | quality | NEW | high | `.github/workflows/ci.yml:71 (py-mypy job) — errors across 72 files in python/packages/corlinman-server` | The required py-mypy gate runs `uv run mypy .` which fails with 155 type errors at HEAD (0b8befa) on a git-clean tree. py-mypy IS in the `gate` aggregator needs list (ci.yml:277), so the required branch-protection check is red. |
| NEW-fresh-quality-3 | High | quality | NEW | high | `.github/workflows/ci.yml (py-ruff job) — errors concentrated in python/` | The required py-ruff gate (`uv run ruff check .`) fails with ~1600 errors at HEAD on a git-clean tree. py-ruff is in the gate aggregator. Dominated by 525 RUF100 'unused noqa' (code is littered with `# noqa: BLE001` / `# noqa: PLC0415` for rules that are NOT in the root [tool.ruff.lint] select list, so ruff treats every such directive as an unused-noqa error), plus 200 I001 unsorted-imports and 91 F401 unused-import. |
| QUAL-006 | High | quality | CONFIRMED | high | `.importlinter:22,31` | current: `.importlinter` declares `corlinman_embedding` in `root_packages` (line 22) and in the `layers` contract (line 31), but no such package exists on disk (only corlinman-episodes ships an embed.py). The header comment also asserts "every package must appear in exactly one layer so the contract stays total" — yet only 5 of 25 packages are listed (non-total). expected: phantom package removed + comment fixed (or contract made total). IMPACT > cosmetic: `uv run lint-imports` (run by `make lin |
| NEW-completeness-backend-3 | Medium | completeness | NEW | high | `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:60; python/packages/corlinman-server/src/corlinman_server/gateway/lifecycle/entrypoint.py:1944` | current: the gateway-side MCP *server* (`/mcp` WebSocket that exposes corlinman's own tools/resources/prompts to external MCP clients like Claude Desktop) is fully built but never bound at boot. `build_mcp_server` is defined + re-exported but has zero callers outside its own module/tests. Only the MCP *client* (McpClientManager, for consuming external MCP servers) is wired in entrypoint.py:1944. expected (docs/design/phase4-w3-c1-design.md:6,29: 'mounted at /mcp ... point Desktop at ws://gateway |
| NEW-completeness-backend-4 | Medium | completeness | NEW | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:387; python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/persistence.py:154` | current: voice session start/end rows are NEVER persisted in production. `build_voice_state_from_app` (the gateway mount path) constructs VoiceState with config_loader/provider/data_dir but never sets `session_store`, which defaults to None (mod.py:225). Every persistence call site guards `if state.session_store is not None` (mod.py:642,767,1387), so record_start/record_end are silently skipped. There is also NO SQLite-backed VoiceSessionStore implementation — only MemoryVoiceSessionStore (tests |
| NEW-completeness-backend-5 | Medium | completeness | NEW | high | `python/packages/corlinman-providers/src/corlinman_providers/declarative.py:236` | current: a custom declarative provider configured with `auth_kind="header"` (e.g. X-API-Key) or `auth_kind="query_param"` silently sends the wrong auth. load_spec_from_toml validates these auth_kinds as legal (_VALID_AUTH_KINDS line 267) and parses the header_name/param_name into auth_config, but _build_inner ignores them entirely and always constructs an OpenAIProvider that forwards the key as `Authorization: Bearer`. The provider then gets 401s. expected: header/query_param auth kinds apply th |
| NEW-fresh-bug-server-2 | Medium | completeness | NEW | high | `python/packages/corlinman-server/src/corlinman_server/auto_resume.py:76 (_CHANNEL_NEEDS_BOOT_REPLAY) + 121-166 (BootReplayDispatcher.replay)` | current vs expected: auto-resume treats telegram/discord/slack/feishu as channels whose upstream won't redeliver, so it synthesizes a `pending` inbox row and counts it as resumed (resumed+=1, logs agent.resume.replay_enqueued). EXPECTED: a per-channel dispatch loop drains that inbox row and re-runs the chat RPC. ACTUAL: only QQ-family dispatch loops exist (`_qq_dispatch_loop`, `_qq_official...`); there is NO _telegram/_discord/_slack/_feishu dispatch loop, and those channels never enqueue or dra |
| NEW-fresh-quality-4 | Medium | completeness | NEW | high | `docs/ci-status.md:61-96 vs .github/workflows/ci.yml:71` | current vs expected: doc claims the python lint gate was scoped down (`Scoped uv run mypy . to uv run mypy python/packages/`, line 78) and that only 7 mypy + 17 ruff errors remain (lines 63,87,95); expected = doc matches CI. Reality: ci.yml:71 still runs the un-scoped `uv run mypy .`, and the counts are now 155 mypy / ~1600 ruff. The doc materially misrepresents the lint posture, masking that two required gates are red. |
| NEW-fresh-sec-gateway-2 | Medium | security | NEW | medium | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/persistence.py:242 (audio_path_for) and :252 (tts_audio_path_for), consumed at routes_voice/mod.py:637` | audio_path_for/tts_audio_path_for build a filesystem path by interpolating tenant_id verbatim with no sanitization: `Path(data_dir)/'tenants'/tenant_id/'voice'/f'{session_id}.pcm'`. tenant_id originates from the unauthenticated, client-controlled X-Tenant-Id header (see finding #1). A value like `../../../../tmp/x` escapes the per-tenant tree. mod.py:637 already materializes this path into the session-start row; if audio retention is enabled (retain_audio=true) and an audio writer mkdir/open()s  |
| SEC-008 | Medium | security | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_b/providers.py:615-626` | Admin-configured provider base_url is dialed with no SSRF guard. _query_provider_models builds `url = base_url.rstrip('/') + '/v1/models'` from the operator-supplied `entry_dict['base_url']` and issues `await client.get(url, headers={'Authorization': f'Bearer {api_key}'})` directly. No scheme/host/IP validation (no is_safe_host equivalent like the agent web fetch has). An authenticated admin (or anyone who can write provider config) can probe arbitrary internal/metadata endpoints and exfiltrate  |
| SEC-012 | Medium | security | CONFIRMED | high | `python/packages/corlinman-agent/src/corlinman_agent/web/_common.py:266-361 + python/packages/corlinman-agent/src/corlinman_agent/web/fetch.py:160-170` | DNS-rebind TOCTOU in the agent web-fetch SSRF guard. is_safe_host(url) resolves the host via socket.getaddrinfo and classifies the resulting IPs, but does NOT pin them. The subsequent `client.stream('GET', current_url)` dials by hostname, so httpx performs an independent second DNS resolution. A rebinding attacker can answer the guard's lookup with a public IP (passing) and the connect's lookup with 10.0.0.5 / 169.254.169.254 (the actual dial), bypassing the guard. Same gap on every redirect hop |
| BUG-006 | Medium | bug | CONFIRMED | high | `python/packages/corlinman-providers/src/corlinman_providers/openai_provider.py:222-238` | When a streamed OpenAI tool-call's first delta arrives without an `id` (common — OpenAI often sends the id only in the first chunk but some compatible servers send it late), the call is opened with a synthetic `f'call_{idx}'` id AND the 'finalized' flag is hardcoded True. The late-id `elif tc_id and not open_calls[idx][1]` branch then checks `not True == False` and is therefore unreachable, so the real server-issued tool_call_id arriving in a later delta is silently dropped. The synthetic id sti |
| BUG-010 | Medium | bug | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:812-840` | begin_turn derives turn_id from wall-clock ms and retries on PK collision with ms+offset for offset in range(0,20). If all 20 inserts collide (>=20 turns started in the same ms window), L839-840 falls through to `return ts` — returning ts+0, the exact id that already collided on the first iteration, WITHOUT any row having been inserted. The caller receives a turn_id that maps to a different, pre-existing turn's row. Subsequent complete_turn/error_turn/message-append against that id mutate the wr |
| NEW-fresh-ui-3 | Medium | bug | NEW | medium | `ui/components/sessions/cost-footer.tsx:34-41 and ui/components/sessions/session-cost-cells.tsx:37-44` | Two inline cost fetchers omit the GATEWAY_BASE_URL prefix, so the sessions cost column (every list row) and the session-detail cost footer silently fail (all cells -> '—') whenever NEXT_PUBLIC_GATEWAY_URL is set. Expected: prefix GATEWAY_BASE_URL like every other client call (apiFetch, uploadAsset, all streamX EventSources). |
| NEW-fresh-ui-4 | Medium | bug | NEW | medium | `ui/lib/api.ts:2528-2537 (streamUpgradeEvents)` | streamUpgradeEvents builds its EventSource URL without the GATEWAY_BASE_URL prefix, so the system-upgrade live progress SSE fails to connect when NEXT_PUBLIC_GATEWAY_URL is set. Expected: prefix GATEWAY_BASE_URL like the sibling streamSubagentEvents/streamSubagentsOverview/streamHubInstallEvents. |
| NEW-fresh-ui-5 | Medium | bug | NEW | medium | `ui/lib/chat/event-merger.ts:9-16 (docstring) + 190-198; ui/lib/chat/use-chat-stream.ts:278-283 (live onEvent) + 99-100 (applyEvent text-delta)` | Assistant text can be double-rendered. The merger's documented dedup between the token stream and the journal TextDelta events is NOT implemented, so if the /events/live journal emits TextDelta during the same turn the /v1/chat/completions token stream is rendering, draft.content gets each text fragment appended twice. |
| NEW-providers-3 | Medium | bug | NEW | medium | `python/packages/corlinman-providers/src/corlinman_providers/google_provider.py:96-102 (chat_stream prompt build)` | GoogleProvider serialises multimodal/structured message content with f-string interpolation of the raw Python object. When a user turn carries OpenAI content-parts (list form — produced by reasoning_loop._inject_attachments for ANY turn with an image/file attachment), `f"{role}: {content}"` embeds the Python repr of the list (e.g. "user: [{'type': 'image_url', 'image_url': {'url': '...'}}]") into the Gemini prompt. The image/attachment is never sent as a real image part, so every vision/attachme |
| NEW-fresh-perf-2 | Medium | performance | NEW | high | `python/packages/corlinman-server/src/corlinman_server/agent_servicer.py:1916-1924` | Builtin coding tools execute synchronous, potentially heavy filesystem work directly on the gRPC servicer's async event loop, stalling ALL concurrent chat streams for the duration. current: the async _dispatch_builtin awaits nothing for dispatch_read_file/dispatch_write_file/dispatch_edit_file/dispatch_list_files/dispatch_search_files — they are plain sync functions called inline. dispatch_search_files rglobs the workspace and read_text()s up to 5000 files (O(files) sync disk I/O) on the loop. e |
| PERF-008 | Medium | performance | CONFIRMED | high | `python/packages/corlinman-episodes/src/corlinman_episodes/store.py:388 (caller python/packages/corlinman-episodes/src/corlinman_episodes/runner.py:349)` | Episode persistence does one commit (one fsync round-trip) per row. There is no batch/executemany insert API, and the distillation runner inserts episodes one-at-a-time inside its per-bundle loop. |
| PERF-010 | Medium | performance | CONFIRMED | high | `ui/lib/chat/use-chat-stream.ts:238-249` | reduceEvent rebuilds the entire pendingMessage object — spreading prev and deep-copying its toolCalls, subagents (incl. nested events arrays), approvals, and usage — on every single SSE/journal event, even token deltas that touch only `content`. |
| PERF-012 | Medium | performance | CONFIRMED | high | `ui/components/chat/chat-model-picker.tsx:93-100` | Opening the model picker fans out one upstream model-probe HTTP request per enabled provider in parallel via useQueries, hitting every configured provider's /models endpoint at once. |
| NEW-fresh-test-3 | Medium | test | NEW | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/api_keys.py:240-266 (revoke_key handler)` | current: the DELETE /admin/api_keys/{key_id} route handler is untested at the route level; in particular its documented cross-tenant behavior (revoke is keyed on key_id ALONE, tenant resolution result is discarded) is not asserted by any test. expected: a route test that proves revoke ignores the ?tenant= scope (and a decision/test on whether that is acceptable), plus mint/list happy-path + tenant-isolation tests. |
| TEST-007 | Medium | test | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes/{wechat_webhook.py,plugin_callback.py,canvas.py}` | current: gateway/routes/wechat_webhook.py and gateway/routes/plugin_callback.py have NO direct route tests; canvas.py only has an entropy unit test on _new_session_id (not the SSE subscribe / POST /v1/canvas/frame handlers). channels.py/memory.py route LOGIC is only incidentally touched by auth-prefix tests. expected: handler-level tests for these production routes. NOTE: the wechat_official channel parser is tested in corlinman-channels/test_wechat_official.py, but that is the channel adapter,  |
| NEW-fresh-quality-5 | Medium | quality | NEW | high | `.github/workflows/ci.yml:241-266 (boundary-check) vs :272-285 (gate needs)` | The boundary-check job (import-linter) is NOT listed in the `gate` aggregator's `needs`, while branch protection is documented to require only the single `gate` check. So even when lint-imports hard-fails (which it currently does, see NEW-fresh-quality-1), the required gate stays green and nobody is forced to notice the dead layering contract. |
| QUAL-007 | Medium | quality | CONFIRMED | high | `python/packages/corlinman-agent/src/corlinman_agent/session_query.py:1-238` | current: 238-line SessionQueryClient/SessionMessage/SessionQueryError module whose docstring (lines 10-25, 73-76) states it reads the Rust gateway's `<data_dir>/sessions.sqlite` `sessions` table written by `corlinman_core::session_sqlite` (rust/crates/corlinman-core/src/session_sqlite.rs). expected: removed. It is dead on HEAD — `SessionQueryClient(` is instantiated ONLY in its own test (tests/test_session_query.py); no production code uses it. The Python plane now persists sessions via agent_jo |
| QUAL-010 | Medium | quality | CONFIRMED | high | `python/packages/ (29 src files)` | current: 29 separate `def _now_ms()` definitions in src (33 incl. tests), each effectively `return int(time.time() * 1000)`. expected: one shared helper. No shared now_ms util exists anywhere. |
| QUAL-011 | Medium | quality | CONFIRMED | high | `corlinman-agent/.../session_query.py:214,232; corlinman-identity/.../resolver.py:143-160; corlinman-replay/.../session_store.py:159-173; corlinman-server/.../routes_admin_a/identity.py:124; corlinman-user-model/.../cli.py:280` | current: 5+ packages each reimplement an RFC3339 formatter and/or parser (several ship both format+parse halves). expected: one shared RFC3339 util. Duplication of the same Z<->+00:00 normalization logic across packages. |
| QUAL-013 | Medium | quality | CONFIRMED | high | `corlinman-identity/.../verification.py:45; corlinman-server/.../routes_admin_b/credentials.py:42 + providers.py:44; routes_admin_a/personas.py:350; gateway/grpc/agent_server.py:289` | current: multiple sites cross-import leading-underscore private symbols across module/package boundaries, violating the underscore convention systematically. expected: promote to public or expose a public wrapper. |
| QUAL-015 | Medium | quality | CONFIRMED | high | `docs/providers.md:44,255; docs/architecture.md:71; docs/plugin-authoring.md:355; docs/roadmap.md:68-275; docs/milestones.md:32-40` | current: live, top-level docs link/point into `rust/crates/...` paths that the active Rust->Python migration is deleting. After rust/ removal these become dead links/references. architecture.md:71 even hardcodes an absolute machine path `/Users/cornna/project/corlinman/rust/crates/`. expected: repoint to the Python equivalents or remove. |
| NEW-fresh-quality-7 | Low | completeness | NEW | medium | `python/packages/corlinman-evolution-engine/src/corlinman_evolution_engine/{gepa.py:32-33,agent_card.py:13-14,110,engine.py:89-91,149,prompt_template.py:11-12}` | current vs expected: multiple evolution-engine docstrings assert the ShadowTester 'already routes high-risk kinds through the shadow stage' before proposals reach the operator queue; expected = a real call path into corlinman_shadow_tester. Reality: corlinman_shadow_tester is imported nowhere (see NEW-fresh-quality-6), so the documented high-risk gating does not exist — a documented safety control is absent. |
| NEW-fresh-sec-channels-deploy-6 | Low | completeness | NEW | high | `python/packages/corlinman-channels/src/corlinman_channels/commands.py:867-882` | is_command_admin() is default-open: when CORLINMAN_COMMAND_ADMINS is unset/blank it returns True for every caller (lines 877-879), so any inbound channel user is treated as a command admin. Current vs expected: in the shipped channels package no built-in CommandSpec sets admin_only=True (grep finds zero), so today this gates nothing dangerous and the behavior is intentional + unit-tested (test_command_handlers.py:363-368). Flagging for completeness because any deployment that registers an admin_ |
| NEW-verify-functional-3 | Low | completeness | CHANGED | high | `python/packages/corlinman-channels/src/corlinman_channels/qq_official.py:354-357,596,674-680,552-580` | current vs expected: the prompt hypothesizes QQ reconnect 501s/raises/deadloops — it does NOT. On a server RECONNECT (op 7, _OP_RECONNECT at :674-675) _pump returns cleanly; on INVALID_SESSION (:676-680) it wipes _session_id/_last_seq and returns; on websockets.ConnectionClosed (:647-648) it returns. In every case _connect_once finishes and the outer _reader_loop (:561-580) resets attempt=0 (clean) or backs off via RECONNECT_SCHEDULE (failure) and calls _connect_once() again, which always does a |
| NEW-verifyqualtest-3 | Low | completeness | CONFIRMED | high | `python/packages/corlinman-channels/tests/test_telegram_outbound.py:345-354` | current vs expected: assignment lists 'Telegram webhook _verify_secret untested' as a still-open gap. It is RESOLVED — verify_secret (telegram_webhook.py:86, the constant-time secret-token compare) is fully tested with match/mismatch/empty/None cases. Recording so the backlog drops this sub-item. |
| NEW-verifyqualtest-4 | Low | completeness | CONFIRMED | high | `README.md:5 vs 669,674,483,673` | current vs expected: README has internal version drift. Version badge (line 5) reads 1.9.0 (matches root pyproject.toml version=1.9.0 and server pyproject.toml), but the Versioning section says `**v1.1.0** (current)` (line 669) and `Released 2026-05-24, tagged v1.1.0` (line 674) — stale by 8 minor versions. Also admin page count: README says `Ten pages covering the full control plane` (line 483) and `10 operator pages` (line 673), but there are 33 page.tsx under ui/app/(admin)/ (~26 distinct top |
| NEW-fresh-sec-channels-deploy-3 | Low | security | NEW | medium | `deploy/corlinman-upgrader.sh:67,263` | Root-run upgrader writes its log to a predictable path in the world-writable /tmp (`LOG_FILE=${UPGRADER_LOG_FILE:-/tmp/corlinman-upgrader.log}`) and truncates it with `: >"$LOG_FILE"` as root. On a multi-user host an unprivileged user can pre-create /tmp/corlinman-upgrader.log as a symlink to an arbitrary file; the root truncation/append then clobbers the target (classic /tmp symlink/TOCTOU). Current vs expected: a root daemon should write logs under a root-owned dir (e.g. /var/log/corlinman or  |
| NEW-fresh-sec-channels-deploy-4 | Low | security | NEW | medium | `deploy/install.sh:497-509 / docker/compose/docker-compose.yml:16` | Supply-chain: container images are pulled by mutable tag, never by digest. install.sh pulls `ghcr.io/${REPO}:${REF}` (line 497-502) and the compose file uses `image: ghcr.io/sweetcornna/corlinman:${CORLINMAN_TAG:-latest}` (docker-compose.yml:16). No `--platform` pin, no digest pin, no signature/attestation verification (cosign/SLSA). A registry compromise or tag-overwrite (`latest`/`edge` are rolling per release-image.yml:50-52) silently ships replaced code that then runs as root on the native p |
| SEC-009 | Low | security | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:150-157 (emitted at :272)` | Admin session cookie is set without the `Secure` flag. _set_cookie_header builds `<name>=<token>; HttpOnly; SameSite=Strict; Path=/; Max-Age=...` with no `Secure`, so over a plaintext-HTTP hop the session token can be sniffed. The docstring explicitly states 'no Secure flag (TLS terminates upstream)'. Documented design choice per ISSUES.md L31; still technically a weakened cookie. |
| SEC-010 | Low | security | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes/wechat_webhook.py:95-98` | `bot_name` path parameter is reflected unvalidated into the 404 response body via `PlainTextResponse(f"no wechat bot registered as {bot_name!r}", status_code=404)`. The reflection is real, but PlainTextResponse uses Content-Type text/plain, so a browser will not execute injected HTML/JS — so this is NOT an exploitable reflected XSS. Effectively cosmetic / information-reflection only. |
| SEC-011 | Low | security | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:263 + python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/_auth_shim.py:192` | Admin username is compared with the non-constant-time `!=` operator at both auth sites, creating a username timing oracle. In auth.py the check `body.username != state.admin_username or not argon2_verify(...)` short-circuits on the username, so a wrong username skips the (slow, constant-time) argon2 path entirely — a measurable timing difference reveals whether the username is correct. _auth_shim.py Basic-auth fallback has the identical pattern. argon2_verify itself is constant-time, but the use |
| SEC-013 | Low | security | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:255-273` | The /admin/login handler has no rate-limiting, attempt counter, or lockout — online password brute-force is bounded only by argon2 verification cost. This is inconsistent with the sibling password_reset.py which DOES implement a global throttle (rejects with 429 if called twice within a window). No app-level limiter middleware covers /admin/login (the middleware/ dir has no rate limiter; the only `rate_limit_seconds=30` in entrypoint.py:2094 is unrelated to login). Backlog cited auth.py:74 — dri |
| BUG-008 | Low | bug | CHANGED | high | `python/packages/corlinman-providers/src/corlinman_providers/_retry.py:89-93` | with_retry uses `except BaseException as exc` (L89), which catches asyncio.CancelledError, and relies entirely on the caller's classifier to re-raise it. The structural defect (no explicit CancelledError guard before consulting the classifier) still exists, but it is NOT currently exploitable: the sole classifier in use, default_retryable_codex, returns None for CancelledError (no .status_code, _is_connection_error matches by class name only and 'CancelledError' is not in the set), so L92 `overr |
| NEW-fresh-bug-server-3 | Low | bug | NEW | medium | `python/packages/corlinman-server/src/corlinman_server/system/audit.py:74 (utcnow_iso ms precision) + 252-253 (_read_tail before_ts filter)` | Audit-log pagination drops entries that share the boundary millisecond. utcnow_iso() stamps ts at millisecond precision (`%Y-%m-%dT%H:%M:%S.` + zero-padded ms + 'Z'), so multiple appends within the same ms get byte-identical ts strings. tail() paginates with `before_ts` and filters `e.ts < before_ts` using the OLDEST ts the caller has seen. Any other entries that share that exact ms — including ones not yet returned — satisfy `ts == before_ts` (not `<`) and are silently excluded from the next pa |
| NEW-fresh-bug-server-4 | Low | bug | NEW | low | `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:812-840 (begin_turn collision fallback)` | begin_turn's wall-clock-ms turn_id collision retry loops `for offset in range(0, 20)`; on 20 consecutive IntegrityError collisions it falls through and `return ts` (line 840) — but `ts` is exactly the first id (offset 0) that already collided, so NO row was inserted for the returned turn_id. The caller (servicer line 1054+) then treats it as a live journal_turn_id and issues append_message/append_messages against it. With `PRAGMA foreign_keys=ON` (line 686) and the turn_messages FK to turns(turn |
| NEW-fresh-bug-server-5 | Low | bug | NEW | medium | `python/packages/corlinman-server/src/corlinman_server/system/upgrader/native_upgrader.py:336-358 (poll loop terminal check) + state.py:107-111 (is_terminal/is_in_flight overlap for 'stalled')` | Native upgrader poll loop only returns when the helper-written state is in {succeeded, failed} (line 337), NOT 'stalled'. If the helper itself writes state='stalled' into the status file, _apply_payload mirrors it to the store (with finished_at) but the poll loop keeps running; since the payload then stops changing, `payload != last_payload` is False so no further work happens, and the loop spins until the 600s overall_timeout fires and OVERWRITES the store row to state='failed'/phase='timeout', |
| NEW-fresh-bug-server-6 | Low | bug | NEW | low | `python/packages/corlinman-server/src/corlinman_server/inbox.py:164-197 (increment_retry CASE)` | increment_retry's UPDATE forces status to INBOX_PENDING for rows that are below the retry cap, regardless of whether the row was previously 'pending' or 'dispatched' (the WHERE matches both). A row legitimately in 'dispatched' that gets a retry bump is silently demoted back to 'pending', which would let the boot drainer / a poll re-dispatch a message already in flight (double-processing). NOTE: no production caller currently invokes increment_retry (channels marks rows done/dead directly), so th |
| NEW-fresh-sec-channels-deploy-5 | Low | bug | NEW | medium | `python/packages/corlinman-channels/src/corlinman_channels/wechat_official.py:409,436` | WeChat encrypted/compatibility mode is silently broken (fails closed, not a bypass). The webhook accepts either `signature` OR `msg_signature` (line 409) and feeds whichever it gets into verify_signature(), which only computes the PLAINTEXT scheme sha1(sort(token,timestamp,nonce)) (line 164-165). For an encrypted-mode account the real header is `msg_signature = sha1(sort(token,timestamp,nonce,encrypt))`, which will never match this digest, so every encrypted message is rejected with 401; and par |
| NEW-providers-4 | Low | bug | NEW | medium | `python/packages/corlinman-providers/src/corlinman_providers/declarative.py:236-240 (_build_inner) + ModelSpec/chat_endpoint` | Declarative providers ignore their own configured `chat_endpoint` and non-bearer auth kinds. For request_format=openai_compatible the inner adapter is built as OpenAIProvider(api_key, base_url) which always posts to <base_url>/chat/completions via the OpenAI SDK — a spec that declared chat_endpoint='/v1/messages' or '/api/chat' is silently dropped. Likewise auth_kind 'header'/'query_param' (parsed and accepted by load_spec_from_toml) are ignored and the key is sent as Authorization: Bearer, so a |
| NEW-fresh-perf-3 | Low | performance | NEW | medium | `python/packages/corlinman-episodes/src/corlinman_episodes/archive.py:229-263, 352, 355-366` | archive_unreferenced_episodes does one DB commit per archived row plus a synchronous per-file atomic write inside an async function. current: the loop calls _blank_hot_columns (one UPDATE + commit per row, line 352) and _atomic_write (sync mkdir+write_text+replace per row, 355-366) for each candidate, all on the event loop. expected: batch the UPDATEs into a single transaction (commit once per sweep) and/or offload file writes, matching PERF-008's per-row-commit concern in the same package. |
| NEW-fresh-perf-4 | Low | performance | NEW | medium | `python/packages/corlinman-agent-brain/src/corlinman_agent_brain/link_planner.py:225-256, 261-284` | plan_links issues one retrieval round-trip per candidate strictly serially (await inside the for loop), so a batch of K candidates costs K sequential RTTs; plan_links_batch then fans out over an unbounded number of batches with a bare asyncio.gather. current: candidates within a batch are awaited one-by-one; batch count is uncapped. expected: within a batch, gather candidate retrievals concurrently (or bound them), and cap the cross-batch gather to avoid unbounded concurrency against the retriev |
| NEW-providers-6 | Low | performance | NEW | low | `python/packages/corlinman-agent/src/corlinman_agent/subagent/blackboard.py:112-121 (_open) vs docstring line 116` | BlackboardStore._open opens each connection with isolation_level='IMMEDIATE' and the inline comment claims 'Readers are still concurrent under WAL', but WAL is never enabled (no `PRAGMA journal_mode=WAL`). With the default rollback journal an IMMEDIATE write transaction takes a reserved/exclusive lock that blocks concurrent sibling readers — exactly the concurrent fan-out (spawn_many siblings reading/writing the same blackboard) the module is designed for. Behaviour diverges from the documented  |
| NEW-verifyperf-1 | Low | performance | CONFIRMED | low | `python/packages/corlinman-agent/src/corlinman_agent/context_assembler.py:217,247,263` | context_assembler.assemble re-scans the full message list in 3 independent sequential loops (cascade-var, toolbox dedup/gate, placeholder pass) on top of stage-1 expansion — multiple full passes over msgs per assembly. Low impact because it runs once per turn over the turn-start message list (typically small), not per round or per delta. |
| PERF-007 | Low | performance | RESOLVED | medium | `python/packages/corlinman-agent/src/corlinman_agent/reasoning_loop.py:903-949 (cited line 929)` | Filed defect (full O(N) re-walk + `hash(repr(messages[0]))` token re-estimate every reasoning round) no longer holds: the per-round estimate is now an incremental cache that re-walks only the appended tail. |
| PERF-009 | Low | performance | CHANGED | medium | `ui/components/chat/markdown-message.tsx:88 (+ ui/components/chat/message-bubble.tsx:64)` | The all-bubbles re-parse storm is fixed (R4-D5), but the actively-streaming bubble still fully re-parses its markdown via <ReactMarkdown> on every SSE delta. This residual is largely inherent to live streaming and now affects only one bubble. |
| PERF-011 | Low | performance | CONFIRMED | low | `ui/lib/api/chat.ts:143-146` | Manual SSE frame parser does `buffer += decoder.decode(value)` then `buffer.split("\n\n")` on each read. Worst case is O(N^2) when a single large SSE frame spans many reads: the growing buffer is fully re-split on every chunk until the frame terminator arrives. |
| PERF-013 | Low | performance | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/services/chat_service.py:292` | Per tool-done frame the code does `__import__("json")` (dynamic import lookup) plus `bytes(tc.args_json)` materialization instead of a module-level `import json`. Minor but on a per-frame path; the file has no top-level `import json`. |
| PERF-015 | Low | performance | CONFIRMED | high | `python/packages/corlinman-agent/src/corlinman_agent/reasoning_loop.py:1573` | _extend_with_tool_round copies the entire running message list via `list(messages)` once per tool round, so total copy work grows O(rounds × messages) over a multi-round agent loop. |
| TEST-010 | Low | test | CHANGED | high | `python/packages/corlinman-providers/src/corlinman_providers/openai_compatible.py:1-73` | current: backlog points TEST-010 at openai_compatible.py for 'error-classification untested', but openai_compatible.py is a thin subclass of OpenAIProvider that overrides only build/params_schema/supports — it contains NO error-classification code. The actual classifier `_map_openai_error` lives in openai_provider.py:382 and IS tested (test_openai_provider_error_mapping.py covers 429/Retry-After/401/etc.), and OpenAICompatibleProvider inherits it unchanged. So the substance is RESOLVED/misdirect |
| NEW-fresh-quality-6 | Low | quality | NEW | high | `python/packages/corlinman-nodebridge/, python/packages/corlinman-wstool/, python/packages/corlinman-shadow-tester/` | Three full packages are dead: zero external code importers, no [project.scripts] entry points, and absent from the root meta-package dependencies (so `uv sync` of the meta-package never even pulls them). They are Rust-port leftovers nobody wired into the server or agent. |
| NEW-fresh-quality-8 | Low | quality | NEW | high | `python/packages/corlinman-channels/src/corlinman_channels/{discord.py:718-741,feishu.py:744-757,slack.py:643-654,telegram_send.py:420-438}` | `_note_retry_after(self, resp)` is duplicated nearly verbatim across 4 channel adapters: same default `retry_after = 1.0`, same `try: float(...)` parse, same `self._edit_rate_limit_until = time.time() + retry_after`. The only real variation is where the value is read (JSON body vs header vs telegram parameters). Duplicate 429 back-off logic that should be a shared helper / mixin. Distinct from backlog QUAL-011 (RFC3339 formatter dup). |
| NEW-fresh-quality-9 | Low | quality | NEW | high | `.importlinter:17-33` | Even setting aside the broken embedding reference, the layering contract is incomplete: only 5 root packages are declared (grpc, providers, agent, embedding[deleted], server) out of 26 workspace packages. 21 packages (channels, hooks, identity, evolution-engine, evolution-store, memory-host, mcp-server, subagent, skills-registry, persona, episodes, goals, replay, canvas, tagmemo, auto-rollback, user-model, agent-brain, nodebridge, wstool, shadow-tester) sit in no layer, so cross-layer imports in |
| NEW-providers-5 | Low | quality | NEW | medium | `python/packages/corlinman-subagent/src/corlinman_subagent/supervisor.py:569 (_safe_emit_obs)` | Uses the deprecated asyncio.get_event_loop() to schedule the W3.2 observability emit. On Python 3.12 this emits a DeprecationWarning and, if ever called from a thread/context without a running loop, raises RuntimeError (only partially guarded — the surrounding try catches RuntimeError but the intent was get_running_loop()). The sibling code in reasoning_loop.py:858 correctly uses get_running_loop(); supervisor diverges. |
| NEW-verifyqualtest-1 | Low | quality | CONFIRMED | high | `python/packages/corlinman-agent/src/corlinman_agent/session.py:1-17` | current: corlinman_agent/session.py is a 17-line pure stub (docstring + future import + structlog logger; zero classes/functions, TODO(M2) to implement a pydantic Session). It has no importers anywhere. expected: implemented or removed. (The 'session.py/cancel.py stubs' backlog note — session.py is the genuine dead stub; cancel.py actually implements with_timeout but its promised `combine` is unimplemented and cancel.py also has zero importers.) |
| QUAL-008 | Low | quality | CONFIRMED | high | `python/packages/corlinman-agent/src/corlinman_agent/approval_gate.py:1-17` | current: 17-line stub module — module docstring + `from __future__ import annotations` + `import structlog` + a module-level logger; defines zero classes/functions (only a TODO(M2)). expected: implemented or removed. No symbols, no importers; confuses contributors looking for the approval gate (the real one is corlinman-server gateway/middleware/approval.py). |
| QUAL-009 | Low | quality | CHANGED | high | `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:2021-2083` | current: RedisJournalBackend.open() raises NotImplementedError and open_backend_from_env routes CORLINMAN_JOURNAL_BACKEND=redis to it. backlog framed this as dead-code/defect; on HEAD it is an INTENTIONAL fail-loud stub (comment lines 2014-2018, 2056-2059) so a misconfigured HA deployment errors at startup instead of silently writing a local file other gateways can't read. The redis branch is only reachable when an operator explicitly sets the env var. Not a defect — flag CHANGED so the backlog  |
| QUAL-012 | Low | quality | CONFIRMED | high | `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/sessions.py:617-620` | current: route handler calls `journal._load_messages(tid)` — reaching across a module boundary into a leading-underscore (semi-private) method on AgentJournal. The comment at :617 itself flags it as "semi-private but stable". agent_journal.py:234 defines `_load_messages` only as a backward-compat alias to the public `load_messages`. expected: call the public `load_messages` (or journal exposes a public accessor). This is the exact pattern the backlog says caused commit ef4e341. |


## Full detail (evidence + verify per finding)

### NEW-completeness-backend-1 — High completeness (NEW, conf=high) — scout:fresh-completeness-backend
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_b/onboard.py:689`
- **problem**: current: POST /admin/onboard/finalize-image-provider with choice="reuse" ALWAYS returns 409 CONFLICT and never confirms image capability, even for genuinely image-capable providers (e.g. one exposing dall-e-3). expected: the route probes the provider and accepts when image generation is supported. The first-run wizard's 'reuse my current API key for image generation' step is broken for every provider.
- **evidence**: onboard.py line 689 calls the probe synchronously: `result = probe(entry)` then `if isinstance(result, dict): ... else: getattr(result, "supported", False)`. But `probe_image_capability` in corlinman-providers/src/corlinman_providers/capabilities.py:234 is `async def probe_image_capability(provider) -> dict`. Calling it without `await` yields a coroutine object, NOT a dict, so `isinstance(result, dict)` is False, the else-branch runs `getattr(<coroutine>, 'supported', False)` -> False, and `evidence` becomes the literal 'probe_image_capability'. With supported=False the handler returns 409 (onboard.py:721-723). The coroutine is also never awaited (RuntimeWarning + leaked outbound HTTP probe). Note the stale '# TODO: Agent C ... module not yet importable' comments at lines 671/709 are wrong: capabilities.py exists and probe_image_capability is defined and used correctly (with await) in routes_admin_b/image_provider.py:155 — only onboard.py forgot the await. Repro (/tmp/repro_probe.py): an async probe returning {supported:True} produces type(result)=coroutine, supported=False, evidence='probe_image_capability'.
- **verify**: Async test: POST /admin/onboard/finalize-image-provider {choice:'reuse', provider_name:'openai'} with a provider whose /v1/models lists 'dall-e-3' (mock probe_image_capability to return {'supported':True,...}). Currently returns 409; should return 200 supported. Unit-level: assert the onboard handler awaits probe (asyncio.iscoroutinefunction guard) and yields supported=True.

### NEW-completeness-backend-2 — High completeness (NEW, conf=high) — scout:fresh-completeness-backend
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/openai_provider.py:280; python/packages/corlinman-episodes/src/corlinman_episodes/embed.py:24`
- **problem**: current: the entire embeddings / dense-retrieval feature is dead. The [embedding] config block is parsed (specs.py EmbeddingSpec, lifecycle/py_config.py:157), seeded by the onboarding wizard (onboard.py:329), and exposed via /admin/embedding, but NO production code path ever constructs an embedder or calls .embed(). Every real provider's embed() raises NotImplementedError (OpenAIProvider.embed line 281 'lands in M3', google_provider.py:178, bedrock_provider.py:349). The episodes embedding sweep (populate_pending_embeddings) takes an injected EmbeddingProvider callable whose 'production wiring' (register_embedding_provider_factory) is NEVER called outside the episodes package. expected: configuring [embedding] should enable episode/RAG semantic retrieval.
- **evidence**: (1) Only non-test `.embed(` call in the whole repo is declarative.py:176 delegating to inner (which raises NotImplementedError for openai/google/bedrock). (2) `grep register_embedding_provider_factory / register_summary_provider_factory` across python/ returns ONLY the episodes package itself (cli.py defines it, __init__.py re-exports) — no gateway/server caller, so `_resolve_embedding_provider` raises SystemExit('no embedding provider registered') unless run with --stub-embedding-dim. (3) embed.py docstring references `corlinman_embedding.router.EmbeddingRouter` and 'production wiring in iter 6+' — but there is NO corlinman-embedding package and NO EmbeddingRouter class anywhere (find/grep both empty). (4) The episodes resolver (gateway/placeholder/episodes_stub.py:399-447) does ONLY recency/importance/kind ordering on summary_text — it never reads the embedding BLOB column or does cosine search, so even backfilled vectors are never used for retrieval.
- **verify**: Boot the gateway with a [embedding] section and run the episodes embed-pending CLI without --stub-embedding-dim: it exits with 'no embedding provider registered'. Then grep the running process for any EmbeddingRouter construction — none exists. Acceptance: an end-to-end test where [embedding] is configured and an episode summary is embedded then retrieved by semantic similarity (currently impossible).

### NEW-verify-functional-1 — High completeness (CONFIRMED, conf=high) — scout:verify-functional
- **loc**: `ui/lib/mocks/nodes.ts:24-26 + ui/app/(admin)/nodes/page.tsx:24,52-57`
- **problem**: current: /nodes admin page (shipped in sidebar nav at sidebar.tsx:139, label nav.nodes, isDeveloper) is permanently empty — it polls fetchRunnersMock() every 5s, which is `async function fetchRunnersMock(): Promise<Runner[]> { return []; }` (a hardcoded empty array). expected: it should fetch the live runner registry. No real backend endpoint exists: the UI/i18n strings (en.ts:1565,1568; zh-CN.ts:1530,1532) and the page TODO(B4-BE3) all reference a future GET /wstool/runners endpoint that is NOT implemented anywhere in python/ (repo-wide grep for 'wstool/runners' in Python returns zero hits). The corlinman-wstool package holds a live runner registry (registry.py:82 self.runners dict, runner_count() at :132) but no HTTP route exposes it, and the gateway never mounts the wstool server as an HTTP surface (config_watcher.py only lists 'wstool' as a config-section name). Because query.isError stays false on an empty success, the page renders EmptyBlock forever, never OfflineBlock. The NodeDetailDrawer 'reconnect' action is also inert (page.tsx:214 onReconnect={undefined}).
- **evidence**: ui/lib/mocks/nodes.ts:24 `export async function fetchRunnersMock(): Promise<Runner[]> { return []; }`; page.tsx:52-57 wires it as the React Query queryFn. Wiring gap: no apiFetch call exists in any ui/components/nodes/* file (grep -rln apiFetch ui/components/nodes = empty), and no /wstool/runners route exists server-side.
- **verify**: Boot the gateway, log in, open /nodes — page shows EmptyBlock indefinitely. curl any guessed runner-list path (e.g. GET /wstool/runners, /v1/wstool/runners) -> 404 (route does not exist). Grep proof: `grep -rn 'wstool/runners' python/ | grep -v .venv` returns nothing.

### NEW-verify-functional-2 — High completeness (CONFIRMED, conf=high) — scout:verify-functional
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes/register.py:123-136 + lifecycle/entrypoint.py:1064-1070 + grpc/plugin_invoker.py:198-206`
- **problem**: current: the plugin async-callback surface (POST /v1/plugins/callback/{task_id} and legacy /plugin-callback/{task_id}) is PERMANENTLY 501 in production, AND the parking side is also unwired, so the whole async-plugin (accepted_for_later/task_id) feature is a no-op end to end. expected: an async plugin returns {task_id}, the gateway parks the tool call on a shared AsyncTaskRegistry, the plugin POSTs the real result to the callback route, and the parked tool call wakes. Two independent gaps: (1) register.py:123 only builds a real router when state.plugin_async_tasks is not None, else falls to plugin_callback.router(None) (register.py:136) which returns 501 not_implemented (plugin_callback.py:51-61). But entrypoint.py:1064-1070 constructs GatewayState as `gateway_state_cls(app_state=state)` and NEVER sets plugin_async_tasks — repo-wide there is zero assignment to plugin_async_tasks (only the dataclass default `= None` at register.py:78). So every callback is 501. (2) plugin_invoker.py:198-206 handles output.kind=='accepted_for_later' by merely returning {"status":"accepted","task_id":...} to the model verbatim; it never calls registry.register(task_id), so nothing is ever parked anyway. The only AsyncTaskRegistry instantiation in the codebase is the unit test (corlinman-providers/tests/plugins/test_async_task.py); no production code creates one.
- **evidence**: register.py:136 `parent.include_router(plugin_callback.router(None))`; plugin_callback.py:52-60 returns HTTP_501_NOT_IMPLEMENTED with {"error":"not_implemented"} when registry is None; entrypoint.py:1064-1068 `gw_state = gateway_state_cls(app_state=state) ...` (no plugin_async_tasks kwarg, no later mutation); plugin_invoker.py:202-206 surfaces the task_id without any registry.register() park.
- **verify**: curl -X POST http://127.0.0.1:8080/v1/plugins/callback/tsk_anything -d '{}' (with valid api key) -> HTTP 501 {"error":"not_implemented"}. Also: drive an async plugin that returns accepted_for_later — the model receives {status:accepted,task_id:...} but the tool call is never parked and no future ever resolves. Grep proof: `grep -rn plugin_async_tasks python/.../corlinman-server/src` shows only register.py (default + reads), no writer.

### NEW-fresh-sec-channels-deploy-1 — High security (NEW, conf=high) — scout:fresh-sec-channels-deploy
- **loc**: `deploy/install.sh:709-728`
- **problem**: Native systemd install runs the internet-facing gateway as root. The generated corlinman.service unit has NO User=/Group= directive, so a systemd system unit defaults to running as root. The gateway binds BIND=0.0.0.0 (line 721), serves the admin REST API, and drives channel/agent/plugin code paths. Current vs expected: should run under a dedicated unprivileged system user (as the Docker path already does via `useradd --system corlinman` + `USER corlinman` in docker/Dockerfile:114-116,154); native path grants full root to the whole runtime.
- **evidence**: corlinman.service heredoc (install.sh:709-728) contains [Service] Type=simple / ExecStart=${uv_path} run corlinman-gateway ... with no `User=` line, while `Environment=BIND=0.0.0.0` (line 721) exposes it on all interfaces. Compare deploy/install.sh:756 where the *upgrader* unit explicitly sets `User=root` — the gateway unit simply omits it and thus also runs as root. Memory note deploy_prod_server_native.md confirms production (43.133.12.98) runs this native systemd mode. Any RCE/SSRF/path bug reachable through the gateway (or any agent/plugin it spawns) therefore executes with uid 0.
- **verify**: On a native-mode install, run `systemctl show corlinman -p User -p MainPID` and `ps -o user= -p $(systemctl show -p MainPID --value corlinman)` — observe `root`. Acceptance: the unit should carry `User=corlinman` (or a DynamicUser) and DATA_DIR ownership adjusted accordingly.

### NEW-fresh-sec-channels-deploy-2 — High security (NEW, conf=high) — scout:fresh-sec-channels-deploy
- **loc**: `deploy/install.sh:682 + deploy/install.sh:748-765`
- **problem**: Local privilege escalation via writable root-executed upgrade scripts. install.sh chowns the ENTIRE prefix (`sudo chown -R "$(id -u):$(id -g)" "$PREFIX"`, lines 481 and 682) to the non-root install user, so $PREFIX/repo/deploy/corlinman-upgrader.sh and $PREFIX/repo/deploy/install.sh become owned/writable by that unprivileged user. The corlinman-upgrader.service unit (User=root) does `ExecStart=/bin/bash ${PREFIX}/repo/deploy/corlinman-upgrader.sh` (line 760) and the script in turn runs `bash ${INSTALL_PREFIX}/repo/deploy/install.sh --upgrade` as root. The upgrade is triggered by writing $DATA_DIR/.upgrade-request, watched by corlinman-upgrader.path (lines 767-774). All of the request-validation (tag regex, GitHub release whitelist) protects the *tag* but none of it protects the *script body itself*: a process running as the install user can overwrite corlinman-upgrader.sh with arbitrary commands, then drop any .upgrade-request file to make systemd execute it as root.
- **evidence**: deploy/install.sh:682 `sudo chown -R "$(id -u):$(id -g)" "$PREFIX"` (PREFIX defaults /opt/corlinman, line 89; includes repo/deploy). deploy/install.sh:760 `ExecStart=/bin/bash ${PREFIX}/repo/deploy/corlinman-upgrader.sh` with `User=root` (line 756). deploy/corlinman-upgrader.sh:268 `bash "$INSTALL_SH" --upgrade --version "$TAG"` where INSTALL_SH=${INSTALL_PREFIX}/repo/deploy/install.sh (line 68). The path-unit trigger file $DATA_DIR/.upgrade-request (install.sh:773) lives under DATA_DIR which is likewise install-user-owned. Net: install-user write access -> root code exec.
- **verify**: On a native install: `ls -l /opt/corlinman/repo/deploy/corlinman-upgrader.sh` shows install-user owner+write bit. As that user: append `id > /tmp/pwned` to the script, then `printf '{"request_id":"<uuid>","tag":"v9.9.9"}' > $DATA_DIR/.upgrade-request` and observe /tmp/pwned written by uid 0 (even though the tag fails the release check, the malicious line runs before/around validation when the body is attacker-controlled). Acceptance: root-executed scripts must be owned by root and not group/other-writable.

### NEW-fresh-sec-gateway-1 — High security (NEW, conf=high) — scout:fresh-sec-gateway
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:423 (and entrypoint.py:1074-1078, middleware/auth.py:145)`
- **problem**: The /v1/voice WebSocket is exposed with NO authentication. The gateway's only /v1 gate is ApiKeyAuthMiddleware, a Starlette BaseHTTPMiddleware, which is never invoked for WebSocket scopes. The voice router is mounted unconditionally at boot (entrypoint.py:1078 -> routes_voice.mod.router()), and run_voice_session performs subprotocol negotiation + a per-tenant daily budget check but never verifies a bearer/API key. When an operator turns voice on ([voice] enabled=true with budget_minutes_per_tenant_per_day>0), any unauthenticated network client can open ws://host/v1/voice and drive a real OpenAI Realtime provider session billed to the operator's OpenAI key (resolve_voice_provider -> _resolve_openai_api_key). The tenant is taken straight from the client-controlled X-Tenant-Id header (VoiceState.resolve_tenant, mod.py:238) with no auth, so an attacker can also rotate that header to mint a fresh per-tenant budget on each connect, bypassing/exhausting any other tenant's voice budget (the budget keyed off the spoofable header is the only control). This is distinct from R1-001/R2-001/SEC-007, which only added HTTP path-prefix gating ('/v1/','/memory/','/canvas/','/channels/','/plugin-callback/') and structurally cannot cover the WebSocket upgrade.
- **evidence**: mod.py:423 `@api.websocket("/v1/voice")` -> run_voice_session, which contains zero token/api-key checks (grep shows no require_api_key/authorization handling in the WS path). mod.py:238 `raw = websocket.headers.get("x-tenant-id")` selects tenant unauthenticated. build_voice_state_from_app (mod.py:387) constructs VoiceState WITHOUT a tenant_resolver, so the header default is used; grep confirms no tenant_resolver/VoiceState( wired anywhere else in the gateway. Proven the gate cannot fire on WS: a Starlette repro with a BaseHTTPMiddleware + WebSocketRoute showed 0 dispatch() invocations during the WS handshake and the endpoint received the spoofed `x-tenant-id: victim` header. evaluate_budget (cost.py:246) is the only gate and is keyed on that header.
- **verify**: With [voice] enabled=true and budget_minutes_per_tenant_per_day=1 in config, run a pytest that drives run_voice_session (or TestClient.websocket_connect('/v1/voice')) with NO Authorization/X-API-Key header and an arbitrary `X-Tenant-Id: attacker-<rand>`; assert the socket is accepted and a session 'started' event is emitted (provider session opened) -- demonstrating both the missing api-key gate and per-connect tenant-budget reset. Compare against /v1/chat/completions which 401s without a key.

### NEW-fresh-bug-server-1 — High bug (NEW, conf=high) — scout:fresh-bug-server
- **loc**: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:1019-1130 (append_message / append_messages BEGIN IMMEDIATE) vs 873-953,961-974,1578-1584 (complete_turn/error_turn/append_event autocommit)`
- **problem**: SqliteJournalBackend shares ONE aiosqlite.Connection across all sessions, but mixes two transaction models on it: append_message/append_messages/delete_session/append_events_batch open an explicit `BEGIN IMMEDIATE`...COMMIT envelope, while complete_turn, error_turn, _populate_turn_aggregates, append_event, update_turn_cost issue bare autocommit `execute(...)` + `commit()`. The per-session asyncio lock in agent_servicer (_lock_for/_session_locks) only serialises RPCs of the SAME session_key; two DIFFERENT sessions run concurrently and share the same connection. While session A is between its `BEGIN IMMEDIATE` and `commit` (it awaits at the SELECT MAX(seq) and each INSERT), the event loop can switch to session B whose complete_turn/error_turn/append_event runs `UPDATE...; commit()` on the SAME connection — B's commit() commits A's still-open transaction's partial INSERTs prematurely, breaking append_messages' documented all-or-nothing atomicity (line 1079-1080) and corrupting the replay buffer / aggregates under concurrent multi-session chat load.
- **evidence**: append_messages docstring claims atomicity ('a TypeError mid-transaction can't leave us with a half-applied batch'); but commit() on a shared aiosqlite connection is connection-global. Repro confirmed: with conn shared, session_b's `await conn.commit()` (after a bare INSERT) durably committed session_a's earlier `BEGIN IMMEDIATE` + INSERT('A-partial') while `conn.in_transaction` was already True — final rows contained both A-partial and B-row, i.e. A's uncommitted work was force-committed by B. Trigger path: two clients chat in different sessions; A's per-tool-round `await journal.append_messages(...)` (servicer line 1306) overlaps B's `await journal.complete_turn(...)` (line 1407) / `error_turn` (line 1356) on the same backend connection.
- **verify**: Async test: open one SqliteJournalBackend; begin two turns; coroutine A calls append_messages with an asyncio.sleep injected between BEGIN and commit (or patch to yield), coroutine B calls complete_turn on its own turn concurrently; assert A's batch is NOT visible until A commits and that B's commit did not flush A's partial rows / did not break A's rollback-on-error path. Expect current code to leak A's partial state.

### NEW-fresh-ui-1 — High bug (NEW, conf=high) — scout:fresh-ui
- **loc**: `ui/lib/chat/use-chat-stream.ts:442-468 (editAndRerun) + 308-313 (runTurn payload build)`
- **problem**: edit-and-rerun sends STALE, un-truncated history to the model. Expected: after editing user message N, the model receives history[0..N-1] + edited content. Actual: it receives the full ORIGINAL history (including the messages that were dropped and the pre-edit version of message N) plus the edited content appended at the end.
- **evidence**: editAndRerun() calls setMessages(prev => [...prev.slice(0,idx), next]) to truncate, then `await Promise.resolve(); await runTurn(editedUser)`. runTurn is a useCallback with deps [args, messages, reduceEvent]; editAndRerun closes over the runTurn reference current at render time, whose `messages` closure is the PRE-truncation array. `await Promise.resolve()` is only a microtask — it does NOT cause React to commit the setMessages update or recreate the runTurn callback, so runTurn line 308 `for (const m of messages)` iterates the OLD full list, then line 313 pushes the new edited content. The author's own comment at line 465 ("Wait a tick so the messages state is observed by runTurn closure") shows the intent, but a microtask cannot refresh a useCallback closure. The UI renders the truncated view (setMessages applied for render) while the LLM gets the wrong history — silent divergence. sendMessage is unaffected because it never drops messages.
- **verify**: Unit test: render useChatStream, hydrate([u1, a1, u2, a2]); spy streamChatCompletions; call editAndRerun(u1.id, 'EDITED'); assert the request body `messages` == [{user:'EDITED'}] (or system+EDITED) and does NOT contain a1/u2/a2 or the original u1 text. Currently the body will contain all four original messages plus EDITED.

### NEW-fresh-ui-2 — High bug (NEW, conf=high) — scout:fresh-ui
- **loc**: `ui/app/(admin)/chat/page.tsx:302-308 (ChatArea render) and 149-151,317-331 (pickerOpen/ChatModelPicker)`
- **problem**: The chat composer's model-picker pill is a dead/no-op button on /chat, and the ChatModelPicker can never open. Expected: clicking the model pill (or the /model slash command) opens the picker so operators can override the LLM/image model; the override state (llmOverride/imageOverride, persisted to localStorage) is plumbed but unreachable.
- **evidence**: ChatPage renders <ChatArea sessionKey model conversation initialHistory/> WITHOUT onOpenModelPicker / onOpenPersonaPicker / onOpenImageModelPicker. ChatArea forwards those (undefined) to <Composer>, whose model pill is ALWAYS rendered with onClick={onOpenModelPicker} (composer.tsx:383-392) — so clicking it does nothing, and the /model slash command calls onOpenModelPicker?.() (composer.tsx:124) which no-ops. Meanwhile <ChatModelPicker open={pickerOpen !== null}/> is rendered (chat/page.tsx:317) but setPickerOpen(...) is never called with a non-null value (grep: only setPickerOpen(null) at line 319 in onClose). The whole picker UI + persistOverride machinery is unreachable. (Contrast: models/page.tsx wires setPickerOpen(true) correctly.)
- **verify**: Playwright on /chat?session=...: click [data-testid=composer-model]; assert the ChatModelPicker dialog becomes visible. It currently never appears.

### NEW-providers-1 — High bug (NEW, conf=high) — scout:fresh-bug-agent-providers
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/anthropic_provider.py:433-464 (_split_system)`
- **problem**: AnthropicProvider drops assistant `tool_calls` and never emits Anthropic tool_use/tool_result blocks on the multi-round INPUT path. An assistant message produced by the reasoning loop's _extend_with_tool_round (content="", tool_calls=[...]) is collapsed to {"role":"assistant","content":""} (tool_calls silently discarded), and the following {"role":"tool",...} message becomes a bare {"role":"user","content":<result>}. Anthropic rejects empty assistant text content and a tool_result with no matching tool_use, so EVERY multi-round tool-calling turn on the Anthropic provider fails after the first tool call.
- **evidence**: _split_system maps non-system messages: `anth_role = "user" if role in ("user","tool") else "assistant"` then `chat.append({"role": anth_role, "content": content or ""})` — there is no branch reading msg['tool_calls'] nor any emission of `{"type":"tool_use",...}` / `{"type":"tool_result",...}` blocks. Repro on the exact transcript reasoning_loop._extend_with_tool_round emits yields: messages sent = [user, {assistant, content:''}, {user, '{"temp":65}'}] — tool_calls gone, empty assistant content. This path is LIVE: corlinman-server/agent_servicer.py:1292 & :2698 wire ReasoningLoop.feed_tool_result, driving the loop into the next provider round with these messages.
- **verify**: Add a test that calls AnthropicProvider.chat_stream with messages=[system, user, {role:'assistant',content:'',tool_calls:[{id:'call_1',type:'function',function:{name:'f',arguments:'{}'}}]}, {role:'tool',tool_call_id:'call_1',content:'res'}] using a fake AsyncAnthropic and assert the kwargs['messages'] passed to client.messages.stream contain a tool_use block on the assistant turn and a tool_result block (not an empty-string assistant content).

### NEW-providers-2 — High bug (NEW, conf=high) — scout:fresh-bug-agent-providers
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/bedrock_provider.py:406-423 (_split_system) + _build_anthropic_body:374-403`
- **problem**: Bedrock (Anthropic-on-Bedrock) has the identical multi-round tool-input defect as the native Anthropic adapter: assistant `tool_calls` are dropped and `role="tool"` results are turned into bare user messages with no tool_use/tool_result correlation. Multi-round tool calling against Anthropic-on-Bedrock breaks after the first tool call.
- **evidence**: bedrock _split_system: `anth_role = "user" if role in ("user","tool",None) else "assistant"; chat.append({"role": anth_role, "content": content or ""})` — never reads tool_calls, never builds tool_use/tool_result content blocks. _build_anthropic_body forwards this `chat` verbatim as `messages` to the Bedrock Messages API. Same live path as NEW-providers-1 (feed_tool_result wired in agent_servicer).
- **verify**: Feed the same assistant-tool_calls + tool-result transcript through _build_anthropic_body and assert body['messages'] carries tool_use/tool_result blocks rather than an empty assistant content string.

### NEW-fresh-perf-1 — High performance (NEW, conf=high) — scout:fresh-perf
- **loc**: `python/packages/corlinman-server/src/corlinman_server/system/subagent/store.py:250-275, 296, 313, 403, 424, 443`
- **problem**: PersistentSubagentStore rewrites its ENTIRE state to disk on every mutation and never prunes terminal rows, giving unbounded memory growth + O(N) write amplification per mutation (O(N^2) over process lifetime). current: _flush_locked() serializes the full _requests + _statuses dicts to JSON and does a synchronous tmp-write + os.replace, and is called from begin()/update()/append_log()/set_killed()/set_summary() after EVERY change; _statuses/_requests are never pruned and are reloaded from disk on boot. expected: prune/evict terminal entries (cap or TTL), and avoid full-state rewrite per log chunk (append-only or debounced flush), with disk I/O offloaded off the event loop.
- **evidence**: _flush_locked builds payload={'requests':{rid:req.to_json() for ...}, 'statuses':{rid:status.to_json() for ...}} (store.py:252-259) then tmp.write_text(json.dumps(payload,...)); os.replace(tmp, path) (266-269) — full O(N) serialize + sync blocking write. Called per spawn: dispatcher.py:311 store.begin(req) -> _flush_locked (296); dispatcher.py:312/404/454 store.update(...) -> _flush_locked (313). No del self._statuses / prune / maxlen anywhere (grep found none). count_in_flight_for_tenant (367-384) also scans ALL statuses incl. terminal on every spawn (dispatcher.py:303) = O(N) per spawn. append_log (386-403) calls _flush_locked per chunk (latent today; would be O(N) full rewrite per streamed log chunk). Hot path: every subagent_spawn / subagent_spawn_many tool call (agent_servicer.py:1865, 1885) drives begin+update transitions.
- **verify**: Unit/bench: open PersistentSubagentStore on a tmp file; loop begin()+update(completed) for N=10k requests; assert _statuses len keeps growing (no prune) and time/IO per begin() scales with N (instrument _flush_locked write size). Add a regression asserting flush bytes stay bounded after a prune cap.

### PERF-005 — High performance (CONFIRMED, conf=high) — scout:verify-perf
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/services/direct_backend.py:294,302,313`
- **problem**: O(K^2) string accumulation in the streaming tool-call argument assembler. Each provider arguments_delta fragment is appended to an immutable Python str via `+=`, reallocating the whole accumulated string every delta.
- **evidence**: L294 `open_calls[call_id] = [name, ""]` (slot [1] is a str), then L302 `open_calls[call_id][1] += args0` (tool_call_start), L313 `open_calls[call_id][1] += frag` (tool_call_delta). For a tool call whose arguments JSON of length K streams over M deltas, each `+=` copies the existing string → total O(K^2) copy work + O(K^2) transient allocation. Trigger: any real provider stream that emits tool-call arguments incrementally (the normal case for large code-edit / structured-output tool args). The assembled `args` then flows to `_tool_call_frame(call_id, name, args, seq)` at L324/334/356.
- **verify**: Feed a fake stream of ~2000 tool_call_delta chunks each 50 bytes through `_relay_stream`; profile time vs a list.append+''.join variant — expect superlinear growth as fragment count rises.

### PERF-006 — High performance (CONFIRMED, conf=high) — scout:verify-perf
- **loc**: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:1310-1321`
- **problem**: list_session_summaries (powers /admin/sessions) still uses three correlated subqueries per grouped session row. R4-D6 only added the `turn_id DESC` tie-breaker for preview/status; it did not remove the correlated-subquery cost.
- **evidence**: Inside the grouped SELECT: L1310-1313 message_count = `(SELECT COUNT(*) FROM turn_messages tm WHERE tm.turn_id IN (SELECT turn_id FROM turns WHERE session_key = t.session_key))` — a nested correlated subquery (outer turns-group × inner turns scan × turn_messages scan). L1314-1317 and L1318-1321 add two more correlated `(SELECT … FROM turns WHERE session_key = t.session_key ORDER BY started_at_ms DESC, turn_id DESC LIMIT 1)` subqueries (last_user_text, last_status). For S sessions averaging T turns and M messages/turn, cost ≈ O(S × (T + T·M)) absent covering indexes. Trigger: every /admin/sessions list render.
- **verify**: Seed a journal with ~500 sessions × ~30 turns × ~10 messages, EXPLAIN QUERY PLAN the statement and time list_session_summaries; compare against a single GROUP BY + window-function (ROW_NUMBER) rewrite.

### NEW-fresh-test-1 — High test (NEW, conf=high) — scout:fresh-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/cost.py:240 (evaluate_budget), :310 (SessionMeter.poll), :133 (InMemoryVoiceSpend.add_seconds) + budget.py:110 (BudgetEnforcer.tick), :132 (finalize), :140 (_checkpoint_delta)`
- **problem**: current: the entire per-tenant voice money/quota subsystem has ZERO test coverage. expected: the budget admission gate, mid-session termination, and delta-checkpoint billing arithmetic should each have unit tests since this is a money path and a live WS gate.
- **evidence**: `grep -rln 'evaluate_budget|InMemoryVoiceSpend|BudgetDecision|day_budget_exhausted|BudgetEnforcer|SessionMeter|MeterTick|add_seconds|VoiceSpend' tests/` returns NOTHING for the budget/meter logic (only test_route_integration.py mentions the word 'budget' once, at line 117 setting `budget_minutes_per_tenant_per_day=60`, but never drives the enforcer). The code is live-wired: mod.py:537 `decision_budget = evaluate_budget(cost_cfg, today, reset_at); if not decision_budget.allowed: await _close_budget_exhausted(...)` is the session-admission gate, and mod.py:663 `BudgetEnforcer.start(...)` runs the 1-Hz ticker. The arithmetic is non-trivial: SessionMeter.poll (cost.py:398+) decides MAX_SESSION_SECONDS hard-kill (close 4001) vs DAY_BUDGET_EXHAUSTED (close 4002) vs one-shot warn; BudgetEnforcer._checkpoint_delta (budget.py:140) does delta-only billing (`delta = elapsed - self._last_checkpointed`). A silent off-by-one or sign flip here = either unbilled session overruns (revenue loss) or false mid-call terminations (broken feature). InMemoryVoiceSpend.add_seconds (cost.py:133) is guarded by a threading.Lock specifically to prevent double-billing on a tick race (comment cost.py:117-118) — that concurrency invariant is also untested.
- **verify**: Add tests/gateway/routes_voice/test_cost.py: (a) evaluate_budget returns deny(day_budget_exhausted) when snapshot.seconds_used >= cap and allow(remaining) otherwise, plus budget_is_zero when cap==0; (b) SessionMeter.poll returns terminate(MAX_SESSION_SECONDS, 4001) at elapsed>=max_session_seconds, terminate(DAY_BUDGET_EXHAUSTED, 4002) at start_seconds_used+elapsed>=cap, and a one-shot budget_warn 60s before cap (assert warn fires exactly once); (c) BudgetEnforcer.tick checkpoints only the delta (drive tick at t=0,5,5,12 against an InMemoryVoiceSpend and assert add_seconds was called with 5 then 0 then 7, last_checkpointed==12); (d) finalize flushes the final delta; (e) concurrency: spawn N threads calling InMemoryVoiceSpend.add_seconds and assert seconds_used == N*delta (no lost update).

### NEW-fresh-test-2 — High test (NEW, conf=high) — scout:fresh-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes/channels.py:73 (_verify_secret), :88 (_handle), :156 (POST /v1/channels/telegram/webhook)`
- **problem**: current: the Telegram-webhook route's secret-token authentication gate (`_verify_secret`) and the inbound update-injection handler have NO test. expected: the security gate that decides whether an unauthenticated caller may inject a Telegram Update into the agent loop must be covered (mismatch -> 401, empty-config -> open, valid secret -> 200).
- **evidence**: `grep -rln 'channels/telegram/webhook|telegram_webhook|TelegramWebhookState|_verify_secret|SECRET_HEADER|X-Telegram-Bot-Api-Secret-Token|gateway.routes.channels' tests/` in corlinman-server returns NOTHING (exit 1). This route is the public ingress for Telegram: `_handle` (channels.py:88) calls `if not _verify_secret(state.secret_token, secret_header): return 401` then decodes the JSON body and runs `process_update(ctx, update)` which drives an agent reply. _verify_secret (channels.py:79) treats empty `configured` as DISABLED (returns True -> anyone can inject). If a refactor ever drops the gate, inverts the bool, or breaks the constant-time compare, an attacker could spoof Telegram updates / drive the agent unauthenticated — and no test would catch it. NOTE: this is a DIFFERENT untested function from the channels-package `verify_secret` the backlog mentions; this is the gateway-route copy at routes/channels.py.
- **verify**: Add tests/gateway/routes/test_telegram_webhook.py with a FastAPI TestClient mounting router(TelegramWebhookState(secret_token='s3cret',...)): (1) POST with correct X-Telegram-Bot-Api-Secret-Token -> 200 {ok:true}; (2) POST with wrong/missing header -> 401 'unauthorized'; (3) configured secret_token='' -> request without header is accepted (document the fail-open); (4) malformed JSON body with valid secret -> 400 invalid_update; (5) assert process_update is invoked only after the secret check passes (mock it).

### NEW-verifyqualtest-2 — High test (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:119-132`
- **problem**: current: token_config_to_acl translates per-token MCP ACLs (tools_allowlist, resources_allowed, prompts_allowed, tenant_id with DEFAULT_TENANT_ID fallback via TokenAcl.effective_tenant) — i.e. the cross-tenant authorization boundary for MCP tokens — with zero tests. expected: tests proving an empty/missing tenant_id falls back to DEFAULT_TENANT_ID and a set tenant_id is preserved (cross-tenant ACL not leaked). A regression here silently widens or mis-scopes tenant access on the MCP surface.
- **evidence**: server.py:119-132 builds TokenAcl(tenant_id=t.tenant_id) and docstring at :121-122 says empty/missing tenant_id falls back to DEFAULT_TENANT_ID. No test references token_config_to_acl (grep across python/packages/*/tests empty).
- **verify**: add test: token_config_to_acl(McpTokenConfig(token='t', tenant_id=None)).effective_tenant == DEFAULT_TENANT_ID and with tenant_id='acme' it stays 'acme'.

### TEST-006 — High test (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:1-209`
- **problem**: current: 209-line MCP server module with public funcs token_config_to_acl (:119), build_server_config (:135), build_dispatcher (:144), build_mcp_server (:170) and config dataclasses — zero tests. The tests/gateway/mcp/ directory exists but is EMPTY (0 files). expected: tests covering config->ACL translation, dispatcher build, server build. token_config_to_acl is security-relevant (see token_config_to_acl finding).
- **evidence**: `ls tests/gateway/mcp/` → empty dir. `grep -rln 'gateway.mcp.server\|from corlinman_server.gateway.mcp' python/packages/*/tests` → no matches.
- **verify**: add tests/gateway/mcp/test_server.py asserting build_server_config maps tokens and token_config_to_acl preserves/derives tenant_id; currently no test imports the module.

### TEST-008 — High test (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/home_channel_store.py:1-335`
- **problem**: current: 335-line /sethome SQLite store (set_home/get_home/list_all_homes/mark_tip_shown/resolve_user_id, idempotent PK on user_id) actively used by channels/commands.py, gateway/lifecycle/entrypoint.py, and gateway/services/chat_bootstrap.py — zero tests. expected: tests for set/get round-trip, idempotent re-/sethome on user_id PK, resolve_user_id channel+sender mapping, mark_tip_shown.
- **evidence**: `grep -rln 'home_channel_store\|HomeChannelStore\|sethome\|set_home' python/packages/*/tests` → no matches. Real consumers: channels/commands.py, entrypoint.py, chat_bootstrap.py.
- **verify**: add tests/test_home_channel_store.py: set_home then get_home returns the row; re-set_home with a different channel for same user_id overwrites (PK on user_id); no test currently imports the module.

### TEST-009 — High test (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/middleware/admin_session.py:84-145`
- **problem**: current: AdminSessionStore TTL expiry (validate, :95), sliding-refresh (replace last_used=now, :101-102), gc() eviction (:119) and the background sweep task start_gc (:140, tick ttl/4 min 60s) have NO direct unit test. Many test files reference admin sessions but only as auth fixtures (mint via _admin_auth.py) — none exercise expiry/gc/sliding behavior. expected: unit tests on AdminSessionStore lifecycle.
- **evidence**: `grep -rn 'from corlinman_server.gateway.middleware.admin_session import\|middleware.admin_session import admin_session' python/packages/corlinman-server/tests` → no direct importer of the module's store. admin_session.py:16-17 docstring promises ttl expiry + gc every ttl/4; logic at :95 (elapsed>ttl evict), :101-102 (sliding), :119-138 (gc).
- **verify**: add test: store=AdminSessionStore(ttl_seconds=0.01); tok=store.create('u'); after sleep validate(tok) is None and len(store)==0; validate within ttl bumps last_used (sliding).

### NEW-fresh-quality-1 — High quality (NEW, conf=high) — scout:fresh-quality
- **loc**: `.importlinter:21-23,31 (root) + .github/workflows/ci.yml:266`
- **problem**: The layering contract names corlinman_embedding as a root package and as a layer, but that package was DELETED in commit 50515f6 ('refactor: delete the corlinman-embedding package'). import-linter aborts before evaluating any contract, so the layering guard is silently 100% disabled.
- **evidence**: `uv run lint-imports` -> 'Could not find package corlinman_embedding in your Python path.' exit code 1. .importlinter line 22 `corlinman_embedding` under [importlinter] root_packages and line 31 `corlinman_providers | corlinman_embedding` under the layers contract. No python/packages/corlinman-embedding/ dir exists (git log -- python/packages/corlinman-embedding shows it deleted). CI boundary-check job (ci.yml:265-266) runs `uv run lint-imports`. Net effect: layering / no-cross-layer-import enforcement has been dead since the embedding deletion; any new layering violation passes unchecked.
- **verify**: From repo root: `uv run lint-imports; echo $?` -> exit 1 with the 'Could not find package corlinman_embedding' message. Removing `corlinman_embedding` from both stanzas makes it run.

### NEW-fresh-quality-2 — High quality (NEW, conf=high) — scout:fresh-quality
- **loc**: `.github/workflows/ci.yml:71 (py-mypy job) — errors across 72 files in python/packages/corlinman-server`
- **problem**: The required py-mypy gate runs `uv run mypy .` which fails with 155 type errors at HEAD (0b8befa) on a git-clean tree. py-mypy IS in the `gate` aggregator needs list (ci.yml:277), so the required branch-protection check is red.
- **evidence**: `uv run mypy .` -> 'Found 155 errors in 72 files (checked 473 source files)', exit 1. Categories include 47 no-any-return, 26 attr-defined, 19 arg-type, 11 union-attr, 4 name-defined, 1 no-redef. Zero are import/stub-resolution errors (generated grpc stubs present). Examples: routes_admin_b/napcat.py:400 `client.check_status()` and :440 `client.quick_login()` on a `_NapcatClient | None` with no None-guard (only the surrounding `async with client` has type:ignore); cli/init.py:214 `kind` redefined (no-redef). `git status --porcelain python/` is empty so this is HEAD, not local drift. Latest main CI run (gh run 26586320541) = failure.
- **verify**: `uv run mypy .; echo $?` from repo root -> 155 errors, exit 1. Matches the exact ci.yml:71 command.

### NEW-fresh-quality-3 — High quality (NEW, conf=high) — scout:fresh-quality
- **loc**: `.github/workflows/ci.yml (py-ruff job) — errors concentrated in python/`
- **problem**: The required py-ruff gate (`uv run ruff check .`) fails with ~1600 errors at HEAD on a git-clean tree. py-ruff is in the gate aggregator. Dominated by 525 RUF100 'unused noqa' (code is littered with `# noqa: BLE001` / `# noqa: PLC0415` for rules that are NOT in the root [tool.ruff.lint] select list, so ruff treats every such directive as an unused-noqa error), plus 200 I001 unsorted-imports and 91 F401 unused-import.
- **evidence**: `uv run ruff check python/ --statistics`: 525 RUF100, 200 I001, 115 SIM105, 100 RUF001, 95 E402, 91 F401, 84 UP017, ... plus a hard `warning: Invalid # noqa directive` at tests/test_codex_provider.py:633. Example RUF100: providers code line `except Exception as exc:  # noqa: BLE001` — BLE is not in select=['E','F','I','N','UP','B','A','C4','SIM','RUF'] (pyproject.toml:75), so RUF100 fires. Also 4 real F821 (Undefined name `Any`) at corlinman-server/tests/test_agent_journal.py:508,512 (file uses `Any` annotations but never `from typing import Any`; only saved from a runtime NameError by `from __future__ import annotations`). ruff 0.15.14.
- **verify**: `uv run ruff check python/; echo $?` -> exit 1, ~1659 errors. `uv run ruff check python/ --statistics` reproduces the rule histogram.

### QUAL-006 — High quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `.importlinter:22,31`
- **problem**: current: `.importlinter` declares `corlinman_embedding` in `root_packages` (line 22) and in the `layers` contract (line 31), but no such package exists on disk (only corlinman-episodes ships an embed.py). The header comment also asserts "every package must appear in exactly one layer so the contract stays total" — yet only 5 of 25 packages are listed (non-total). expected: phantom package removed + comment fixed (or contract made total). IMPACT > cosmetic: `uv run lint-imports` (run by `make lint` Makefile:59 AND the boundary-check CI job, ci.yml:266) aborts with exit code 1 at package resolution — it never evaluates the layering contract at all. The boundary-check CI gate is therefore broken/red, and the layering contract it claims to enforce is silently NOT enforced.
- **evidence**: `.importlinter:22` `corlinman_embedding` (root_packages) + `:31` `corlinman_providers | corlinman_embedding` (layers). `ls python/packages/` shows no corlinman-embedding. Ran `uv run lint-imports` → output `Could not find package 'corlinman_embedding' in your Python path.` and real exit code 1 (captured to /tmp; the earlier `head`-piped 0 was head's exit). Only `Could not find` is printed — no contract line is evaluated.
- **verify**: `uv run lint-imports; echo $?` → exits 1 with 'Could not find package corlinman_embedding'. After removing corlinman_embedding from both stanzas it should evaluate the layers contract and exit 0 (or report a real violation).

### NEW-completeness-backend-3 — Medium completeness (NEW, conf=high) — scout:fresh-completeness-backend
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:60; python/packages/corlinman-server/src/corlinman_server/gateway/lifecycle/entrypoint.py:1944`
- **problem**: current: the gateway-side MCP *server* (`/mcp` WebSocket that exposes corlinman's own tools/resources/prompts to external MCP clients like Claude Desktop) is fully built but never bound at boot. `build_mcp_server` is defined + re-exported but has zero callers outside its own module/tests. Only the MCP *client* (McpClientManager, for consuming external MCP servers) is wired in entrypoint.py:1944. expected (docs/design/phase4-w3-c1-design.md:6,29: 'mounted at /mcp ... point Desktop at ws://gateway/mcp?token='): operators can connect Claude Desktop to the gateway's /mcp endpoint.
- **evidence**: `grep build_mcp_server` across python/ (non-test) returns only gateway/mcp/server.py (defines it) and gateway/mcp/__init__.py (re-exports). No `server.bind`, no `McpServer(` instantiation, no `/mcp` route mount anywhere in gateway/lifecycle/entrypoint.py or main.py. entrypoint.py:1944 imports only `McpClientManager` (the client). The module docstring (server.py:16-18) says boot 'calls await server.bind(host, port)' but nothing does. Design doc phase4-w3-c1-design.md:1 titles the feature 'MCP server (/mcp WebSocket)' and §9 line 402 lists 'mount /mcp' as a step, plus a /admin/mcp/tokens route — none of which the Python boot path performs for the server side.
- **verify**: Boot the gateway and attempt `GET /mcp?token=<bearer>` (WebSocket upgrade). Expected per design: 101 upgrade (or 401 without token). Actual: route not mounted -> 404. Acceptance: a transport test connecting an MCP client to ws://gateway/mcp and listing tools.

### NEW-completeness-backend-4 — Medium completeness (NEW, conf=high) — scout:fresh-completeness-backend
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:387; python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/persistence.py:154`
- **problem**: current: voice session start/end rows are NEVER persisted in production. `build_voice_state_from_app` (the gateway mount path) constructs VoiceState with config_loader/provider/data_dir but never sets `session_store`, which defaults to None (mod.py:225). Every persistence call site guards `if state.session_store is not None` (mod.py:642,767,1387), so record_start/record_end are silently skipped. There is also NO SQLite-backed VoiceSessionStore implementation — only MemoryVoiceSessionStore (tests). expected (persistence.py:154 docstring: 'production uses the SQLite-backed adapter'): voice session history/audit persists to the voice_sessions table.
- **evidence**: mod.py:387-391 `return VoiceState(config_loader=..., provider=..., data_dir=...)` — no session_store/ transcript_sink args. VoiceState.session_store default is None (mod.py:225). The only VoiceSessionStore impl is MemoryVoiceSessionStore (persistence.py:169, 'for tests'). persistence.py:18 states the SQLite swap 'mirrors the Rust SqliteVoiceSessionStore and is left as a [stub]'. grep for `Sqlite.*Voice|class .*VoiceSessionStore` (non-test) finds none. A voice_sessions CREATE TABLE schema exists (persistence.py:44) but no adapter writes it.
- **verify**: Open a /v1/voice WebSocket session against a booted gateway, end it, then query the voice_sessions SQLite table — it stays empty. Acceptance: build_voice_state_from_app should wire a persistent session_store and the table should hold one row per session.

### NEW-completeness-backend-5 — Medium completeness (NEW, conf=high) — scout:fresh-completeness-backend
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/declarative.py:236`
- **problem**: current: a custom declarative provider configured with `auth_kind="header"` (e.g. X-API-Key) or `auth_kind="query_param"` silently sends the wrong auth. load_spec_from_toml validates these auth_kinds as legal (_VALID_AUTH_KINDS line 267) and parses the header_name/param_name into auth_config, but _build_inner ignores them entirely and always constructs an OpenAIProvider that forwards the key as `Authorization: Bearer`. The provider then gets 401s. expected: header/query_param auth kinds apply the declared header/param name.
- **evidence**: declarative.py:236 `_build_inner`: for request_format=='openai_compatible' it returns `OpenAIProvider(api_key=api_key, base_url=spec.base_url)` regardless of auth_kind. The docstring at line 222 admits 'header / query_param auth kinds currently ignore the declared header/param names'. auth_config is documented (line 89-94) to carry header_name='X-API-Key' / param_name='api_key', and load_spec_from_toml stores it (line 326 auth_config=dict(auth_config_raw)), but nothing reads header_name/param_name at request time. _resolve_api_key only pulls env_var. This affects the custom-provider feature in docs/PLAN_PROVIDER_AUTH.md.
- **verify**: Create a declarative provider TOML with auth_kind='header', auth_config={header_name='X-API-Key', env_var='MY_KEY'}, point base_url at a server that only accepts X-API-Key. A chat call sends Authorization: Bearer instead and is rejected. Acceptance: the outbound request carries `X-API-Key: <key>` and no Bearer header.

### NEW-fresh-bug-server-2 — Medium completeness (NEW, conf=high) — scout:fresh-bug-server
- **loc**: `python/packages/corlinman-server/src/corlinman_server/auto_resume.py:76 (_CHANNEL_NEEDS_BOOT_REPLAY) + 121-166 (BootReplayDispatcher.replay)`
- **problem**: current vs expected: auto-resume treats telegram/discord/slack/feishu as channels whose upstream won't redeliver, so it synthesizes a `pending` inbox row and counts it as resumed (resumed+=1, logs agent.resume.replay_enqueued). EXPECTED: a per-channel dispatch loop drains that inbox row and re-runs the chat RPC. ACTUAL: only QQ-family dispatch loops exist (`_qq_dispatch_loop`, `_qq_official...`); there is NO _telegram/_discord/_slack/_feishu dispatch loop, and those channels never enqueue or drain the inbox at all (grep shows inbox usage in service.py is QQ-only). So the synthesized resume row is orphaned forever — the user is never re-driven despite the scan reporting success.
- **evidence**: auto_resume.py:76 `_CHANNEL_NEEDS_BOOT_REPLAY = frozenset({"telegram", "discord", "slack", "feishu"})`; replay() enqueues and returns True → run() does `resumed += 1` and logs `agent.resume.replay_enqueued`. grep of corlinman-channels/service.py: the only dispatch loop consuming the inbox is `_qq_dispatch_loop` (line 672) / qq_official; telegram code paths (`_telegram_try_dispatch_command` line 1763) never call inbox.enqueue/list_pending/mark_dispatched. Module docstring (lines 18-23) and BootReplayDispatcher docstring (line 112-113) describe `_telegram_dispatch_loop` as a 'future' / 'existing _qq_dispatch_loop, future _telegram_dispatch_loop' — i.e. unimplemented.
- **verify**: Integration test: journal an in_progress turn with channel='telegram', run run_boot_auto_resume with a real Inbox; assert report.resumed==1 AND that some telegram dispatch path actually drains the inbox row. The second assertion fails today (no drainer exists), so the 'resumed' count is a false positive.

### NEW-fresh-quality-4 — Medium completeness (NEW, conf=high) — scout:fresh-quality
- **loc**: `docs/ci-status.md:61-96 vs .github/workflows/ci.yml:71`
- **problem**: current vs expected: doc claims the python lint gate was scoped down (`Scoped uv run mypy . to uv run mypy python/packages/`, line 78) and that only 7 mypy + 17 ruff errors remain (lines 63,87,95); expected = doc matches CI. Reality: ci.yml:71 still runs the un-scoped `uv run mypy .`, and the counts are now 155 mypy / ~1600 ruff. The doc materially misrepresents the lint posture, masking that two required gates are red.
- **evidence**: ci-status.md line 78: 'Scoped `uv run mypy .` to `uv run mypy python/packages/` so the top-level scripts/ and stale caches are not traversed.' But ci.yml:70-71 reads `- name: mypy` / `run: uv run mypy .`. ci-status.md line 95 '`uv run mypy python/packages/` -> 7 errors'; actual `uv run mypy .` -> 155, and even `mypy python/packages/` -> 155. ci-status.md line 63 'yellow (7 mypy + 17 ruff)'; actual ruff -> ~1600. Doc last modified May 16; HEAD is May 29.
- **verify**: diff the doc claim against `grep -n 'uv run mypy' .github/workflows/ci.yml` and against live `uv run mypy . 2>&1 | tail -1`.

### NEW-fresh-sec-gateway-2 — Medium security (NEW, conf=medium) — scout:fresh-sec-gateway
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/persistence.py:242 (audio_path_for) and :252 (tts_audio_path_for), consumed at routes_voice/mod.py:637`
- **problem**: audio_path_for/tts_audio_path_for build a filesystem path by interpolating tenant_id verbatim with no sanitization: `Path(data_dir)/'tenants'/tenant_id/'voice'/f'{session_id}.pcm'`. tenant_id originates from the unauthenticated, client-controlled X-Tenant-Id header (see finding #1). A value like `../../../../tmp/x` escapes the per-tenant tree. mod.py:637 already materializes this path into the session-start row; if audio retention is enabled (retain_audio=true) and an audio writer mkdir/open()s this path, an unauthenticated attacker can direct retained PCM writes outside data_dir (path traversal on write). Compounds finding #1 (same unauthenticated entry point + spoofable tenant).
- **evidence**: persistence.py:242 `return Path(data_dir) / "tenants" / tenant_id / "voice" / f"{session_id}.pcm"` with no `..`/separator rejection. tenant flows unauthenticated from mod.py:238 -> resolve_tenant -> persistence row at mod.py:637 (`str(audio_path_for(state.data_dir, tenant, session_id))`).
- **verify**: Unit-test audio_path_for(Path('/data'), '../../etc', 'sid') and assert the resolved path escapes /data/tenants (it does). Then, with retain_audio=true, drive a voice connection with `X-Tenant-Id: ../../../../tmp/pwn` and confirm whether any writer opens the escaped path; if so it is a confirmed traversal-write. confidence is medium because retain_audio defaults False and I did not confirm a concrete write() that uses this exact path in the live boot path.

### SEC-008 — Medium security (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_b/providers.py:615-626`
- **problem**: Admin-configured provider base_url is dialed with no SSRF guard. _query_provider_models builds `url = base_url.rstrip('/') + '/v1/models'` from the operator-supplied `entry_dict['base_url']` and issues `await client.get(url, headers={'Authorization': f'Bearer {api_key}'})` directly. No scheme/host/IP validation (no is_safe_host equivalent like the agent web fetch has). An authenticated admin (or anyone who can write provider config) can probe arbitrary internal/metadata endpoints and exfiltrate the configured API key as a bearer header to an attacker-chosen host.
- **evidence**: L615 `raw_base = entry_dict.get("base_url") or "https://api.openai.com"`; L616 `base_url = str(raw_base).rstrip("/")`; L618 `url = base_url.rstrip("/") + "/v1/models"`; L625-626 `async with _httpx.AsyncClient(timeout=10.0) as client: resp = await client.get(url, headers=headers)`. Reached via the `/admin/providers/{name}/models` route (list_provider_models L1180 → _query_provider_models_with_retry L491/495 → _query_provider_models). httpx follows no SSRF policy; base_url unvalidated. Admin-gated, hence Medium not High.
- **verify**: Configure a provider with base_url=`http://169.254.169.254/latest/meta-data` (or `http://127.0.0.1:<internal-port>`), call GET /admin/providers/<name>/models, observe the server dials the internal address and returns latency/HTTP info; assert is_safe_host-style guard rejects private/loopback/metadata before the client.get.

### SEC-012 — Medium security (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/web/_common.py:266-361 + python/packages/corlinman-agent/src/corlinman_agent/web/fetch.py:160-170`
- **problem**: DNS-rebind TOCTOU in the agent web-fetch SSRF guard. is_safe_host(url) resolves the host via socket.getaddrinfo and classifies the resulting IPs, but does NOT pin them. The subsequent `client.stream('GET', current_url)` dials by hostname, so httpx performs an independent second DNS resolution. A rebinding attacker can answer the guard's lookup with a public IP (passing) and the connect's lookup with 10.0.0.5 / 169.254.169.254 (the actual dial), bypassing the guard. Same gap on every redirect hop (is_safe_host(next_url) at fetch.py:207, then re-dial) and in search.py:177/288.
- **evidence**: _common.py L326 `infos = socket.getaddrinfo(host, None)` validates IPs but returns None (no pinning). fetch.py L160 `is_safe_host(url)` then L166-170 `async with make_client(...) as client: ... async with client.stream("GET", current_url) as response:` — httpx re-resolves `current_url`'s hostname. The validated IP is never used to pin the connection (no resolver override / no connect-by-IP+Host-header). Requires attacker-controlled DNS with low TTL + race timing, hence Medium.
- **verify**: Stand up a DNS name whose first A-record answer is public and second is 127.0.0.1/169.254.169.254; call web_fetch and confirm the connection reaches the internal IP despite is_safe_host passing. Fix: resolve once, pin the validated IP (custom httpx transport/resolver or connect to IP with explicit Host header).

### BUG-006 — Medium bug (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/openai_provider.py:222-238`
- **problem**: When a streamed OpenAI tool-call's first delta arrives without an `id` (common — OpenAI often sends the id only in the first chunk but some compatible servers send it late), the call is opened with a synthetic `f'call_{idx}'` id AND the 'finalized' flag is hardcoded True. The late-id `elif tc_id and not open_calls[idx][1]` branch then checks `not True == False` and is therefore unreachable, so the real server-issued tool_call_id arriving in a later delta is silently dropped. The synthetic id sticks for tool_call_delta/end and is what gets sent back to the model, breaking tool-result correlation for servers that issue ids late.
- **evidence**: L163 `open_calls: dict[int, tuple[str, bool]] = {}`. L222-224 first sighting: `call_id = tc_id or f"call_{idx}"; open_calls[idx] = (call_id, True)` — flag always True even when tc_id was None. L230 `elif tc_id and not open_calls[idx][1]:` — `open_calls[idx][1]` is always True ⇒ condition always False ⇒ dead branch. L240 `call_id = open_calls[idx][0]` keeps the synthetic id. Fix: store flag = `bool(tc_id)` at open time, so the elif can promote the synthetic id to the real one.
- **verify**: Drive chat_stream with a fake stream whose first tool_calls delta has id=None and a later delta supplies id='call_real_123'; assert the emitted tool_call_start/delta/end use 'call_real_123', not 'call_0' (currently fails — synthetic id wins).

### BUG-010 — Medium bug (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:812-840`
- **problem**: begin_turn derives turn_id from wall-clock ms and retries on PK collision with ms+offset for offset in range(0,20). If all 20 inserts collide (>=20 turns started in the same ms window), L839-840 falls through to `return ts` — returning ts+0, the exact id that already collided on the first iteration, WITHOUT any row having been inserted. The caller receives a turn_id that maps to a different, pre-existing turn's row. Subsequent complete_turn/error_turn/message-append against that id mutate the wrong turn's record (cross-turn data corruption), and the new turn has no row at all. The return type int|None gives callers no way to detect the fabricated id (it's a normal int).
- **evidence**: L811 `ts = int(time.time() * 1000)`; L812 `for offset in range(0, 20):`; L813 `tid = ts + offset`; L831-832 success `await conn.commit(); return tid`; L833 `except aiosqlite.IntegrityError: ... continue`; L839-840 `# Vanishingly unlikely; fall through with a tagged turn_id. \n return ts`. Since offset 0 (=ts) already failed with IntegrityError, returning ts hands back a known-colliding id pointing at an existing row. Requires 20 same-ms collisions, hence Medium.
- **verify**: Pre-seed turns table with rows for turn_id ts..ts+19, then call begin_turn with time mocked to ts; assert it raises/returns None rather than returning ts (a foreign existing turn). Confirm a follow-up complete_turn does not stamp the pre-existing colliding row.

### NEW-fresh-ui-3 — Medium bug (NEW, conf=medium) — scout:fresh-ui
- **loc**: `ui/components/sessions/cost-footer.tsx:34-41 and ui/components/sessions/session-cost-cells.tsx:37-44`
- **problem**: Two inline cost fetchers omit the GATEWAY_BASE_URL prefix, so the sessions cost column (every list row) and the session-detail cost footer silently fail (all cells -> '—') whenever NEXT_PUBLIC_GATEWAY_URL is set. Expected: prefix GATEWAY_BASE_URL like every other client call (apiFetch, uploadAsset, all streamX EventSources).
- **evidence**: _loadCostInline does `fetch(\`/admin/sessions/${encodeURIComponent(key)}/cost\`, {credentials:'include'})` with a RELATIVE path. api.ts documents GATEWAY_BASE_URL as an opt-in escape hatch (`NEXT_PUBLIC_GATEWAY_URL=http://localhost:6005`) for local dev without a reverse proxy; with it set the UI runs on a different origin (e.g. :3000) and this relative fetch hits the UI origin, not the gateway, returning 404/connection-refused. session-cost-cells.tsx then catches and renders the errored 3x '—' branch (lines 109-117); cost-footer similarly fails. personas.ts:397 uploadAsset correctly uses `${GATEWAY_BASE_URL}${...}` — proving the pattern, making these two a copy-paste regression. Mounted on real paths: session-row.tsx:66 (<SessionCostCells/>) and sessions/detail/page.tsx:74 (<CostFooter/>).
- **verify**: Set NEXT_PUBLIC_GATEWAY_URL to a non-empty origin distinct from the UI host, open /admin/sessions; observe cost/avg/last-tool cells show '—' (network tab shows the request going to the UI origin). Fix: prefix `${GATEWAY_BASE_URL}` in both _loadCostInline functions.

### NEW-fresh-ui-4 — Medium bug (NEW, conf=medium) — scout:fresh-ui
- **loc**: `ui/lib/api.ts:2528-2537 (streamUpgradeEvents)`
- **problem**: streamUpgradeEvents builds its EventSource URL without the GATEWAY_BASE_URL prefix, so the system-upgrade live progress SSE fails to connect when NEXT_PUBLIC_GATEWAY_URL is set. Expected: prefix GATEWAY_BASE_URL like the sibling streamSubagentEvents/streamSubagentsOverview/streamHubInstallEvents.
- **evidence**: Line 2535: `const url = \`/admin/system/upgrade/${encodeURIComponent(request_id)}/events${suffix}\`;` then line 2536 `new EventSource(url, ...)` — no `${GATEWAY_BASE_URL}`. Every other EventSource factory in this file prefixes it: streamSubagentEvents (2670 -> `${GATEWAY_BASE_URL}/admin/subagents/...`), streamSubagentsOverview (2679), streamHubInstallEvents (2699). Consumed by components/system/upgrade-progress.tsx:102 `es = streamUpgradeEvents(requestId)`. With NEXT_PUBLIC_GATEWAY_URL pointing at a different origin, the upgrade-progress panel's live updates never arrive (the EventSource opens against the UI origin and errors), so the operator watches a stalled progress UI.
- **verify**: With NEXT_PUBLIC_GATEWAY_URL set to the gateway origin (distinct from UI), trigger a system upgrade and open the progress panel; the SSE 'status' frames never arrive. Fix: `const url = \`${GATEWAY_BASE_URL}/admin/system/upgrade/...\``.

### NEW-fresh-ui-5 — Medium bug (NEW, conf=medium) — scout:fresh-ui
- **loc**: `ui/lib/chat/event-merger.ts:9-16 (docstring) + 190-198; ui/lib/chat/use-chat-stream.ts:278-283 (live onEvent) + 99-100 (applyEvent text-delta)`
- **problem**: Assistant text can be double-rendered. The merger's documented dedup between the token stream and the journal TextDelta events is NOT implemented, so if the /events/live journal emits TextDelta during the same turn the /v1/chat/completions token stream is rendering, draft.content gets each text fragment appended twice.
- **evidence**: event-merger.ts docstring (lines 9-16) explicitly promises: "When the matching journal TextDelta lands later, we mark it as already-rendered to avoid double-emit." No such mechanism exists. Token chunks become text-delta events with sequence:-1 (EventDedupSet.shouldEmit returns true for any sequence<0, line 334-335), while journal TextDelta becomes a text-delta with a REAL sequence (emitted once). In runTurn both the live onEvent (liveEventToChatEvent -> reduceEvent for ANY non-null event incl. TextDelta, lines 279-282) and the token loop (chunkToChatEvents -> reduceEvent) feed the same draft, and applyEvent's text-delta case does `draft.content += ev.text` unconditionally (line 99-100). The chat-mvp e2e only avoids the doubling because its live-SSE stub deliberately omits TextDelta (sends only ToolState*/TurnComplete) — i.e. it relies on the backend NOT mirroring text on the live channel, which the journal (canonical event log) is otherwise expected to carry. Confidence medium: depends on the gateway emitting TextDelta on the live channel concurrently with the OpenAI-compat stream.
- **verify**: Unit test: drive useChatStream so BOTH a token chunk (delta.content:'Hi') and a journal TextDelta(text:'Hi', sequence>=0, same turn_id) reach reduceEvent; assert pendingMessage.content === 'Hi' (not 'HiHi'). Today it doubles.

### NEW-providers-3 — Medium bug (NEW, conf=medium) — scout:fresh-bug-agent-providers
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/google_provider.py:96-102 (chat_stream prompt build)`
- **problem**: GoogleProvider serialises multimodal/structured message content with f-string interpolation of the raw Python object. When a user turn carries OpenAI content-parts (list form — produced by reasoning_loop._inject_attachments for ANY turn with an image/file attachment), `f"{role}: {content}"` embeds the Python repr of the list (e.g. "user: [{'type': 'image_url', 'image_url': {'url': '...'}}]") into the Gemini prompt. The image/attachment is never sent as a real image part, so every vision/attachment request via Gemini is garbled. Same flattening corrupts tool-result rounds (role 'tool' content prefixed with 'tool: ').
- **evidence**: Loop: `content = _get(m,'content') or ''; if content: prompt_parts.append(f"{role}: {content}")`. Repro: messages with a user content-parts list yields prompt = "...\nuser: [{'type': 'text', 'text': \"What's in this image?\"}, {'type': 'image_url', 'image_url': {'url': 'https://example.com/cat.jpg'}}]". Attachments are injected for the QQ/channel image path (reasoning_loop._inject_attachments / _attachment_to_content_part), so this is reachable in production, not only tests.
- **verify**: Call GoogleProvider.chat_stream with a user message whose content is a content-parts list (text + image_url) and assert the SDK receives a structured multimodal `contents` payload (image part) rather than a flat prompt string containing the list repr.

### NEW-fresh-perf-2 — Medium performance (NEW, conf=high) — scout:fresh-perf
- **loc**: `python/packages/corlinman-server/src/corlinman_server/agent_servicer.py:1916-1924`
- **problem**: Builtin coding tools execute synchronous, potentially heavy filesystem work directly on the gRPC servicer's async event loop, stalling ALL concurrent chat streams for the duration. current: the async _dispatch_builtin awaits nothing for dispatch_read_file/dispatch_write_file/dispatch_edit_file/dispatch_list_files/dispatch_search_files — they are plain sync functions called inline. dispatch_search_files rglobs the workspace and read_text()s up to 5000 files (O(files) sync disk I/O) on the loop. expected: offload these blocking dispatchers via asyncio.to_thread (the codebase already does exactly this in corlinman-wstool/file_fetcher.py:419 _read_within_root).
- **evidence**: agent_servicer.py:1924 `return dispatch_search_files(args_json=event.args_json)` — no await/to_thread, inside `async def _dispatch_builtin`. search.py:_iter_files (112) `for path in root.rglob('*')` bounded by _MAX_FILES_SCANNED=5000 (search.py:38), then content mode `text = path.read_text(...)` per file (search.py:179) — all synchronous. Contrast: dispatch_run_shell IS async (shell.py:234 uses asyncio.create_subprocess_shell), and wstool offloads with `return await asyncio.to_thread(_do)` (file_fetcher.py:419). One search_files call on a large workspace blocks every other in-flight stream sharing the loop.
- **verify**: Integration: start two concurrent chat streams; have one invoke search_files over a workspace with thousands of files; assert the second stream's token latency does not spike during the scan (today it stalls). Or unit: monkeypatch dispatch_search_files to sleep(1) sync and assert a concurrent coroutine is blocked.

### PERF-008 — Medium performance (CONFIRMED, conf=high) — scout:verify-perf
- **loc**: `python/packages/corlinman-episodes/src/corlinman_episodes/store.py:388 (caller python/packages/corlinman-episodes/src/corlinman_episodes/runner.py:349)`
- **problem**: Episode persistence does one commit (one fsync round-trip) per row. There is no batch/executemany insert API, and the distillation runner inserts episodes one-at-a-time inside its per-bundle loop.
- **evidence**: store.py `insert_episode` ends with `await self.conn.commit()` (L388) after a single-row INSERT (L361-387). runner.py loops over bundles and calls `await store.insert_episode(episode); written += 1` (L349-350) — N bundles ⇒ N separate transactions/commits. No `executemany`/batch path exists (grep found only the single-row insert). Impact is bounded: the store docstring (L357-359) notes rowcount stays low (a few thousand/tenant/year), so this is a background-distillation cost, not a hot user path → Medium.
- **verify**: Insert 1000 episodes via insert_episode vs a single transaction wrapping all 1000; compare wall time (each commit is an fsync on default SQLite journal mode).

### PERF-010 — Medium performance (CONFIRMED, conf=high) — scout:verify-perf
- **loc**: `ui/lib/chat/use-chat-stream.ts:238-249`
- **problem**: reduceEvent rebuilds the entire pendingMessage object — spreading prev and deep-copying its toolCalls, subagents (incl. nested events arrays), approvals, and usage — on every single SSE/journal event, even token deltas that touch only `content`.
- **evidence**: L238-249 `setPendingMessage((prev) => { … const next = { ...prev, toolCalls: prev.toolCalls?.map((t)=>({...t})), subagents: prev.subagents?.map((s)=>({...s, events: s.events ? [...s.events] : []})), approvals: prev.approvals?.map((a)=>({...a})), usage: prev.usage ? {...usage} : undefined }; applyEvent(next, ev); return next; })`. Every event allocates fresh shallow copies of all sub-arrays regardless of which field the event mutates. During a long agent loop (many toolCalls + subagent event streams) a pure text token still clones every tool-call and every subagent event array → O(toolCalls + Σ subagent.events) allocations per token delta.
- **verify**: Mock a turn with 20 toolCalls and a subagent carrying 200 events; stream 500 token deltas through reduceEvent and count object allocations vs an immer/structural-sharing reducer.

### PERF-012 — Medium performance (CONFIRMED, conf=high) — scout:verify-perf
- **loc**: `ui/components/chat/chat-model-picker.tsx:93-100`
- **problem**: Opening the model picker fans out one upstream model-probe HTTP request per enabled provider in parallel via useQueries, hitting every configured provider's /models endpoint at once.
- **evidence**: L93-100 `const probeQueries = useQueries({ queries: enabledProviders.map((p) => ({ queryKey: ["chat-picker","provider-models",p.name], queryFn: () => getProviderModels(p.name), staleTime: 120_000, enabled: open })) })`. `enabledProviders` (L84-91) = all enabled providers (image-capable subset for image kind). With N enabled providers, every picker `open` schedules N concurrent getProviderModels calls. staleTime:120s caches repeats within 2 min, but the first open (and any open after staleness) fans out N live upstream probes — latency + upstream rate-limit exposure scales with provider count.
- **verify**: Configure 8 enabled providers, open the picker, and assert 8 concurrent getProviderModels network calls fire; consider lazy/on-hover probing or a single server-side aggregate endpoint.

### NEW-fresh-test-3 — Medium test (NEW, conf=high) — scout:fresh-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/api_keys.py:240-266 (revoke_key handler)`
- **problem**: current: the DELETE /admin/api_keys/{key_id} route handler is untested at the route level; in particular its documented cross-tenant behavior (revoke is keyed on key_id ALONE, tenant resolution result is discarded) is not asserted by any test. expected: a route test that proves revoke ignores the ?tenant= scope (and a decision/test on whether that is acceptable), plus mint/list happy-path + tenant-isolation tests.
- **evidence**: In revoke_key (api_keys.py:249) the resolved tenant is explicitly thrown away: `_ = _resolve_tenant(state, tenant)` then `revoked = await db.revoke_api_key(key_id)`. The DB method revoke_api_key (tenancy/admin_schema.py:600) runs `UPDATE tenant_api_keys SET revoked_at_ms=? WHERE key_id=? AND revoked_at_ms IS NULL` with NO tenant_id predicate — so an admin scoped to tenant A can revoke tenant B's key by id. The DB method is unit-tested (tenancy/test_admin_schema.py:305/357) but ONLY within a single tenant; the ROUTE handler (mint_key/list_keys/revoke_key) has no test: `grep -rln 'mint_key|revoke_key|api_keys router' tests/` finds only test_must_change_blocks_admin_routes.py, which mounts the router solely to assert the password-change auth gate returns 403 — it never asserts mint/list/revoke behavior or cross-tenant scoping. Risk: if multi-tenant ([tenants].enabled) is turned on, the missing tenant predicate is a cross-tenant key-revocation (and the un-asserted list_keys tenant scoping is a cross-tenant enumeration risk) that no test would flag.
- **verify**: Add tests/gateway/routes_admin_a/test_api_keys.py: (1) POST /admin/api_keys?tenant=acme mints, GET ?tenant=acme lists it, GET ?tenant=other does NOT list it (tenant isolation); (2) mint with blank scope -> 400 invalid_request; (3) DELETE an unknown key_id -> 404; DELETE then DELETE again -> 404 (idempotent revoke returns false->404); (4) cross-tenant regression guard: mint under tenant acme, then DELETE /admin/api_keys/{id}?tenant=other and assert the EXPECTED policy (either 404/forbidden if scoping is required, or document that revoke is intentionally global) — this pins down the currently-silent behavior.

### TEST-007 — Medium test (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes/{wechat_webhook.py,plugin_callback.py,canvas.py}`
- **problem**: current: gateway/routes/wechat_webhook.py and gateway/routes/plugin_callback.py have NO direct route tests; canvas.py only has an entropy unit test on _new_session_id (not the SSE subscribe / POST /v1/canvas/frame handlers). channels.py/memory.py route LOGIC is only incidentally touched by auth-prefix tests. expected: handler-level tests for these production routes. NOTE: the wechat_official channel parser is tested in corlinman-channels/test_wechat_official.py, but that is the channel adapter, NOT the gateway route.
- **evidence**: `grep -rln wechat_webhook python/packages/*/tests` → only corlinman-channels/test_wechat_official.py (different module). `grep -rln plugin_callback python/packages/*/tests` → only test_chat_requires_auth.py (auth gate, not handler). canvas test (test_canvas_session_id_entropy.py:13-31) only exercises `_new_session_id`.
- **verify**: add route tests hitting POST /wechat/<bot_name> (verify/echo + message dispatch) and /plugin-callback handler; today no test calls those handlers.

### NEW-fresh-quality-5 — Medium quality (NEW, conf=high) — scout:fresh-quality
- **loc**: `.github/workflows/ci.yml:241-266 (boundary-check) vs :272-285 (gate needs)`
- **problem**: The boundary-check job (import-linter) is NOT listed in the `gate` aggregator's `needs`, while branch protection is documented to require only the single `gate` check. So even when lint-imports hard-fails (which it currently does, see NEW-fresh-quality-1), the required gate stays green and nobody is forced to notice the dead layering contract.
- **evidence**: gate.needs (ci.yml:275-281) = [py-ruff, py-mypy, py-test, ui-typecheck, ui-lint, ui-test] — boundary-check absent. Comment at ci.yml:269-271: 'a single required check that fans-in the core gate jobs so branch protection only needs one rule.' boundary-check has no continue-on-error, so it fails loudly but non-blockingly.
- **verify**: Inspect ci.yml: confirm `boundary-check` is not in `gate.needs`. Combined with NEW-fresh-quality-1, an intentionally-introduced cross-layer import would merge to main without blocking.

### QUAL-007 — Medium quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/session_query.py:1-238`
- **problem**: current: 238-line SessionQueryClient/SessionMessage/SessionQueryError module whose docstring (lines 10-25, 73-76) states it reads the Rust gateway's `<data_dir>/sessions.sqlite` `sessions` table written by `corlinman_core::session_sqlite` (rust/crates/corlinman-core/src/session_sqlite.rs). expected: removed. It is dead on HEAD — `SessionQueryClient(` is instantiated ONLY in its own test (tests/test_session_query.py); no production code uses it. The Python plane now persists sessions via agent_journal_backend/agent_journal (its own SQLite), and the rust/ tree is scheduled for deletion in the active migration, so the `sessions` table this module range-scans is going away.
- **evidence**: `grep -rn 'SessionQueryClient(' python/.../src` → no src hits; only tests/test_session_query.py:120..208. Module is re-exported by corlinman_agent/__init__.py:27-43 but never consumed. Docstring lines 73-76 point at `rust/crates/corlinman-core/src/session_sqlite.rs` for the authoritative schema; SQL at :152 SELECTs `FROM sessions`.
- **verify**: `grep -rn 'SessionQueryClient(' python/packages/*/src` returns nothing → confirm no runtime consumer; deleting session_query.py + its __init__ re-export should leave `uv run pytest` green except its own removed test.

### QUAL-010 — Medium quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/ (29 src files)`
- **problem**: current: 29 separate `def _now_ms()` definitions in src (33 incl. tests), each effectively `return int(time.time() * 1000)`. expected: one shared helper. No shared now_ms util exists anywhere.
- **evidence**: `grep -rn 'def _now_ms' python/.../src | grep -v /tests/ | wc -l` → 29 (e.g. persona/store.py, episodes/store.py, goals/reflection.py, server system/* and gateway/* etc). Sampled bodies: persona/store.py and episodes/store.py both `return int(time.time() * 1000)`. No `def now_ms` shared util found.
- **verify**: `grep -rln 'def _now_ms' python/packages/*/src --include='*.py' | grep -v worktrees` lists 29 files; consolidating into one helper would remove 28 copies.

### QUAL-011 — Medium quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `corlinman-agent/.../session_query.py:214,232; corlinman-identity/.../resolver.py:143-160; corlinman-replay/.../session_store.py:159-173; corlinman-server/.../routes_admin_a/identity.py:124; corlinman-user-model/.../cli.py:280`
- **problem**: current: 5+ packages each reimplement an RFC3339 formatter and/or parser (several ship both format+parse halves). expected: one shared RFC3339 util. Duplication of the same Z<->+00:00 normalization logic across packages.
- **evidence**: `grep -rn 'def .*rfc3339'` → session_query.py:214 `_rfc3339` + :232 `_parse_rfc3339`; resolver.py:143 `_now_utc_rfc3339`,:148 `_to_rfc3339`,:160 `_parse_rfc3339`; session_store.py:159 `_format_rfc3339`,:173 `_parse_rfc3339`; routes_admin_a/identity.py:124 `_to_rfc3339_z`; user_model/cli.py:280 `_rfc3339_to_ms`. All hand-roll `.replace('Z','+00:00')` / `.isoformat().replace('+00:00','Z')`.
- **verify**: `grep -rn 'def .*rfc3339' python/packages/*/src --include='*.py'` enumerates the duplicate format+parse pairs.

### QUAL-013 — Medium quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `corlinman-identity/.../verification.py:45; corlinman-server/.../routes_admin_b/credentials.py:42 + providers.py:44; routes_admin_a/personas.py:350; gateway/grpc/agent_server.py:289`
- **problem**: current: multiple sites cross-import leading-underscore private symbols across module/package boundaries, violating the underscore convention systematically. expected: promote to public or expose a public wrapper.
- **evidence**: verification.py:45 `from corlinman_identity.resolver import _now_utc_rfc3339, _parse_rfc3339, _to_rfc3339`; credentials.py:42 & providers.py:44 `from ...onboard import _write_config_atomic`; personas.py:350 `from corlinman_server.persona.store import _now_ms  # type: ignore[attr-defined]`; agent_server.py:289 `from corlinman_server.main import _run_boot_auto_resume`. Plus QUAL-012's `journal._load_messages` dotted access.
- **verify**: `grep -rn 'import _[a-z]' python/packages/*/src --include='*.py' | grep '^.*from corlinman'` lists the cross-package private imports.

### QUAL-015 — Medium quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `docs/providers.md:44,255; docs/architecture.md:71; docs/plugin-authoring.md:355; docs/roadmap.md:68-275; docs/milestones.md:32-40`
- **problem**: current: live, top-level docs link/point into `rust/crates/...` paths that the active Rust->Python migration is deleting. After rust/ removal these become dead links/references. architecture.md:71 even hardcodes an absolute machine path `/Users/cornna/project/corlinman/rust/crates/`. expected: repoint to the Python equivalents or remove.
- **evidence**: providers.md:44 markdown link `[`rust/crates/corlinman-core/src/config.rs`](../rust/crates/corlinman-core/src/config.rs)`; architecture.md:71 `所有 crate 在 /Users/cornna/project/corlinman/rust/crates/`; plugin-authoring.md:355 `rust/crates/corlinman-plugins/src/manifest.rs`; roadmap.md and milestones.md list many `rust/crates/...` scope paths.
- **verify**: `grep -rn 'rust/crates' docs/{providers,architecture,plugin-authoring,roadmap,milestones}.md`; relative link `../rust/crates/...` 404s once rust/ is deleted.

### NEW-fresh-quality-7 — Low completeness (NEW, conf=medium) — scout:fresh-quality
- **loc**: `python/packages/corlinman-evolution-engine/src/corlinman_evolution_engine/{gepa.py:32-33,agent_card.py:13-14,110,engine.py:89-91,149,prompt_template.py:11-12}`
- **problem**: current vs expected: multiple evolution-engine docstrings assert the ShadowTester 'already routes high-risk kinds through the shadow stage' before proposals reach the operator queue; expected = a real call path into corlinman_shadow_tester. Reality: corlinman_shadow_tester is imported nowhere (see NEW-fresh-quality-6), so the documented high-risk gating does not exist — a documented safety control is absent.
- **evidence**: agent_card.py:13-14 'The Phase 3 W1-A ShadowTester already routes high-risk kinds through the shadow stage'. engine.py:89-91 'high-risk kinds gated by the docker shadow ... ShadowTester decides whether they reach the operator queue.' Yet `grep -rn 'import corlinman_shadow_tester|from corlinman_shadow_tester' python/` (excluding the shadow-tester pkg) = 0 matches, and evolution-engine has no real import of it. The gate is aspirational doc text only.
- **verify**: `grep -rn shadow_tester python/packages/corlinman-evolution-engine/src/ --include=*.py` returns only docstring prose, no import/call. Confirm no runtime path constructs corlinman_shadow_tester.runner before a high-risk proposal is queued.

### NEW-fresh-sec-channels-deploy-6 — Low completeness (NEW, conf=high) — scout:fresh-sec-channels-deploy
- **loc**: `python/packages/corlinman-channels/src/corlinman_channels/commands.py:867-882`
- **problem**: is_command_admin() is default-open: when CORLINMAN_COMMAND_ADMINS is unset/blank it returns True for every caller (lines 877-879), so any inbound channel user is treated as a command admin. Current vs expected: in the shipped channels package no built-in CommandSpec sets admin_only=True (grep finds zero), so today this gates nothing dangerous and the behavior is intentional + unit-tested (test_command_handlers.py:363-368). Flagging for completeness because any deployment that registers an admin_only runtime command via register_command() inherits a fail-open authorization default keyed only on a comma-separated env allowlist — a footgun if operators assume admin commands are protected out of the box.
- **evidence**: commands.py:877-879 `raw = os.environ.get(_COMMAND_ADMINS_ENV, "").strip(); if not raw: return True`. Enforcement points: commands.py:273/729/762 gate solely on `spec.admin_only and not ctx.is_admin`, and is_admin flows from is_command_admin(req.binding) at service.py:740,1806. No built-in spec uses admin_only=True (COMMAND_REGISTRY lines 466-556).
- **verify**: Register a CommandSpec(admin_only=True) at runtime, leave CORLINMAN_COMMAND_ADMINS unset, and send the command as a non-listed channel user — it executes. Acceptance: decide whether default-open is desired; if not, default-deny when no allowlist is configured (or require explicit opt-out).

### NEW-verify-functional-3 — Low completeness (CHANGED, conf=high) — scout:verify-functional
- **loc**: `python/packages/corlinman-channels/src/corlinman_channels/qq_official.py:354-357,596,674-680,552-580`
- **problem**: current vs expected: the prompt hypothesizes QQ reconnect 501s/raises/deadloops — it does NOT. On a server RECONNECT (op 7, _OP_RECONNECT at :674-675) _pump returns cleanly; on INVALID_SESSION (:676-680) it wipes _session_id/_last_seq and returns; on websockets.ConnectionClosed (:647-648) it returns. In every case _connect_once finishes and the outer _reader_loop (:561-580) resets attempt=0 (clean) or backs off via RECONNECT_SCHEDULE (failure) and calls _connect_once() again, which always does a fresh op-2 IDENTIFY (_send_identify at :596,728). So the channel DOES recover; there is no raise/501/deadloop. The 'RESUME is not implemented yet' comment (:354) is accurate but cosmetic-functional: _OP_RESUME=6 is defined but no _send_resume() exists anywhere, so RESUME is simply never attempted. The only real impact is the missing RESUME optimization — events delivered by the gateway during the disconnect window are NOT replayed (a full re-IDENTIFY starts a fresh session, RESUMED at :666 is handled but never triggered since we never RESUME), so a small burst of messages can be lost across a reconnect. Graceful degradation, not user-facing breakage. Recommend downgrading the backlog entry from 'QQ reconnect 501' to 'QQ reconnect drops in-window events (no RESUME replay)'.
- **evidence**: qq_official.py:674 `elif op == _OP_RECONNECT: return` -> :563 `await self._connect_once()` re-runs -> :596 `await self._send_identify(ws)`; :567-568 only re-raises CancelledError, all other Exceptions are logged and retried (:569-576). No `_send_resume` symbol exists (grep returns only _OP_RESUME constant + RESUMED handler that is never reached).
- **verify**: Unit/integration: feed the adapter a HELLO->READY(session_id)->{op:7 RECONNECT} frame sequence via a fake ws; assert _connect_once returns, _reader_loop loops, and a fresh IDENTIFY (op 2, not op 6 RESUME) is sent on the next cycle (i.e. no exception, no RESUME). Then assert that a DISPATCH event delivered only on the pre-reconnect socket is not re-observed after reconnect (the lost-event gap).

### NEW-verifyqualtest-3 — Low completeness (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-channels/tests/test_telegram_outbound.py:345-354`
- **problem**: current vs expected: assignment lists 'Telegram webhook _verify_secret untested' as a still-open gap. It is RESOLVED — verify_secret (telegram_webhook.py:86, the constant-time secret-token compare) is fully tested with match/mismatch/empty/None cases. Recording so the backlog drops this sub-item.
- **evidence**: test_telegram_outbound.py imports verify_secret (:38) and asserts :345 verify_secret('sekret','sekret') True; :348 mismatch False; :349 empty False; :350 None False; :353-354 empty-configured cases. (Function is named verify_secret, not _verify_secret.)
- **verify**: `grep -rn 'verify_secret' python/packages/corlinman-channels/tests` shows the assertion table at :345-354.

### NEW-verifyqualtest-4 — Low completeness (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `README.md:5 vs 669,674,483,673`
- **problem**: current vs expected: README has internal version drift. Version badge (line 5) reads 1.9.0 (matches root pyproject.toml version=1.9.0 and server pyproject.toml), but the Versioning section says `**v1.1.0** (current)` (line 669) and `Released 2026-05-24, tagged v1.1.0` (line 674) — stale by 8 minor versions. Also admin page count: README says `Ten pages covering the full control plane` (line 483) and `10 operator pages` (line 673), but there are 33 page.tsx under ui/app/(admin)/ (~26 distinct top-level admin sections). expected: README body matches 1.9.0 + real admin page count. (AgentCard 'Rust-engine mention' doc-drift does NOT reproduce — all AgentCard refs in README/docs are the Python corlinman_agent registry; matches R4 QUAL-019 CANT_REPRO.)
- **evidence**: README.md:5 `version-1.9.0`; :669 `**v1.1.0** (current)`; :674 `tagged v1.1.0`; :483 `Ten pages`; :673 `10 operator pages`. pyproject.toml:3 `version = "1.9.0"`. `find ui/app -path '*(admin)*' -name page.tsx | wc -l` → 33. `grep -i AgentCard README.md docs/*.md` → no Rust-engine mention.
- **verify**: `grep -n 'v1.1.0\|Ten pages\|10 operator pages' README.md` vs `grep '^version' pyproject.toml` and admin page count.

### NEW-fresh-sec-channels-deploy-3 — Low security (NEW, conf=medium) — scout:fresh-sec-channels-deploy
- **loc**: `deploy/corlinman-upgrader.sh:67,263`
- **problem**: Root-run upgrader writes its log to a predictable path in the world-writable /tmp (`LOG_FILE=${UPGRADER_LOG_FILE:-/tmp/corlinman-upgrader.log}`) and truncates it with `: >"$LOG_FILE"` as root. On a multi-user host an unprivileged user can pre-create /tmp/corlinman-upgrader.log as a symlink to an arbitrary file; the root truncation/append then clobbers the target (classic /tmp symlink/TOCTOU). Current vs expected: a root daemon should write logs under a root-owned dir (e.g. /var/log/corlinman or DATA_DIR) or use O_NOFOLLOW.
- **evidence**: deploy/corlinman-upgrader.sh:67 `LOG_FILE="${UPGRADER_LOG_FILE:-/tmp/corlinman-upgrader.log}"`; line 263 `: >"$LOG_FILE"` truncates as root; line 268 appends install.sh output (`>>"$LOG_FILE"`). systemd already redirects StandardOutput/StandardError to journal (install.sh:761-762), so the /tmp file is redundant attack surface.
- **verify**: As a non-root user: `ln -s /etc/cron.d/evil /tmp/corlinman-upgrader.log` then trigger the upgrader; observe root truncating/writing through the symlink. Acceptance: log to a root-only directory or open with O_NOFOLLOW.

### NEW-fresh-sec-channels-deploy-4 — Low security (NEW, conf=medium) — scout:fresh-sec-channels-deploy
- **loc**: `deploy/install.sh:497-509 / docker/compose/docker-compose.yml:16`
- **problem**: Supply-chain: container images are pulled by mutable tag, never by digest. install.sh pulls `ghcr.io/${REPO}:${REF}` (line 497-502) and the compose file uses `image: ghcr.io/sweetcornna/corlinman:${CORLINMAN_TAG:-latest}` (docker-compose.yml:16). No `--platform` pin, no digest pin, no signature/attestation verification (cosign/SLSA). A registry compromise or tag-overwrite (`latest`/`edge` are rolling per release-image.yml:50-52) silently ships replaced code that then runs as root on the native path or as the corlinman user under Docker. Current vs expected: pin by `@sha256:` digest and/or verify cosign signatures before run.
- **evidence**: deploy/install.sh:502 `docker pull "$image_ref"` with image_ref=`ghcr.io/${REPO}:${REF}` (line 497). docker/compose/docker-compose.yml:16 `image: ghcr.io/sweetcornna/corlinman:${CORLINMAN_TAG:-latest}`. release-image.yml:51-52 publishes mutable `edge` + `latest`. No digest/cosign verification anywhere in install.sh or compose.
- **verify**: Overwrite the `latest` tag in a test registry mirror and re-run `docker compose pull` — the running image changes with no warning. Acceptance: install/compose reference `ghcr.io/...@sha256:<digest>` and verify a signature.

### SEC-009 — Low security (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:150-157 (emitted at :272)`
- **problem**: Admin session cookie is set without the `Secure` flag. _set_cookie_header builds `<name>=<token>; HttpOnly; SameSite=Strict; Path=/; Max-Age=...` with no `Secure`, so over a plaintext-HTTP hop the session token can be sniffed. The docstring explicitly states 'no Secure flag (TLS terminates upstream)'. Documented design choice per ISSUES.md L31; still technically a weakened cookie.
- **evidence**: L150-157 `def _set_cookie_header(...): ... return (f"{SESSION_COOKIE_NAME}={token}; HttpOnly; SameSite=Strict; Path=/; Max-Age={max_age_seconds}")` — no `Secure`. Used at L272 `response.headers["set-cookie"] = _set_cookie_header(token, max_age)` in the /admin/login handler. _clear_cookie_header (L160-165) likewise omits Secure.
- **verify**: POST /admin/login, assert the Set-Cookie response header contains `Secure` (currently absent). Decision is whether to make Secure conditional on X-Forwarded-Proto=https.

### SEC-010 — Low security (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes/wechat_webhook.py:95-98`
- **problem**: `bot_name` path parameter is reflected unvalidated into the 404 response body via `PlainTextResponse(f"no wechat bot registered as {bot_name!r}", status_code=404)`. The reflection is real, but PlainTextResponse uses Content-Type text/plain, so a browser will not execute injected HTML/JS — so this is NOT an exploitable reflected XSS. Effectively cosmetic / information-reflection only.
- **evidence**: L95-98 `return PlainTextResponse(f"no wechat bot registered as {bot_name!r}", status_code=404,)`. Confirmed `PlainTextResponse.media_type == 'text/plain'` (starlette). Reflected via repr `{bot_name!r}`. Routes `@api.get('/{bot_name}')` (L101) and `@api.post('/{bot_name}')` (L106) both reach _dispatch (L92).
- **verify**: GET /wechat/<script>alert(1)</script> on an unregistered name; assert response Content-Type is text/plain and the browser does not execute the payload (it does not). To fully remove reflection, drop bot_name from the body.

### SEC-011 — Low security (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:263 + python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/_auth_shim.py:192`
- **problem**: Admin username is compared with the non-constant-time `!=` operator at both auth sites, creating a username timing oracle. In auth.py the check `body.username != state.admin_username or not argon2_verify(...)` short-circuits on the username, so a wrong username skips the (slow, constant-time) argon2 path entirely — a measurable timing difference reveals whether the username is correct. _auth_shim.py Basic-auth fallback has the identical pattern. argon2_verify itself is constant-time, but the username compare is not.
- **evidence**: auth.py L263-265 `if body.username != state.admin_username or not argon2_verify(body.password, state.admin_password_hash):`. _auth_shim.py L192-194 `if username != admin_username or not _verify_password(password, admin_password_hash):`. Neither uses hmac.compare_digest for the username. Note backlog cited _auth_shim.py:109 — drifted; the compare is now at :192 (L109 is now _parse_basic).
- **verify**: Wrap both username comparisons in `hmac.compare_digest` and always run argon2 on a dummy hash when the username mismatches so total response time is independent of username correctness; add a test asserting compare_digest is used.

### SEC-013 — Low security (CONFIRMED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:255-273`
- **problem**: The /admin/login handler has no rate-limiting, attempt counter, or lockout — online password brute-force is bounded only by argon2 verification cost. This is inconsistent with the sibling password_reset.py which DOES implement a global throttle (rejects with 429 if called twice within a window). No app-level limiter middleware covers /admin/login (the middleware/ dir has no rate limiter; the only `rate_limit_seconds=30` in entrypoint.py:2094 is unrelated to login). Backlog cited auth.py:74 — drifted; login is now at :255.
- **evidence**: login handler L255-273: validates credentials (L263) and immediately issues a session on success with zero throttling between attempts. By contrast password_reset.py L34-36/196-223 implements `_LAST_REQUEST_AT` global throttle returning `error: 'rate_limited'` (429). grep for slowapi/Limiter/lockout/failed_attempts across gateway/ finds none applied to login.
- **verify**: Fire N rapid POST /admin/login with wrong passwords; assert the endpoint begins returning 429 after a threshold (currently it never does). Add a per-source or global failed-attempt lockout mirroring password_reset's throttle.

### BUG-008 — Low bug (CHANGED, conf=high) — scout:verify-sec-bug
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/_retry.py:89-93`
- **problem**: with_retry uses `except BaseException as exc` (L89), which catches asyncio.CancelledError, and relies entirely on the caller's classifier to re-raise it. The structural defect (no explicit CancelledError guard before consulting the classifier) still exists, but it is NOT currently exploitable: the sole classifier in use, default_retryable_codex, returns None for CancelledError (no .status_code, _is_connection_error matches by class name only and 'CancelledError' is not in the set), so L92 `override is None` ⇒ `raise` re-raises it on the first attempt. The only two callers (codex_provider.py:440,464) use this classifier, and the outer `except Exception as exc2` (L471) won't swallow the BaseException. So latent, not active cancel-eating.
- **evidence**: _retry.py L89 `except BaseException as exc:  # noqa: BLE001`. L91-93 `override = retryable(exc); if override is None or attempt >= max_attempts: raise`. default_retryable_codex (L139-187): _extract_status→None for CancelledError, _is_connection_error (L243-269) class-name set has no 'CancelledError' ⇒ returns False ⇒ classifier returns None ⇒ re-raised. Only callers: codex_provider.py:440 & :464, both `retryable=partial(default_retryable_codex, ...)`. No `except CancelledError: raise` shield in with_retry itself.
- **verify**: Add a defensive `except asyncio.CancelledError: raise` ahead of the BaseException handler; add a test that make_attempt raises CancelledError and asserts with_retry re-raises immediately (0 retries) regardless of the classifier. Today this passes only because the classifier happens to return None.

### NEW-fresh-bug-server-3 — Low bug (NEW, conf=medium) — scout:fresh-bug-server
- **loc**: `python/packages/corlinman-server/src/corlinman_server/system/audit.py:74 (utcnow_iso ms precision) + 252-253 (_read_tail before_ts filter)`
- **problem**: Audit-log pagination drops entries that share the boundary millisecond. utcnow_iso() stamps ts at millisecond precision (`%Y-%m-%dT%H:%M:%S.` + zero-padded ms + 'Z'), so multiple appends within the same ms get byte-identical ts strings. tail() paginates with `before_ts` and filters `e.ts < before_ts` using the OLDEST ts the caller has seen. Any other entries that share that exact ms — including ones not yet returned — satisfy `ts == before_ts` (not `<`) and are silently excluded from the next page, so they are never surfaced.
- **evidence**: Confirmed: with two entries having identical ts==before_ts, `[e for e in entries if e.ts < before_ts]` returns []. Trigger: two `system.*` audit events written in the same millisecond (e.g. two browser tabs both POST /admin/system/upgrade, or a kill+notification burst) that straddle a 50-item page boundary — the older same-ms entry is lost from the operator's audit view. Should use a strict `(ts, monotonic-tiebreak)` cursor or `<=` with an explicit seen-id set.
- **verify**: Unit test: append 3 AuditEntry with identical ts; tail(limit=2) → page1; then tail(limit=2, before_ts=page1[-1].ts) → expect the 3rd entry; current code returns [] (entry lost).

### NEW-fresh-bug-server-4 — Low bug (NEW, conf=low) — scout:fresh-bug-server
- **loc**: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:812-840 (begin_turn collision fallback)`
- **problem**: begin_turn's wall-clock-ms turn_id collision retry loops `for offset in range(0, 20)`; on 20 consecutive IntegrityError collisions it falls through and `return ts` (line 840) — but `ts` is exactly the first id (offset 0) that already collided, so NO row was inserted for the returned turn_id. The caller (servicer line 1054+) then treats it as a live journal_turn_id and issues append_message/append_messages against it. With `PRAGMA foreign_keys=ON` (line 686) and the turn_messages FK to turns(turn_id), those inserts fail with IntegrityError (caught + logged), and find_resumable_turn can never match the turn — the turn is silently un-journaled / non-resumable.
- **evidence**: Line 839-840: `# Vanishingly unlikely; fall through with a tagged turn_id.` `return ts` — `ts` equals the colliding base, not a guaranteed-free id. The function signature documents returning a valid new turn_id or None; returning an id with no backing row violates that contract. turn_messages FK + foreign_keys=ON means subsequent appends to a phantom turn_id raise.
- **verify**: Pre-seed turns rows for turn_id in [ts, ts+19] then call begin_turn at that ms; assert it does NOT return an id whose row is absent (should return None or a fresh id). Current code returns ts with no row.

### NEW-fresh-bug-server-5 — Low bug (NEW, conf=medium) — scout:fresh-bug-server
- **loc**: `python/packages/corlinman-server/src/corlinman_server/system/upgrader/native_upgrader.py:336-358 (poll loop terminal check) + state.py:107-111 (is_terminal/is_in_flight overlap for 'stalled')`
- **problem**: Native upgrader poll loop only returns when the helper-written state is in {succeeded, failed} (line 337), NOT 'stalled'. If the helper itself writes state='stalled' into the status file, _apply_payload mirrors it to the store (with finished_at) but the poll loop keeps running; since the payload then stops changing, `payload != last_payload` is False so no further work happens, and the loop spins until the 600s overall_timeout fires and OVERWRITES the store row to state='failed'/phase='timeout', discarding the helper's accurate 'stalled' diagnosis. Compounded by UpgradeStatus where 'stalled' is BOTH is_terminal()==True and is_in_flight()==True, so current_in_flight() reports a stalled (dead) upgrade as in-flight, blocking any new upgrade via the single-flight guard.
- **evidence**: native_upgrader.py:337 `if state in {"succeeded", "failed"}: return` (omits 'stalled' which _apply_payload explicitly accepts at line 384). state.py:108 is_terminal returns True for 'stalled'; state.py:111 is_in_flight also returns True for 'stalled'. NativeUpgrader.start() (line 204-206) raises UpgradeAlreadyRunning whenever current_in_flight() is non-None — a stalled row never clears, so retries are permanently blocked until process restart.
- **verify**: Drive _poll_status_file with a status file that writes state='stalled'; assert the poller returns promptly and the store keeps state=='stalled' (not overwritten to 'failed' after overall_timeout). Separately assert start() can begin a new upgrade after a prior one ended 'stalled'. Both fail today.

### NEW-fresh-bug-server-6 — Low bug (NEW, conf=low) — scout:fresh-bug-server
- **loc**: `python/packages/corlinman-server/src/corlinman_server/inbox.py:164-197 (increment_retry CASE) `
- **problem**: increment_retry's UPDATE forces status to INBOX_PENDING for rows that are below the retry cap, regardless of whether the row was previously 'pending' or 'dispatched' (the WHERE matches both). A row legitimately in 'dispatched' that gets a retry bump is silently demoted back to 'pending', which would let the boot drainer / a poll re-dispatch a message already in flight (double-processing). NOTE: no production caller currently invokes increment_retry (channels marks rows done/dead directly), so this is latent dead-code today, not a live defect.
- **evidence**: Lines 174-187: `status = CASE WHEN retries + 1 >= ? THEN INBOX_DEAD ELSE INBOX_PENDING END ... WHERE id = ? AND status IN (INBOX_PENDING, INBOX_DISPATCHED)`. A dispatched row under the cap is rewritten to pending. grep of all packages shows no non-test caller of increment_retry — channels/service.py uses mark_done/mark_dead/mark_dispatched only — so the bug is unreachable on current paths.
- **verify**: Enqueue → mark_dispatched → increment_retry(id); assert status stays 'dispatched' (or moves to dead at cap) rather than reverting to 'pending'. Currently reverts to 'pending'. Flag as latent until a retry caller is wired.

### NEW-fresh-sec-channels-deploy-5 — Low bug (NEW, conf=medium) — scout:fresh-sec-channels-deploy
- **loc**: `python/packages/corlinman-channels/src/corlinman_channels/wechat_official.py:409,436`
- **problem**: WeChat encrypted/compatibility mode is silently broken (fails closed, not a bypass). The webhook accepts either `signature` OR `msg_signature` (line 409) and feeds whichever it gets into verify_signature(), which only computes the PLAINTEXT scheme sha1(sort(token,timestamp,nonce)) (line 164-165). For an encrypted-mode account the real header is `msg_signature = sha1(sort(token,timestamp,nonce,encrypt))`, which will never match this digest, so every encrypted message is rejected with 401; and parse_wechat_xml (line 436) would see an undecrypted `<Encrypt>` body. Not exploitable (no auth bypass — it fails closed), but encrypted-mode WeChat Official accounts are wholly non-functional. Current vs expected: detect msg_signature/AES-CBC EncodingAESKey path and decrypt, or document that only plaintext mode is supported.
- **evidence**: wechat_official.py:409 `signature = params.get("signature", "") or params.get("msg_signature", "")`; verify_signature (line 162-175) hashes only token+timestamp+nonce, omitting the `encrypt` ciphertext that WeChat's msg_signature includes. No AES/EncodingAESKey handling exists anywhere in the module (grep for AES/Cipher/decrypt returns nothing).
- **verify**: Configure the channel against an encrypted-mode WeChat test account and POST a real `<Encrypt>` envelope with its msg_signature — observe a 401 (verify_signature mismatch) and no decryption. Acceptance: either implement the AES-CBC decrypt + msg_signature scheme or reject at config time with a clear 'encrypted mode unsupported' error.

### NEW-providers-4 — Low bug (NEW, conf=medium) — scout:fresh-bug-agent-providers
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/declarative.py:236-240 (_build_inner) + ModelSpec/chat_endpoint`
- **problem**: Declarative providers ignore their own configured `chat_endpoint` and non-bearer auth kinds. For request_format=openai_compatible the inner adapter is built as OpenAIProvider(api_key, base_url) which always posts to <base_url>/chat/completions via the OpenAI SDK — a spec that declared chat_endpoint='/v1/messages' or '/api/chat' is silently dropped. Likewise auth_kind 'header'/'query_param' (parsed and accepted by load_spec_from_toml) are ignored and the key is sent as Authorization: Bearer, so a provider that requires a custom auth header/param will 401.
- **evidence**: _build_inner: `if spec.request_format == 'openai_compatible': return OpenAIProvider(api_key=api_key, base_url=spec.base_url)` — spec.chat_endpoint is never threaded through; _resolve_api_key reads only env_var and the inner OpenAIProvider has no concept of header/query-param auth. The docstring TODO at lines 222-227 acknowledges the auth gap but the chat_endpoint drop is silent and undocumented.
- **verify**: Load a TOML spec with chat_endpoint='/api/chat' + auth_kind='header' and assert the built DeclarativeProvider actually targets that endpoint / sends the custom header; today it does neither.

### NEW-fresh-perf-3 — Low performance (NEW, conf=medium) — scout:fresh-perf
- **loc**: `python/packages/corlinman-episodes/src/corlinman_episodes/archive.py:229-263, 352, 355-366`
- **problem**: archive_unreferenced_episodes does one DB commit per archived row plus a synchronous per-file atomic write inside an async function. current: the loop calls _blank_hot_columns (one UPDATE + commit per row, line 352) and _atomic_write (sync mkdir+write_text+replace per row, 355-366) for each candidate, all on the event loop. expected: batch the UPDATEs into a single transaction (commit once per sweep) and/or offload file writes, matching PERF-008's per-row-commit concern in the same package.
- **evidence**: archive.py:262 `await _blank_hot_columns(store, episode_id=row['id'])` inside `for row in candidates:` (229); _blank_hot_columns ends with `await store.conn.commit()` (352) — one fsync-class commit per row. archive.py:259 `_atomic_write(cold_path, payload)` per row; _atomic_write does `path.parent.mkdir(...)`, `tmp.write_text(...)`, `tmp.replace(path)` (363-366) — synchronous disk I/O per candidate on the loop. Runs as the evolution.darwin_curate scheduled job (background, so lower impact than request-path).
- **verify**: Bench archive_unreferenced_episodes with N=10k candidates and assert it issues 1 commit (today N) and that the coroutine yields between file writes; or assert wall-time scales sub-linearly after batching the UPDATE.

### NEW-fresh-perf-4 — Low performance (NEW, conf=medium) — scout:fresh-perf
- **loc**: `python/packages/corlinman-agent-brain/src/corlinman_agent_brain/link_planner.py:225-256, 261-284`
- **problem**: plan_links issues one retrieval round-trip per candidate strictly serially (await inside the for loop), so a batch of K candidates costs K sequential RTTs; plan_links_batch then fans out over an unbounded number of batches with a bare asyncio.gather. current: candidates within a batch are awaited one-by-one; batch count is uncapped. expected: within a batch, gather candidate retrievals concurrently (or bound them), and cap the cross-batch gather to avoid unbounded concurrency against the retrieval provider.
- **evidence**: link_planner.py:244 `existing_nodes = await retrieval_provider(query, limit=...)` sits inside `for candidate in candidates:` (225) — K serial RTTs per batch. link_planner.py:279-283 `tasks = [plan_links(batch, ...) for batch in candidate_batches]; results = await asyncio.gather(*tasks)` — no bound on number of batches. Offline memory-curation path (not per-request), so impact is throughput of curation, not chat latency.
- **verify**: Bench plan_links with a retrieval_provider that sleeps 50ms and K=20 candidates: today ~1000ms (serial); concurrent gather should drop to ~50ms. Add a test asserting plan_links_batch caps in-flight batches via a semaphore.

### NEW-providers-6 — Low performance (NEW, conf=low) — scout:fresh-bug-agent-providers
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/subagent/blackboard.py:112-121 (_open) vs docstring line 116`
- **problem**: BlackboardStore._open opens each connection with isolation_level='IMMEDIATE' and the inline comment claims 'Readers are still concurrent under WAL', but WAL is never enabled (no `PRAGMA journal_mode=WAL`). With the default rollback journal an IMMEDIATE write transaction takes a reserved/exclusive lock that blocks concurrent sibling readers — exactly the concurrent fan-out (spawn_many siblings reading/writing the same blackboard) the module is designed for. Behaviour diverges from the documented contract; under contention readers can hit SQLITE_BUSY.
- **evidence**: `conn = sqlite3.connect(self._db_path, isolation_level='IMMEDIATE')` with no `conn.execute('PRAGMA journal_mode=WAL')` anywhere in the file; the comment at line 116 asserts WAL concurrency that is never configured.
- **verify**: Open two BlackboardStore connections concurrently, begin an IMMEDIATE write on one and read on the other, and observe the reader blocking/SQLITE_BUSY (no WAL) rather than the concurrent-read behaviour the comment promises.

### NEW-verifyperf-1 — Low performance (CONFIRMED, conf=low) — scout:verify-perf
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/context_assembler.py:217,247,263`
- **problem**: context_assembler.assemble re-scans the full message list in 3 independent sequential loops (cascade-var, toolbox dedup/gate, placeholder pass) on top of stage-1 expansion — multiple full passes over msgs per assembly. Low impact because it runs once per turn over the turn-start message list (typically small), not per round or per delta.
- **evidence**: `assemble` (L190) iterates `for msg in msgs` three separate times: stage 2 cascade-var at L217-227, stage 3.5 toolbox at L247-260, stage 4 placeholder at L263-288, plus stage 5 emit hook re-iterates in _emit_preprocessed (L456). Each pass re-tests `isinstance(content,str)` / privilege and re-runs regex (`_BARE_KEY_RE.sub`, `_TOOLBOX_NS_RE.search/sub`). Caller agent_servicer._assemble_context (L2286-2294) invokes assemble once per ChatStart (corlinman-user-model/placeholders.py:38 comment: "context_assembler is invoked once per turn"), so the multi-pass cost is bounded and not on the per-round hot loop → Low.
- **verify**: Profile assemble() with a 200-message input and confirm ~3-4 full list traversals; a single fused pass would cut traversal count — but confirm message-count at turn start is small before prioritizing.

### PERF-007 — Low performance (RESOLVED, conf=medium) — scout:verify-perf
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/reasoning_loop.py:903-949 (cited line 929)`
- **problem**: Filed defect (full O(N) re-walk + `hash(repr(messages[0]))` token re-estimate every reasoning round) no longer holds: the per-round estimate is now an incremental cache that re-walks only the appended tail.
- **evidence**: `messages_total_token_estimate` (L903) keeps `_messages_char_total/_messages_token_seen/_messages_token_head_hash` (init L737-741, invalidated L888-901). Hot path L945-949 walks only `messages[self._messages_token_seen:]` (the tail). The `hash(repr(messages[0]))` at L929 is now only a head-fingerprint to detect in-place edits of messages[0]; it hashes a single message (the system prompt), not the whole growing list. Caller L1016 invokes it once per round and feeds the cached total into `_compact_history` (prev_estimate) so that path also skips its own full walk (L387). Net: the original per-round O(N) re-scan is gone; residual cost is O(|messages[0]|) repr+hash, constant across rounds.
- **verify**: Run a 50-round tool loop with a long system prompt; assert messages_total_token_estimate's tail-walk branch (L945) is taken for rounds 2..N and the full re-walk branch (L937) only on round 1 / after compaction.

### PERF-009 — Low performance (CHANGED, conf=medium) — scout:verify-perf
- **loc**: `ui/components/chat/markdown-message.tsx:88 (+ ui/components/chat/message-bubble.tsx:64)`
- **problem**: The all-bubbles re-parse storm is fixed (R4-D5), but the actively-streaming bubble still fully re-parses its markdown via <ReactMarkdown> on every SSE delta. This residual is largely inherent to live streaming and now affects only one bubble.
- **evidence**: message-bubble.tsx L64 wraps MessageBubble in React.memo so settled bubbles (stable identity) no longer re-render/re-parse on a pending delta (comment L59-63 cites R4-D5). But markdown-message.tsx L86-90 renders `<ReactMarkdown rehypePlugins={[rehypeSanitize]} components={components}>{content}</ReactMarkdown>` with no memo keyed on `content`; the streaming bubble's `content` changes every delta (use-chat-stream applyEvent), so its markdown tree is rebuilt + re-sanitized each token. `components` is memoized (L24-84) so only the parse of changing content remains. The filed cross-all-bubbles storm is resolved; the single-streaming-bubble re-parse is expected behavior.
- **verify**: Render a MessageList with N settled bubbles + 1 pending; drive 100 deltas and assert ReactMarkdown parse runs only for the pending bubble (settled bubbles' render count stays 0 due to memo).

### PERF-011 — Low performance (CONFIRMED, conf=low) — scout:verify-perf
- **loc**: `ui/lib/api/chat.ts:143-146`
- **problem**: Manual SSE frame parser does `buffer += decoder.decode(value)` then `buffer.split("\n\n")` on each read. Worst case is O(N^2) when a single large SSE frame spans many reads: the growing buffer is fully re-split on every chunk until the frame terminator arrives.
- **evidence**: L143 `buffer += decoder.decode(value, { stream: true })`, L145 `const frames = buffer.split("\n\n")`, L146 `buffer = frames.pop() ?? ""`. For normal token streams (frequent `\n\n`) the buffer resets to a tiny tail each read → effectively linear. But a single multi-KB frame delivered across M TCP reads is re-concatenated and fully re-split M times → O(frame_len × M). Indexed scanning (lastIndexOf of the terminator from a saved offset) avoids the repeated full split.
- **verify**: Feed a 256KB single SSE `data:` frame in 4KB reads through this async generator and time it vs an offset-based scanner; expect quadratic blowup as frame size grows.

### PERF-013 — Low performance (CONFIRMED, conf=high) — scout:verify-perf
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/services/chat_service.py:292`
- **problem**: Per tool-done frame the code does `__import__("json")` (dynamic import lookup) plus `bytes(tc.args_json)` materialization instead of a module-level `import json`. Minor but on a per-frame path; the file has no top-level `import json`.
- **evidence**: L289 gate `if tc.plugin.startswith(_BUILTIN_DONE_PREFIX)` then L291-293 `meta = (__import__("json").loads(bytes(tc.args_json).decode("utf-8")) if tc.args_json else {})`. Confirmed there is no module-level `import json` in chat_service.py (only asyncio/logging at L25-26). `__import__("json")` resolves via sys.modules cache so cost is an attribute/dict lookup per frame, plus a `bytes(...)` copy of args_json. Frequency is once per `_builtin_done:` tool-completion notification (one per tool call), NOT per token — so genuinely Low impact despite the Medium backlog tag.
- **verify**: Add `import json` at module top and use `json.loads(...)`; assert behavior unchanged and the per-frame dynamic-import lookup is removed.

### PERF-015 — Low performance (CONFIRMED, conf=high) — scout:verify-perf
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/reasoning_loop.py:1573`
- **problem**: _extend_with_tool_round copies the entire running message list via `list(messages)` once per tool round, so total copy work grows O(rounds × messages) over a multi-round agent loop.
- **evidence**: L1566-1573 `def _extend_with_tool_round(messages, calls, results) -> list[...]: extended: list[dict] = list(messages)` then appends the assistant tool_calls message + one tool message per result (L1574-1599). Called at L1166-1167 `messages = _extend_with_tool_round(messages, tool_calls_this_round, results)` inside the round loop. Each round shallow-copies the full (growing) list; over R rounds with the list reaching N entries this is O(R×N) list-slot copies (references only, so modest) plus a fresh list allocation per round. Low severity — references not deep copies — but avoidable by mutating in place or appending to the same list.
- **verify**: Run a 100-round tool loop and count list allocations from _extend_with_tool_round; compare to an in-place `messages.extend(...)` variant.

### TEST-010 — Low test (CHANGED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-providers/src/corlinman_providers/openai_compatible.py:1-73`
- **problem**: current: backlog points TEST-010 at openai_compatible.py for 'error-classification untested', but openai_compatible.py is a thin subclass of OpenAIProvider that overrides only build/params_schema/supports — it contains NO error-classification code. The actual classifier `_map_openai_error` lives in openai_provider.py:382 and IS tested (test_openai_provider_error_mapping.py covers 429/Retry-After/401/etc.), and OpenAICompatibleProvider inherits it unchanged. So the substance is RESOLVED/misdirected. Residual gap is only that OpenAICompatibleProvider's own build/supports/base_url-required validation has light coverage (declarative/registry tests reference it but no dedicated error test). Mark CHANGED.
- **evidence**: openai_compatible.py defines only __init__/build/params_schema/supports, no except/classify. Error mapping is openai_provider.py:382 `_map_openai_error` (401/402/403/404/429 branches), tested by test_openai_provider_error_mapping.py (test_429_with_retry_after_header_extracts_ms etc.). openai_compatible referenced in tests: test_registry_config_driven.py, test_auth_refresh.py, test_declarative.py.
- **verify**: confirm: OpenAICompatibleProvider inherits _map_openai_error (no override) → test_openai_provider_error_mapping.py already exercises the inherited classification path; the only untested own-logic is the base_url-required ValueError in build()/__init__.

### NEW-fresh-quality-6 — Low quality (NEW, conf=high) — scout:fresh-quality
- **loc**: `python/packages/corlinman-nodebridge/, python/packages/corlinman-wstool/, python/packages/corlinman-shadow-tester/`
- **problem**: Three full packages are dead: zero external code importers, no [project.scripts] entry points, and absent from the root meta-package dependencies (so `uv sync` of the meta-package never even pulls them). They are Rust-port leftovers nobody wired into the server or agent.
- **evidence**: For each of corlinman_nodebridge / corlinman_wstool / corlinman_shadow_tester: `grep -rn 'import corlinman_<pkg>|from corlinman_<pkg>' python/ (excluding own dir)` = 0 hits. Their pyprojects declare no [project.scripts]. Root pyproject.toml dependencies list (lines 13-25) does not include them. The only string matches for 'wstool'/'nodebridge' in corlinman-server are config-section name literals in gateway/core/config_watcher.py:63,493,495 (RESTART_REQUIRED_SECTIONS / diff_sections), not Python imports. (NOTE: distinct from backlog QUAL-020 which only covers user-model + agent-brain; corlinman-goals is NOT dead — it has a `corlinman-goals` console entry point.)
- **verify**: `for p in nodebridge wstool shadow-tester; do grep -rln "corlinman_${p//-/_}" python/ --include=*.py | grep -v "/corlinman-$p/"; done` -> all empty. `grep -E 'nodebridge|wstool|shadow-tester' pyproject.toml` -> no dependency entry.

### NEW-fresh-quality-8 — Low quality (NEW, conf=high) — scout:fresh-quality
- **loc**: `python/packages/corlinman-channels/src/corlinman_channels/{discord.py:718-741,feishu.py:744-757,slack.py:643-654,telegram_send.py:420-438}`
- **problem**: `_note_retry_after(self, resp)` is duplicated nearly verbatim across 4 channel adapters: same default `retry_after = 1.0`, same `try: float(...)` parse, same `self._edit_rate_limit_until = time.time() + retry_after`. The only real variation is where the value is read (JSON body vs header vs telegram parameters). Duplicate 429 back-off logic that should be a shared helper / mixin. Distinct from backlog QUAL-011 (RFC3339 formatter dup).
- **evidence**: 4 identical method defs (`grep -rn 'def _note_retry_after' channels/*.py`) and 4 identical `self._edit_rate_limit_until = time.time() + retry_after` lines (discord.py:741, feishu.py:757, slack.py:654, telegram_send.py:438). Drift risk: each copy re-implements its own header-vs-body extraction and could diverge silently.
- **verify**: diff the four `_note_retry_after` bodies; they differ only in the extraction source. Extract to a shared base (e.g. `_RateLimitedSender`) and assert all four still back off correctly.

### NEW-fresh-quality-9 — Low quality (NEW, conf=high) — scout:fresh-quality
- **loc**: `.importlinter:17-33`
- **problem**: Even setting aside the broken embedding reference, the layering contract is incomplete: only 5 root packages are declared (grpc, providers, agent, embedding[deleted], server) out of 26 workspace packages. 21 packages (channels, hooks, identity, evolution-engine, evolution-store, memory-host, mcp-server, subagent, skills-registry, persona, episodes, goals, replay, canvas, tagmemo, auto-rollback, user-model, agent-brain, nodebridge, wstool, shadow-tester) sit in no layer, so cross-layer imports involving them are never checked. The header comment even instructs 'every package must appear in exactly one layer so the contract stays total' — it isn't total.
- **evidence**: .importlinter [importlinter] root_packages lists only corlinman_grpc / corlinman_providers / corlinman_agent / corlinman_embedding / corlinman_server. `ls python/packages` shows 26 packages. Contract comment lines 14-15: 'every package must appear in exactly one layer so the contract stays total.' The 21 listed are absent from both root_packages and the layers stanza.
- **verify**: Compare `ls python/packages/ | wc -l` (26) against the 5 names in .importlinter root_packages. Once the embedding bug is fixed, add the missing packages to make the contract total, then re-run `uv run lint-imports`.

### NEW-providers-5 — Low quality (NEW, conf=medium) — scout:fresh-bug-agent-providers
- **loc**: `python/packages/corlinman-subagent/src/corlinman_subagent/supervisor.py:569 (_safe_emit_obs)`
- **problem**: Uses the deprecated asyncio.get_event_loop() to schedule the W3.2 observability emit. On Python 3.12 this emits a DeprecationWarning and, if ever called from a thread/context without a running loop, raises RuntimeError (only partially guarded — the surrounding try catches RuntimeError but the intent was get_running_loop()). The sibling code in reasoning_loop.py:858 correctly uses get_running_loop(); supervisor diverges.
- **evidence**: `try: loop = asyncio.get_event_loop()  except RuntimeError: return`. asyncio.get_event_loop() is deprecated for this use in 3.12+; the correct call (used elsewhere in the same codebase, reasoning_loop.py:858) is asyncio.get_running_loop().
- **verify**: Run the supervisor spawn path under `python -W error::DeprecationWarning` with an event_emitter wired and observe the DeprecationWarning from get_event_loop().

### NEW-verifyqualtest-1 — Low quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/session.py:1-17`
- **problem**: current: corlinman_agent/session.py is a 17-line pure stub (docstring + future import + structlog logger; zero classes/functions, TODO(M2) to implement a pydantic Session). It has no importers anywhere. expected: implemented or removed. (The 'session.py/cancel.py stubs' backlog note — session.py is the genuine dead stub; cancel.py actually implements with_timeout but its promised `combine` is unimplemented and cancel.py also has zero importers.)
- **evidence**: session.py last line is `logger = structlog.get_logger(__name__)`, no other symbols. `grep -rn 'corlinman_agent.session import\|from .session import'` shows no consumers of this module (the `from .session import` hits belong to corlinman-mcp-server's own session.py). cancel.py:23 implements `with_timeout` but TODO(M2) `combine` never written; `grep 'corlinman_agent.cancel\|from .cancel import'` → no importers.
- **verify**: `grep -rn 'corlinman_agent\.session\b\|corlinman_agent\.cancel\b' python/packages/*/src` returns nothing → both modules unused; session.py exports nothing.

### QUAL-008 — Low quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-agent/src/corlinman_agent/approval_gate.py:1-17`
- **problem**: current: 17-line stub module — module docstring + `from __future__ import annotations` + `import structlog` + a module-level logger; defines zero classes/functions (only a TODO(M2)). expected: implemented or removed. No symbols, no importers; confuses contributors looking for the approval gate (the real one is corlinman-server gateway/middleware/approval.py).
- **evidence**: File is 17 lines total, last symbol is `logger = structlog.get_logger(__name__)`. `grep -rn approval_gate python/.../src` shows the only `approval_gate` references are unrelated `request.state.approval_gate` attrs and ApprovalGate in corlinman-server middleware/approval.py — nothing imports corlinman_agent.approval_gate.
- **verify**: `grep -rn 'corlinman_agent.approval_gate\|from .approval_gate' python/packages/*/src` → no importers; module exports nothing.

### QUAL-009 — Low quality (CHANGED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:2021-2083`
- **problem**: current: RedisJournalBackend.open() raises NotImplementedError and open_backend_from_env routes CORLINMAN_JOURNAL_BACKEND=redis to it. backlog framed this as dead-code/defect; on HEAD it is an INTENTIONAL fail-loud stub (comment lines 2014-2018, 2056-2059) so a misconfigured HA deployment errors at startup instead of silently writing a local file other gateways can't read. The redis branch is only reachable when an operator explicitly sets the env var. Not a defect — flag CHANGED so the backlog stops calling it dead-code; at most a Low documentation/cleanup item.
- **evidence**: `agent_journal_backend.py:2032` `raise NotImplementedError('redis journal backend not yet implemented; set CORLINMAN_JOURNAL_BACKEND=sqlite ...')`; selector at :2074-2080 only routes when kind=='redis' (default is sqlite, :2062-2065). Comment block :2014-2018 documents the loud-fail intent.
- **verify**: `CORLINMAN_JOURNAL_BACKEND` unset → SqliteJournalBackend (no raise). Only redis explicit value raises — intended behavior, covered by reading open_backend_from_env.

### QUAL-012 — Low quality (CONFIRMED, conf=high) — scout:verify-qual-test
- **loc**: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/sessions.py:617-620`
- **problem**: current: route handler calls `journal._load_messages(tid)` — reaching across a module boundary into a leading-underscore (semi-private) method on AgentJournal. The comment at :617 itself flags it as "semi-private but stable". agent_journal.py:234 defines `_load_messages` only as a backward-compat alias to the public `load_messages`. expected: call the public `load_messages` (or journal exposes a public accessor). This is the exact pattern the backlog says caused commit ef4e341.
- **evidence**: `routes_admin_a/sessions.py:620` `msgs = await journal._load_messages(tid)` with :617 comment `# ``_load_messages`` is semi-private but stable`. agent_journal.py:234-242 docstring: 'Backward-compat alias ... the new backend exposes load_messages (public).'
- **verify**: swap call to `journal.load_messages(tid)`? (note: load_messages lives on the backend, not AgentJournal — needs a public passthrough). Static: grep shows the only cross-boundary `_load_messages` consumer is sessions.py:620.
