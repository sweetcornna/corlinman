# Round 6 · Recon findings (79) — HEAD 0c281b6

sev={'High': 21, 'Low': 25, 'Critical': 1, 'Medium': 32} | status={'NEW': 40, 'CANT_REPRO': 3, 'RESOLVED': 6, 'CONFIRMED': 29, 'CHANGED': 1} | type={'regression': 13, 'security': 18, 'bug': 13, 'completeness': 11, 'test': 8, 'performance': 10, 'quality': 6}

### NEW-reg-r5-voice-deploy-ui-1 — Critical regression (NEW, conf=high) — reg-r5-voice-deploy-ui
- loc: `deploy/install.sh:777-799 (install_native systemd unit) + :720-735 + :729`
- problem: R5-S3 added `User=corlinman`/`Group=corlinman` to the native gateway systemd unit but `ExecStart=${uv_path} run corlinman-gateway` still points at the INSTALL user's uv binary, which the unprivileged service user cannot reach. In the documented default deploy (root install + native systemd, per MEMORY deploy_prod_server_native), `command -v uv` (line 777) resolves to `/root/.local/bin/uv` (installed at line 727-729 as root). `/root` is mode 0700 on Linux, so the `corlinman` user (created `--no-create-home --shell /usr/sbin/nologin`) gets EACCES executing `/root/.local/bin/uv`. The unit also sets NO `Environment=PATH=` (the sibling upgrader unit at :830 explicitly does) and NO `HOME=`, so even if uv were reachable, `uv run` for a homeless nologin user has no writable cache dir (`~/.cache/uv`). With `Restart=on-failure`/`RestartSec=5` this is a boot crash-loop: the gateway never comes up after a fresh root install. Pre-R5 the unit ran as root and `uv_path` was accessible, so this is a clean regression introduced by the User/Group addition.
- evidence: Line 729 installs uv to `$HOME/.local/bin` (=/root/.local/bin as root). Line 735 `chown -R "$(id -u):$(id -g)" $PREFIX` = root:root, leaving repo/.venv root-owned (not chowned to corlinman; only DATA_DIR+ui-static are, :770-773). Line 777 `uv_path="$(command -v uv)"` = /root/.local/bin/uv. Line 785-788: `User=corlinman` + `ExecStart=/root/.local/bin/uv run ...`. The upgrader unit (:827 User=root, :830 Environment=PATH=/root/.local/bin:...) works because it runs as root; the gateway unit has neither root nor a PATH/HOME override. R5-S3's own commit msg admits 'real-VM verification deferred (no Linux host in sandbox)'; the static test_install_sh_hardening.py only greps for the User= line and re-chown, so it cannot catch this.
- verify: On a root-installed Linux box: `sudo -u corlinman /root/.local/bin/uv run corlinman-gateway` → EACCES / 'permission denied'. Or `systemctl status corlinman` after install → activating(auto-restart)/failed with exec error. Also `ls -ld /root` shows drwx------ and `ls -l /root/.local/bin/uv`.

### NEW-reg-r5-providers-1 — High regression (NEW, conf=high) — reg-r5-providers
- loc: `python/packages/corlinman-providers/src/corlinman_providers/anthropic_provider.py:468-481 and bedrock_provider.py:432-445 (the role=="tool" branch)`
- problem: PARALLEL tool calls are broken by the R5 (#R5-B1) translation. Each role="tool" message is emitted as its OWN separate {"role":"user","content":[tool_result]} turn. When an assistant turn fires N>1 parallel tool_use blocks, the N tool_results are split across N consecutive user messages. Anthropic's Messages API requires that ALL tool_result blocks answering a single assistant turn's parallel tool_use blocks appear in the immediately-following SINGLE user turn; otherwise it returns 400 (e.g. 'tool_use ids were found without tool_result blocks immediately after' / roles-must-alternate). So R5 fixed single-tool multi-round but newly broke multi-tool (parallel) rounds. This is a reachable path: reasoning_loop supports multiple calls per round (tool_calls_this_round is a list at reasoning_loop.py:1104/1119; _extend_with_tool_round at reasoning_loop.py:1591-1601 appends one role="tool" message PER result; comment at reasoning_loop.py:1233 confirms 'multiple parallel tool calls'). _split_system output is passed straight to client.messages.stream(messages=...) (anthropic_provider.py:322-338) and to the Bedrock body (bedrock_provider.py:391) with no batching/merge step.
- evidence: Ran /tmp/repro_b1.py against the real _split_system functions. Input: assistant with tool_calls [call_a, call_b] then two role="tool" messages (call_a, call_b). Output chat roles = ['user','assistant','user','user']: assistant carries BOTH tool_use blocks, but tool_result for call_a is in chat[2] (its own user turn) and tool_result for call_b is in chat[3] (a second user turn). The first user turn following the 2-tool_use assistant turn therefore contains only ONE of the two required tool_result blocks. No merge happens downstream (anthropic_provider.py:322 -> messages.stream; bedrock_provider.py:391 body['messages']=chat). Identical defect in both providers. No test covers >1 tool_call/round (grep of both test files shows only single-call cases: call_1/call_9/call_bad).
- verify: uv run python /tmp/repro_b1.py (see 'PARALLEL anthropic'/'PARALLEL bedrock' sections: 4 messages, two trailing single-tool_result user turns). To confirm the API rejection, a real Anthropic call with two parallel tool_use blocks answered by two separate user-turn tool_results returns a 400 about tool_use without an immediately-following tool_result. Correct shape: collapse consecutive role="tool" messages that answer the same assistant turn into one user message whose content is [tool_result_A, tool_result_B, ...].

### R5-B3 — High regression (CANT_REPRO, conf=high) — reg-r5-server
- loc: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:826-1004,1053-1184,1423-1446,1516-1604,1634-1701,1982-1992`
- problem: Potential deadlock or missed-lock regression from the new backend-level asyncio write-lock (#R5-B3).
- evidence: Verified NO deadlock and NO missed write method. The only internal write-method call from within a lock-holding method is complete_turn (line 890 `async with self._write_lock`) -> _populate_turn_aggregates (line 911), and _populate_turn_aggregates deliberately does NOT re-acquire the non-reentrant lock (line 977-983 it runs bare execute+commit under the caller's lock). Grep of all 11 write methods (begin_turn, complete_turn, error_turn, append_message, append_messages, delete_session, update_session_meta, mark_stale_in_progress_as_errored, append_event, append_events_batch, update_turn_cost) shows every commit()/BEGIN IMMEDIATE envelope is wrapped in `async with self._write_lock` and none calls another self.<write_method> while holding it (awk grep of lines 789-2040 for internal write calls returns only `_populate_turn_aggregates`). All write SQL (INSERT/UPDATE/DELETE) is accounted for under the lock; reads (find_resumable_turn, load_messages, session_exists, list_resumable_in_progress, etc.) are lock-free and issue NO commit(), so they cannot flush an in-flight transaction. update_session_meta calls session_exists/list_session_summaries OUTSIDE the lock (reads, no commit). Awaits under the lock are all bounded sqlite ops. test_agent_journal_backend.py + test_agent_journal_sessions.py + tests/journal = 45 passed.
- verify: uv run pytest python/packages/corlinman-server/tests/test_agent_journal_backend.py python/packages/corlinman-server/tests/test_agent_journal_sessions.py python/packages/corlinman-server/tests/journal -p no:cacheprovider -q

### R5-P1 — High regression (CANT_REPRO, conf=high) — reg-r5-server
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/services/direct_backend.py:274-371,509-531`
- problem: Potential regression where slot[1] (now list[str], joined at finalization) is read as a str before finalization, or via stale += / indexing.
- evidence: Verified all slot[1] accesses are list-aware. open_calls initialized as `[name, []]` / `["", []]` (lines 297,314); fragments appended via `[1].append(...)` (lines 305,316); all three finalization sites unpack `name, frags = ...` and pass `"".join(frags)` to _tool_call_frame (lines 327,338,362). _tool_call_frame(args_json: str) (line 512) always receives a str. grep of `[1]`/`open_calls`/`.join(` shows no surviving `+=`, no str-indexing, no len() on slot[1]. Behavioral equivalence preserved: old `[1] += (frag or "")` for empty fragments maps to `[1].append("")`, and `"".join([...,"",...])` is byte-identical. Byte-identity + O(K) tests pass (test_many_arg_deltas_assemble_byte_identical, test_arg_assembler_is_linear_not_quadratic). 26 passed for store+direct_backend suite.
- verify: uv run pytest python/packages/corlinman-server/tests/gateway/services/test_direct_backend.py -p no:cacheprovider -q

### R5-P3 — High regression (CANT_REPRO, conf=high) — reg-r5-server
- loc: `python/packages/corlinman-server/src/corlinman_server/system/subagent/store.py:267-296,347-367,458-480`
- problem: Potential regression: _prune_terminal_locked evicting an in-flight row, breaking count_in_flight_for_tenant, or a missing finished_at causing wrong eviction order / KeyError.
- evidence: Verified prune is correct. terminal_ids filtered by status.is_terminal() (line 278-282) so in-flight rows ({queued,running,stalled}, line 83) are never evicted. count_in_flight_for_tenant (line 420) counts only is_in_flight() rows and cross-references self._requests[rid]; since prune evicts only terminal statuses + their paired requests (lines 294-296), every in-flight rid keeps its request -> count stays correct (test_in_flight_rows_never_evicted asserts this for 2 tenants under 3x-cap flood). finished_at is always set on terminal transitions: dispatcher passes finished_at=_now_ms() on both terminal updates (dispatcher.py:407,457) and set_killed sets it (store.py:473); the `finished_at or 0` fallback (line 289) is defensive only. Terminal update() wrapped in try/except KeyError at dispatcher.py:412,465 so an evicted-row late update degrades gracefully; set_summary/append_log are race-tolerant no-ops (store.py:492,449). list_all returns statuses only; admin endpoints + get_request tolerate None for aged-out rows. 26 passed.
- verify: uv run pytest python/packages/corlinman-server/tests/system/subagent/test_store.py -p no:cacheprovider -q

### NEW-fhfunc-1 — High completeness (NEW, conf=high) — fh-func-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/grpc/placeholder.py:529-616 (build_default_engine) + corlinman_persona/placeholders.py:77, corlinman_user_model/placeholders.py:33, corlinman_goals/placeholders.py:1`
- problem: The {{persona.*}}, {{user.*}} and {{goals.*}} prompt placeholders are documented/accepted features but are NEVER wired into the live render path, so they leak verbatim into the prompt sent to the model. build_default_engine() (the single production placeholder engine builder) only register_namespace()'s 'episodes' (line 571) and conditionally 'memory' (line 587). It never registers PersonaResolver / UserModelResolver / goals resolver. A grep across the whole server confirms the only register_namespace calls are episodes+memory. The engine returns unknown tokens VERBATIM (render() docstring line 309), so a template with {{persona.mood}} / {{user.name}} / {{goals.weekly}} ships the literal braces to the LLM and surfaces them in unresolved_keys.
- evidence: /tmp repro against PlaceholderEngine rendered `Mood: {{persona.mood}} | Fatigue: {{persona.fatigue}} | User: {{user.name}}` UNCHANGED with UNRESOLVED=['persona.mood','persona.fatigue','user.name']. Resolver classes exist (PersonaResolver.resolve(self,key,agent_id:str) p.placeholders.py:89; UserModelResolver.resolve(self,key,user_id:str) p.placeholders.py:57) but their signature is (key,id:str) vs the engine's DynamicResolverLike.resolve(key,ctx:PlaceholderCtx) (placeholder.py:199) — incompatible, no adapter exists. corlinman_goals is imported NOWHERE in the server. Docs mark these done: phase3-roadmap.md:108/296 ({{user.*}} 'demonstrably influences a real prompt'), phase4-w4-d2-design.md:131 ({{persona.*}}/{{user.*}} register), phase4-next-tasks.md:218 ({{goals.weekly}} cascade done 300d64d, acceptance D2 '4-item distilled list'). R4-F2 (PROGRESS.md:134) only ever wired EpisodesResolver and honestly left memory echo-only — it never claimed persona/user/goals, so this is NOT covered by the closed R4-F2.
- verify: uv run python -c "import asyncio,sys; sys.path.insert(0,'python/packages/corlinman-server/src'); from corlinman_server.gateway.grpc.placeholder import PlaceholderEngine,PlaceholderCtx; print(asyncio.run(PlaceholderEngine().render('{{persona.mood}} {{goals.weekly}} {{user.name}}', PlaceholderCtx(session_key='s'))))" — prints the template verbatim. Then grep -rn 'register_namespace(' python/packages/corlinman-server/src | grep -v test → only episodes + memory.

### NEW-fhfunc-2 — High completeness (NEW, conf=high) — fh-func-deep
- loc: `python/packages/corlinman-auto-rollback/src/corlinman_auto_rollback/applier.py:189-308 (apply/_commit_apply) ; admin route python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_b/evolution.py:801-824`
- problem: Applying an approved self-evolution proposal via POST /admin/evolution/{id}/apply is bookkeeping-only: it never mutates any skill/prompt file or kb.sqlite. The route constructs corlinman_auto_rollback.EvolutionApplier(_resolve_connection(store)) and calls apply(); apply() only records an intent-log row, inserts an evolution_history row (after_sha = sha256(proposal.diff)), and flips proposal.status -> 'applied' (repo.mark_applied = `SET status='applied'`). The proposal.diff is hashed but written to no file. So 'Apply' for GEPA prompt_template / Darwin skill_update / agent_card / tool_policy proposals changes ZERO live behaviour. The applier docstring (applier.py:5-12) admits 'kb / filesystem mutators stay a follow-up'. The handler-based gateway EvolutionApplier (gateway/evolution/applier.py) that COULD carry per-kind mutators has register_handler() called nowhere in production (grep confirms only its own class definition references it), and its __init__ has no DEFAULT_HANDLERS — even though gateway/evolution/__init__.py:9 calls it 'the concrete Applier for memory_op proposals'.
- evidence: applier.py:271-296 builds EvolutionHistory(before_sha=sha256(inverse_diff), after_sha=sha256(proposal.diff)) and calls self._proposals.mark_applied — no open()/write/Path/skill/prompt/kb anywhere in the module (grep returns only docstring mentions). evolution-store repo.py:459-464 mark_applied = 'UPDATE ... SET status=\'applied\', applied_at=?'. The UI tells the operator this is behaviorally live + irreversible: ui/lib/locales/en.ts:1986 'This is irreversible without manual rollback. Apply?'. multi-agent-release-plan.md:160-166 advertises memory_op/tag_rebalance as 'auto-apply ... already shipped' yet neither applier wires a handler for them. (The one self-evolution path that DOES do real work is UserCorrectionApplier at entrypoint.py:2090, a separate background-review flow — not the operator apply button.)
- verify: grep -rn 'register_handler(' python/packages/corlinman-server/src | grep -v test → no call sites (only the def + docstrings). grep -rn 'open(|write|Path(|kb\.|skill|prompt' python/packages/corlinman-auto-rollback/src/corlinman_auto_rollback/applier.py → only docstring. Drive POST /admin/evolution/{id}/apply on an approved prompt_template proposal → 200 applied + history row, target SKILL.md / prompt file on disk unchanged.

### NEW-fhfunc-4 — High completeness (NEW, conf=high) — fh-func-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:399-429 (build_voice_state_from_app) ; persistence at routes_voice/persistence.py (no SQLite store)`
- problem: Voice session persistence and the voice-transcript->chat bridge never run in production. build_voice_state_from_app() builds VoiceState with provider=resolve_voice_provider(...) but leaves session_store AND transcript_sink at their None defaults (VoiceState fields mod.py:249-250). Every persistence call in run_voice_session is guarded by `if state.session_store is not None` (mod.py:719, 826) and the transcript sink by `if transcript_sink is not None`, so in the gateway mount path: (a) no voice_sessions row is ever written, and (b) voice turns are never appended to the chat session, so the agent loop can't see voice turns. Worse, there is NO SQLite VoiceSessionStore at all — only MemoryVoiceSessionStore (persistence.py:170); the VoiceSessionStore Protocol docstring (line 155) and module docstring (line 19) call the SQLite adapter a 'follow-up' / 'TODO'.
- evidence: build_voice_state_from_app returns VoiceState(config_loader=..., provider=provider, data_dir=...) with no session_store/transcript_sink kwargs (mod.py:425-429). persistence.py:155 'production uses the SQLite-backed adapter (TODO; see file docstring)'; only MemoryVoiceSessionStore exists. Design doc claims it DONE: phase4-next-tasks.md:247 row 6 'voice_sessions SQLite persistence + transcript sink | 7d87a9e'; phase4-w4-d4-design.md:282 specifies test 'transcript_persisted_to_chat_session' asserting the sessions table gets user+assistant rows for voice turns. The voice provider itself IS wired (OpenAIRealtimeProvider when an OpenAI key resolves, mod.py:369-398), so the feature is otherwise reachable — only persistence/transcript-bridge is dead.
- verify: Read build_voice_state_from_app (mod.py:399-429): no session_store/transcript_sink passed. grep -rn 'SqliteVoiceSessionStore' python/packages → none. grep -rn 'session_store=|transcript_sink=' python/packages/corlinman-server/src | grep -v test → no production assignment. Run a voice session against the live mount and query voice_sessions / sessions tables → no rows written for the voice turns.

### NEW-fhB-2 — High security (NEW, conf=high) — fh-undercovered-B
- loc: `python/packages/corlinman-agent-brain/src/corlinman_agent_brain/config.py:40-42 + risk_classifier.py:140-214 + runner.py:194-199`
- problem: Agent-brain memory curator NEVER redacts secrets and has NO functioning block path: the redact_api_keys/redact_emails/redact_phone_numbers config flags are unwired (read nowhere), classify_risk never returns BLOCKED, and HIGH-risk (secret-containing) candidates are written to the on-disk vault - auto-written outright under AUTO policy, or persisted as a draft/conflict file under semi_auto/draft_first.
- evidence: config.py:40-42 defines redact_emails/redact_phone_numbers/redact_api_keys (all default True) and auto_write_max_risk='low' (line 55). grep confirms NONE of these four fields are read anywhere in agent-brain/src outside config.py (extractor.py has no redaction). risk_classifier.classify_risk (lines 92-110) sets HIGH when _contains_sensitive_content matches API-key/secret/credit-card patterns, but only ever returns HIGH/MEDIUM/LOW - never BLOCKED (grep 'return RiskLevel' shows no BLOCKED). decide_write_action's 'block' branch (lines 157-163) is therefore unreachable dead code. Under WritePolicy.AUTO it returns action='auto_write' for ANY non-blocked risk (lines 174-179), and runner.py:194-196 calls self._vault.write_node -> VaultWriter._do_write writes the secret to disk. Under SEMI_AUTO/DRAFT_FIRST a HIGH-risk candidate becomes a 'draft'/'conflict' which runner.py:198-199/167 still writes to disk via write_draft/write_conflict. Contrast: sibling corlinman-user-model/distiller.py DOES redact (redact_text + _trait_value_has_pii); agent-brain is the inconsistent, unprotected one. Violates the project 'No secrets in git' invariant (knowledge/agent-brain vault on disk).
- verify: uv run pytest python/packages/corlinman-agent-brain -p no:cacheprovider -q; then classify_risk on a MemoryCandidate whose summary contains 'sk-live-...'/'ghp_...' -> RiskLevel.HIGH; decide_write_action(policy=AUTO) -> action='auto_write'; confirm VaultWriter writes the secret-bearing markdown to disk.

### NEW-reg-r5-voice-deploy-ui-2 — High security (NEW, conf=high) — reg-r5-voice-deploy-ui
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:1211-1240 (_extract_ws_token query fallback) + entrypoint.py:2607-2614 (uvicorn Config, access_log default on)`
- problem: R5-S1 added a `?api_key=`/`?token=`/`?access_token=` query-string fallback for the voice WS handshake auth (mod.py:1233-1240). The production entrypoint runs uvicorn with the default `uvicorn.Config` (entrypoint.py:2607) which does NOT disable access logging (access_log defaults True). uvicorn's WebSocket access logger logs `get_path_with_query_string(scope)` on every accepted connection — including the full query string. So whenever a browser client uses the query-param fallback (the documented reason it exists, since the WS API can't set headers), the tenant API key/token is written verbatim to the uvicorn access log at INFO on every connect, AND on every DENIED attempt (the 4401 path at mod.py:576 calls websocket.accept() before close, so it is logged too). This persists live credentials in logs/journald. Note: core/server.py:178 sets access_log=False, but the prod entrypoint factory does not — so the token leak is live in production.
- evidence: mod.py:1233-1240 honours `api_key`/`token`/`access_token` from websocket.query_params. uvicorn websockets_impl logs `get_path_with_query_string(self.scope)` on websocket.accept ('%s - "WebSocket %s" [accepted]') — and get_path_with_query_string (uvicorn/protocols/utils.py:52-56) appends `?<query_string>`. entrypoint.py:2607 `uvicorn.Config(app=app, host=host, port=port, log_level=..., loop='asyncio', lifespan='on')` — no access_log=False. The S1 docstring acknowledges this is borrowed from /logstream's `?token=` convention, but /logstream is a doc-only stub (gateway/ws/__init__.py), so the leak ships first via voice.
- verify: Boot the gateway via entrypoint, connect `ws://host/v1/voice?api_key=SECRET`, then inspect uvicorn/journald access log — the line contains `WebSocket /v1/voice?api_key=SECRET`. Or grep uvicorn utils.get_path_with_query_string usage in websockets_impl.

### R5-S1 — High security (RESOLVED, conf=high) — reg-r5-voice-deploy-ui
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:568-586,1200-1296; entrypoint.py:1077,2397-2407,2416`
- problem: Verified the voice WS handshake auth gate has NO ungated production hole and the tenant-binding breaks no legitimate flow. Production mounts voice only via entrypoint._mount_routes (:1077, router() with no state), and entrypoint.build_app ALWAYS calls install_api_key_middleware synchronously (:2397) BEFORE _mount_routes (:2416), publishing app.state.api_key_auth even with admin_db=None. _resolve_auth_state falls back to that instance; when admin_db is still None (pre-lifespan-rebind) _authenticate_ws returns (False,None) → fail-closed 4401. The only `(True,None)` skip path requires app.state.api_key_auth to be entirely absent, which only happens if the middleware install itself fails — that also disables the HTTP /v1 gate, a pre-existing whole-gateway degraded mode, not a voice-specific hole. Tenant binding is safe: ApiKeyRow has exactly one tenant_id (1:1 keys↔tenants), verify_api_key re-validates it via TenantId.new (admin_schema.py:643, slug regex [a-z][a-z0-9-]{0,62}), and the spoofable X-Tenant-Id is now ignored once a key is bound (mod.py:586). No multi-tenant-per-key flow exists to break.
- evidence: entrypoint.build_app order: install_api_key_middleware (2397) then _mount_routes (2416). build_voice_state_from_app (mod.py:399-429) leaves auth_state=None → falls back to app.state.api_key_auth. core/server.build_app does NOT mount voice, so its lack of the auth middleware is irrelevant. test_voice_auth_security.py: 10 passed.
- verify: uv run pytest python/packages/corlinman-server/tests/gateway/routes_voice/test_voice_auth_security.py -p no:cacheprovider -q

### R5-S2 — High security (RESOLVED, conf=high) — reg-r5-voice-deploy-ui
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/persistence.py:236-303; mod.py:691-695,606`
- problem: Verified _validate_path_segment rejects all traversal (.. / . / POSIX+Windows separators / NUL / absolute / drive / multi-part) without breaking legitimate tenant ids. Legitimate tenant_id comes from the verified key (mod.py:586, TenantId-shaped, slug regex [a-z][a-z0-9-]{0,62}) which always passes the validator (no separators/dots/edge-dot). session_id passed to audio_path_for is the server-generated `voice-{uuid4}` (mod.py:606), never the client's session_key, so it is always safe. The validator RAISES (not slugify) and is called un-try/caught at mod.py:691, but both inputs are guaranteed-safe so it never throws on a legitimate session. Traversal inputs that bypassed the (now-ignored) X-Tenant-Id header can no longer reach the path because tenant is bound to the verified key.
- evidence: persistence.py:248-261 rejects empties/./../seps/NUL and re-checks via PurePosixPath+PureWindowsPath. mod.py:692 calls audio_path_for(data_dir, tenant, session_id) where tenant=verified row.tenant_id and session_id=`voice-{uuid4()}`. admin_schema.py:643 verify_api_key re-validates tenant_id through TenantId.new. tenancy/id.py:53 slug regex. 10 voice-security tests pass.
- verify: uv run pytest python/packages/corlinman-server/tests/gateway/routes_voice/test_voice_auth_security.py -p no:cacheprovider -q

### NEW-fhA-1 — High bug (NEW, conf=high) — fh-undercovered-A
- loc: `python/packages/corlinman-wstool/src/corlinman_wstool/server.py:287-308 (reader loop) and :251-261 (handshake); root cause python/packages/corlinman-wstool/src/corlinman_wstool/protocol.py:249 (variant(**kwargs))`
- problem: WsToolMessage.from_dict() does variant(**kwargs) which raises TypeError (not ValueError) for a well-formed-JSON frame carrying an unexpected/missing field. Every reader site (server handshake line 251-252, server reader loop line 287-288, client reader loop client.py:250-251) only catches (ValueError, json.JSONDecodeError). A single malformed frame from a connected runner therefore raises TypeError that escapes the per-frame handler. In _connection_loop the `async for raw in ws` is wrapped only in `except ConnectionClosed`, so the TypeError propagates OUT of the loop and the cleanup block at lines 299-308 (conn.fail_pending(), state.deregister_runner(), task cancellation) is SKIPPED. Result: the runner stays registered in state.runners/tool_index forever, all in-flight invoke() waiters for that runner hang until their individual timeouts, and the writer/heartbeat tasks are leaked.
- evidence: Reproduced: `WsToolMessage.from_dict({"kind":"pong","extra":"x"})` -> `TypeError: _Pong.__init__() got an unexpected keyword argument 'extra'`; `from_dict({"kind":"result","request_id":"r1","ok":true})` (missing payload) -> `TypeError: _Result.__init__() missing 1 required positional argument: 'payload'`. server.py reader loop only catches `except (ValueError, json.JSONDecodeError)` at line 288 and the surrounding try at 281 only catches `except ConnectionClosed`; the cleanup at 299+ is not in a finally.
- verify: Add a test that sends a JSON text frame like {"kind":"pong","extra":1} after handshake and assert the server deregisters the runner / fails pending requests; observe instead an unhandled TypeError and the runner remaining in state.runners.

### NEW-fhB-1 — High bug (NEW, conf=high) — fh-undercovered-B
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/evolution/applier.py:344 (and corlinman-auto-rollback/src/corlinman_auto_rollback/applier.py:279) + monitor.py:175-186`
- problem: Auto-rollback never inspects or reverts any applied proposal because the apply path always writes an EMPTY metrics_baseline ({}), which the monitor cannot parse and therefore fail-safe-skips. The whole auto-rollback feature is dead in production.
- evidence: Both EvolutionApplier implementations build the audit row with metrics_baseline={} (server gateway applier.py:344, auto-rollback applier.py:279). capture_snapshot() is never called at apply time (grep: zero non-test callers outside monitor). At monitor time, monitor.py:176 does MetricSnapshot.from_dict(history.metrics_baseline); from_dict({}) raises (KeyError('target') wrapped as ValueError per metrics.py:78-83 - I reproduced this: empty dict -> KeyError('target')). monitor.py:179-186 catches ValueError, logs 'malformed metrics_baseline JSON; skipping', increments summary.errors, and continue's - so capture_snapshot/compute_delta/breaches_threshold/revert are never reached for any real applied proposal. MEMORY_OP is the only whitelisted kind (metrics.py:144), so rollback is effectively 100% non-functional. test_monitor.py only uses metrics_baseline={} for TAG_REBALANCE (skipped earlier by the empty whitelist); the breach-path tests use a hand-built _baseline_json, so no test exercises the real empty-baseline MEMORY_OP path. Gateway applier.py:340-343 comment admits 'wiring the AutoRollback baseline snapshot is a follow-up', yet cli.py:248-254 presents the feature as live.
- verify: uv run pytest python/packages/corlinman-auto-rollback -p no:cacheprovider -q; then apply a MEMORY_OP proposal via EvolutionApplier (metrics_baseline becomes {}) and run AutoRollbackMonitor.run_once() with fresh warn/error signals on the target - observe summary.errors increments and rollbacks_triggered stays 0 (revert never fires).

### NEW-fhconcurrency-1 — High bug (NEW, conf=medium) — fh-concurrency-data
- loc: `python/packages/corlinman-identity/src/corlinman_identity/verification.py:104-111 (_issue_phrase) and :307-314 (_sweep_expired_phrases); store opened with isolation_level=None at python/packages/corlinman-identity/src/corlinman_identity/store.py:146; tx_lock declared at store.py:104`
- problem: R5-B3 recurrence in SqliteIdentityStore. The store shares ONE aiosqlite.Connection across all concurrent async callers (store.py:79-84, single conn, isolation_level=None autocommit). The transactional methods resolve_or_create (resolver.py:291), merge_users (resolver.py:401) and redeem_phrase (verification.py:155) correctly serialise their explicit BEGIN..COMMIT blocks under store.tx_lock. But _issue_phrase (verification.py:104-111) and _sweep_expired_phrases (verification.py:307-314) issue a bare `await self.conn.execute(...)` + `await self.conn.commit()` WITHOUT taking tx_lock. Because commit() is connection-GLOBAL, a stray commit() from issue_phrase running concurrently with an in-flight resolve_or_create / redeem_phrase transaction ends (flushes) that open transaction mid-way. This breaks the all-or-nothing atomicity of _insert_new_user_and_alias (resolver.py:215-266): if the second INSERT then fails (e.g. UNIQUE conflict from a racing first-caller), the subsequent rollback (resolver.py:263) finds no live transaction, so the already-committed user_identities row survives ORPHANED with no user_aliases binding — the exact orphan the rollback was written to prevent. Identical class of defect to the R5-B3 journal bug. The store docstring's claim (store.py:102-103) that 'single-statement writes bypass the lock and rely on SQLite's internal serialisation' is the buggy reasoning: SQLite serialises the write itself, but the explicit commit() at the Python/aiosqlite layer still terminates any in-flight transaction on the shared connection.
- evidence: Repro (run inside corlinman-identity venv) on an isolation_level=None aiosqlite conn: after BEGIN + first INSERT, in_transaction=True; a stray commit() from a simulated concurrent issue_phrase flips in_transaction=False (transaction prematurely ended); the first INSERT's row then survives a subsequent rollback. Output: 'in_transaction after BEGIN+insert: True' -> 'in_transaction after stray commit(): False' -> surviving rows ['user_identity_row','phrase_row'] (the user_identity row that should have been rolled back persists). Same shared-conn + mixed-transaction-model mechanism the R5-B3 _write_lock fix addressed in agent_journal_backend.py:689.
- verify: Grep verification.py for `self.conn.commit()` not wrapped in `async with self.tx_lock`: lines 111 (_issue_phrase) and 314 (_sweep_expired_phrases) are bare. Contrast with resolver.py:291/401 and verification.py:155 which DO hold tx_lock. Fix: wrap the execute+commit in both methods in `async with self.tx_lock:` (matching the other write paths). Concurrent trigger: any inbound-message resolve_or_create / redeem_phrase racing an admin issue_phrase or a sweep cron on the same per-tenant store instance.

### NEW-fhperf-1 — High performance (NEW, conf=high) — fh-perf-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/observability/emitter.py:106-121 (JournalBackedEmitter.emit) → agent_journal_backend.py:1615-1647 (append_event)`
- problem: Every streamed event — including one TextDelta per token chunk — triggers a synchronous single-row sqlite INSERT+COMMIT on the live streaming hot path. JournalBackedEmitter.emit() (the production emitter wired into every ReasoningLoop) calls the per-row journal.append_event(), which does `execute(INSERT...); commit()` under a shared self._write_lock. The reasoning loop emits a TextDelta envelope per provider token chunk (reasoning_loop.py:1320-1326 → self._emit → emitter.emit). The codebase's OWN measurement (agent_journal_backend.py:1654-1655) states ~6ms per single-insert commit vs ~0.5ms/row batched. A 200-chunk streamed reply therefore pays ~200 commits (~1.2s cumulative commit time, order of magnitude more than batched) BEFORE each token is fanned out to the SSE consumer, and these commits serialize through self._write_lock against every other concurrent session's journal writes. The cost is paid unconditionally — emit() journals even when zero SSE subscribers are attached (the only early-out is `emitter is None`, never true in prod). append_events_batch() exists precisely to fix this but is not used on the live emit path.
- evidence: emitter.py:114-115 `try: await self._journal.append_event(envelope)`; backend append_event commits per row at 1636-1643 `await self._c.execute(INSERT OR IGNORE INTO turn_events ...); await self._c.commit()` inside `async with self._write_lock`; backend docstring 1652-1655: 'A single turn can emit hundreds of TextDelta events; ... ~6ms per single insert commit vs ~0.5ms per row inside one tx'. Loop emits per chunk: reasoning_loop.py:1319-1326 `open_text_cumulative += len(chunk.text); await self._emit(TextDelta(...))`. Prod wiring confirmed: entrypoint.py:1515 `observability_emitter = JournalBackedEmitter(observability_journal)` then 1551/1557 attached as servicer event_emitter.
- verify: Add a counting AgentJournal stub, run a ReasoningLoop over a mock provider streaming N token chunks with a JournalBackedEmitter wired, and assert append_event was called ~N times (one commit per token) rather than batched; or microbenchmark emit() of 200 TextDeltas against a real sqlite agent_journal and observe ~N*~6ms wall time dominated by commits.

### PERF-006 — High performance (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:1354-1365 (list_session_summaries)`
- problem: list_session_summaries (powers GET /admin/sessions) still computes message_count, last_user_text, and last_status via three correlated subqueries per grouped session row. R4-D6's turn_id DESC tie-breaker is present but the correlated-subquery cost was never removed; R5-B3 touched this file (write serialization) but did not change this query.
- evidence: At HEAD the SELECT inside the GROUP BY session_key block contains: (1) message_count = `(SELECT COUNT(*) FROM turn_messages tm WHERE tm.turn_id IN (SELECT turn_id FROM turns WHERE session_key = t.session_key))` (lines 1354-1357), a nested correlated subquery; (2) last_user_text = `(SELECT user_text FROM turns WHERE session_key = t.session_key ORDER BY started_at_ms DESC, turn_id DESC LIMIT 1)` (1358-1361); (3) last_status with the identical correlated pattern (1362-1365). Cost ~ O(S * (T + T*M)) without covering indexes; runs on every /admin/sessions render. The docstring at 1326-1331 even acknowledges 'a window-function form is equivalent ... but more verbose' — i.e. the correlated form is intentional and still in place.
- verify: Seed a journal with ~500 sessions x ~30 turns x ~10 messages, EXPLAIN QUERY PLAN the statement; compare against a single GROUP BY + ROW_NUMBER() window rewrite.

### NEW-fh-test-deep-1 — High test (NEW, conf=high) — fh-test-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/cost.py + budget.py (entire money/quota layer, ~671 LOC)`
- problem: The voice money/quota enforcement layer has ZERO dedicated unit tests. The session-start budget gate (evaluate_budget -> BudgetDecision/BudgetDenyReason: budget_is_zero, day_budget_exhausted), the mid-session meter (SessionMeter.poll: one-shot warn at cap-60s, hard kill at max_session_seconds, day-budget terminate), the per-session enforcer (BudgetEnforcer.tick/finalize delta-checkpoint that exists specifically to NOT leak unbilled minutes on crash, and to NOT double-bill already-checkpointed seconds), the UTC day-rollover helpers (utc_day_epoch / next_utc_midnight), and InMemoryVoiceSpend.add_seconds negative-clamp are all unexercised. A regression in any of these silently either (a) lets a tenant exceed their paid daily voice budget, (b) double-bills / under-bills minutes, or (c) fails to terminate a stuck billable provider session — direct money/quota impact.
- evidence: grep across python/packages/corlinman-server/tests/ for evaluate_budget|BudgetDecision|BudgetDenyReason|SessionMeter|BudgetEnforcer|day_budget_exhausted|budget_is_zero returns EMPTY (zero hits). utc_day_epoch/next_utc_midnight appear only in a docstring comment of test_voice_auth_security.py, never invoked. The only end-to-end test (tests/gateway/routes_voice/test_route_integration.py) merely sets budget_minutes_per_tenant_per_day=60 (line 117) and sleeps 0.05s (lines 146/159) — it never advances time.monotonic to a warn (cap-60s) or terminate (cap / max_session) boundary, so poll() returns OK on every path. cost.py:449 even documents 'Pulled out so tests can monkey-patch' for now_unix_secs, but no such test exists. SessionMeter.poll (cost.py:395-427) has 4 distinct branches; BudgetEnforcer._checkpoint_delta (budget.py:140-145) has the delta>0 guard — none asserted.
- verify: uv run pytest python/packages/corlinman-server -p no:cacheprovider -q -k voice  (confirms only auth/path tests run, nothing exercising the cost meter). grep -rn 'evaluate_budget\|SessionMeter\|BudgetEnforcer' python/packages/corlinman-server/tests/  -> no matches. Add unit tests: evaluate_budget at/over cap -> deny day_budget_exhausted with reset_at=next_utc_midnight; zero cap -> budget_is_zero; SessionMeter.poll fed synthetic monotonic now to assert OK->budget_warn(once)->terminate(4002) and independent max_session terminate(4001); BudgetEnforcer.tick checkpoints delta-only (last_checkpointed monotonic, never re-bills); finalize flushes residual.

### NEW-verifyqualtest-2 — High test (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:119-132 (token_config_to_acl)`
- problem: token_config_to_acl — the cross-tenant authorization boundary for MCP tokens (tools_allowlist/resources_allowed/prompts_allowed + tenant_id with DEFAULT_TENANT_ID fallback via TokenAcl.effective_tenant) — still has zero tests. A regression silently widens or mis-scopes tenant access on the MCP surface.
- evidence: server.py:119 `def token_config_to_acl(t: McpTokenConfig) -> TokenAcl`, docstring at :122 documents the DEFAULT_TENANT_ID fallback via TokenAcl.effective_tenant; DEFAULT_TENANT_ID is imported at :40. No test references token_config_to_acl anywhere (grep across all test trees empty). This is the same gap as TEST-006, isolated to the security-critical ACL function.
- verify: Add test: token_config_to_acl(McpTokenConfig(token='t', tenant_id=None)).effective_tenant == DEFAULT_TENANT_ID and with tenant_id='acme' it stays 'acme' (cross-tenant ACL not leaked).

### TEST-006 — High test (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/mcp/server.py:119,135,144,170; tests/gateway/mcp/ (empty)`
- problem: 209-line MCP server module with public funcs token_config_to_acl (:119), build_server_config (:135), build_dispatcher (:144), build_mcp_server (:170) still has zero tests. The tests/gateway/mcp/ directory exists but is EMPTY (0 files). token_config_to_acl is the security-relevant per-token MCP ACL / tenant-fallback translator.
- evidence: `ls tests/gateway/mcp/` → only . and .. (0 files). `grep -rln 'gateway.mcp.server|token_config_to_acl|build_mcp_server|build_server_config'` across all test trees → no matches. server.py is still 209 lines with the four public funcs at the cited lines.
- verify: Add tests/gateway/mcp/test_server.py asserting build_server_config maps tokens and token_config_to_acl preserves/derives tenant_id; currently no test imports the module.

### TEST-008 — High test (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-server/src/corlinman_server/home_channel_store.py:1-335`
- problem: 335-line /sethome SQLite store (set_home/get_home/list_all_homes/mark_tip_shown/resolve_user_id, idempotent PK on user_id) is actively used in production by entrypoint.py, chat_bootstrap.py, and channels/commands.py, yet still has zero tests.
- evidence: `grep -rln 'home_channel_store|HomeChannelStore|set_home|sethome'` across all test trees → no matches. Live consumers confirmed at HEAD: gateway/lifecycle/entrypoint.py, gateway/services/chat_bootstrap.py, corlinman-channels/commands.py all import it; module is 335 lines.
- verify: Add tests/test_home_channel_store.py: set_home then get_home returns the row; re-set_home with a different channel for same user_id overwrites (PK on user_id); resolve_user_id channel+sender mapping; mark_tip_shown.

### TEST-009 — High test (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/middleware/admin_session.py:59-145 (create/validate/gc/start_gc)`
- problem: AdminSessionStore TTL expiry (validate, elapsed>ttl evict), sliding-refresh (bump last_used on validate), gc() eviction, and the background sweep start_gc (tick ttl/4 min 60s) still have NO direct unit test. Every test reference instantiates the store only as an auth fixture with a long TTL — none exercises expiry/gc/sliding.
- evidence: All ~20 test references at HEAD pass a long TTL purely as a fixture: `session_store=AdminSessionStore(86_400)` / `AdminSessionStore(ttl_seconds=3600)` (e.g. _admin_auth.py:30, test_sessions.py:89, test_username.py:59). grep for `.gc()` / `start_gc` / short-TTL `ttl_seconds=0` calls in tests → none. No test imports the module to assert expiry/sliding. Lifecycle logic lives at admin_session.py:59-145 with docstring (16-24) promising ttl-since-last_used expiry + gc every ttl/4.
- verify: Add test: store=AdminSessionStore(ttl_seconds=0.01); tok=store.create('u'); after sleep validate(tok) is None and len(store)==0; validate within ttl bumps last_used (sliding).

### NEW-reg-r5-voice-deploy-ui-4 — Medium regression (NEW, conf=medium) — reg-r5-voice-deploy-ui
- loc: `deploy/install.sh:871-919 (upgrade_native) vs :761-774 (fresh-install chown to SERVICE_USER)`
- problem: The `--upgrade` native path does not maintain the ownership invariant the fresh install establishes. upgrade_native (1) never calls `ensure_service_user`, (2) runs `uv sync --frozen` (:893) and `build_and_place_ui` (:900, which does `sudo rsync -a --delete` writing $PREFIX/ui-static as root, :265) WITHOUT re-chowning DATA_DIR/ui-static/.venv back to `corlinman`, then (3) restarts the now-`User=corlinman` service (:905). The freshly rewritten `.venv` is root-owned; combined with finding #1's reachability problems, an upgrade can leave the gateway unable to start as corlinman (root-owned venv files with a restrictive root umask, plus the same uv_path/HOME issues). ui-static becoming root-owned is usually still world-readable so lower impact, but the upgrade flow silently drifts the ownership model the fresh install set up.
- evidence: upgrade_native (:871) calls require uv/git, fetch+reset (:883-888), `uv sync --all-packages --frozen --no-dev` (:893), build_and_place_ui (:900), then `systemctl restart corlinman.service` (:905). No `ensure_service_user`, no `chown -R corlinman:corlinman $DATA_DIR`/ui-static after the rewrites. Contrast install_native:768-774 which does both for fresh installs.
- verify: On an R5-fresh install, run `install.sh --upgrade`; afterward `ls -l $PREFIX/repo/.venv/bin` and `$PREFIX/ui-static` show root:root ownership, and the corlinman-user service may fail to start depending on venv file perms.

### NEW-completeness-backend-4 — Medium completeness (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/mod.py:425-429 (build_voice_state_from_app), :249 (VoiceState.session_store default); persistence.py:18,170`
- problem: Voice session start/end rows are still NEVER persisted in production. The gateway mount path build_voice_state_from_app constructs VoiceState without session_store (defaults to None), so every record_start/record_end is silently skipped. No SQLite-backed VoiceSessionStore implementation exists. R5-S1/S2 touched mod.py/persistence.py (auth + path sanitization) but did not wire a session_store.
- evidence: At HEAD build_voice_state_from_app returns `VoiceState(config_loader=lambda: cfg_model, provider=provider, data_dir=...)` (mod.py:425-429) with no session_store arg. VoiceState.session_store defaults to None (mod.py:249). Persistence call sites guard `if state.session_store is not None` (mod.py:697, 822, 1537) so record_start/record_end (699,710,824,837) are skipped. Only MemoryVoiceSessionStore exists (persistence.py:170, 'for tests'); persistence.py:18 says the SQLite swap 'mirrors the Rust SqliteVoiceSessionStore and is left as a stub'. No `class .*VoiceSessionStore` SQLite impl in non-test src.
- verify: Open a /v1/voice WebSocket against a booted gateway, end it, query the voice_sessions table — stays empty. Acceptance: build_voice_state_from_app wires a persistent session_store and the table holds one row per session.

### NEW-completeness-backend-5 — Medium completeness (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-providers/src/corlinman_providers/declarative.py:236-240 (_build_inner), :267 (_VALID_AUTH_KINDS)`
- problem: A custom declarative provider configured with auth_kind='header' (e.g. X-API-Key) or auth_kind='query_param' still silently sends the wrong auth. load_spec_from_toml accepts these auth_kinds as valid and parses header_name/param_name, but _build_inner ignores them and always builds an OpenAIProvider that forwards the key as Authorization: Bearer → the provider 401s.
- evidence: At HEAD declarative.py:236-240: for request_format=='openai_compatible' returns `cast(CorlinmanProvider, OpenAIProvider(api_key=api_key, base_url=spec.base_url))` regardless of auth_kind. The docstring at 222-227 explicitly admits: 'TODO: header / query_param auth kinds currently ignore the declared header/param names — OpenAIProvider forwards the key as Authorization: Bearer'. _VALID_AUTH_KINDS at line 267 still lists 'header' and 'query_param' as legal. Nothing reads header_name/param_name at request time.
- verify: Create a declarative provider TOML with auth_kind='header', auth_config={header_name='X-API-Key', env_var='MY_KEY'}, base_url at a server requiring X-API-Key; a chat call sends Authorization: Bearer instead and is rejected. Acceptance: outbound request carries X-API-Key and no Bearer header.

### NEW-fhfunc-3 — Medium completeness (NEW, conf=high) — fh-func-deep
- loc: `python/packages/corlinman-auto-rollback/src/corlinman_auto_rollback/cli.py:287 (only AutoRollbackMonitor instantiation) ; gateway lifespan python/packages/corlinman-server/src/corlinman_server/gateway/lifecycle/entrypoint.py (no monitor, no auto_rollback builtin)`
- problem: Automatic rollback (auto-revert of an applied evolution proposal when post-apply SLO metrics regress) has no runtime driver in the gateway. AutoRollbackMonitor is instantiated ONLY inside the standalone corlinman-auto-rollback CLI (cli.py:287, run_once at 297), which requires an injected --applier module:callable and is never auto-invoked. There is no 'auto_rollback' / 'evolution.rollback' scheduler builtin in scheduler/builtins/, and the gateway lifespan never constructs or runs AutoRollbackMonitor. The only working rollback is the manual operator button (/admin/evolution/{id}/rollback → EvolutionApplier.revert). The release plan promised auto-rollback as a cron job.
- evidence: grep 'AutoRollbackMonitor(' python/packages → single hit cli.py:287. grep AutoRollback/auto_rollback in entrypoint.py → empty. No file matching auto*rollback under scheduler/builtins/. multi-agent-release-plan.md:181-184: 'AutoRollback: corlinman-auto-rollback is also built; we wire it as a cron 0 */6 * * * that checks SLOs and reverts the last applied mutation if any tracked SLO regresses by >10%.' — that cron job does not exist. Note this is downstream of NEW-fhfunc-2: since apply mutates nothing, an auto-revert would also revert nothing, but the missing scheduler wiring is an independent gap.
- verify: grep -rn 'AutoRollbackMonitor' python/packages/corlinman-server/src | grep -v test → no instantiation. ls python/packages/corlinman-server/src/corlinman_server/scheduler/builtins/ → no auto_rollback module. Inspect lifespan in entrypoint.py around scheduler spawn (2188-2228): only the configured jobs are spawned; none is an auto-rollback monitor.

### NEW-fhfunc-5 — Medium completeness (NEW, conf=high) — fh-func-deep
- loc: `python/packages/corlinman-goals (entire package) ; not imported by python/packages/corlinman-server/src ; no scheduler builtin in scheduler/builtins/`
- problem: The goals subsystem (hierarchical goals + weekly reflection -> goal.weekly_failed evolution signal) has no runtime driver in the gateway and no admin/agent wiring. corlinman_goals is imported NOWHERE in corlinman-server. There is no goals.* / reflection scheduler builtin, so the weekly reflection job (which is supposed to grade self-progress and emit a goal.weekly_failed evolution signal feeding the evolution engine) never runs in production. Combined with NEW-fhfunc-1 ({{goals.*}} placeholder also unregistered), the goal subsystem influences neither prompts nor the evolution loop at runtime — it only runs via the standalone corlinman-goals CLI.
- evidence: grep -rln 'corlinman_goals' python/packages/corlinman-server/src (non-test) → empty. No goals/reflect builtin under scheduler/builtins/ (only evolution_darwin_curate, qzone_daily, system_update_check). evolution_signal.py exists in the goals package (emits evolution_signals row) but nothing in the server invokes run_reflect_once. Docs mark it accepted: phase4-roadmap.md:301 '4-4B Goal hierarchies ... reflection job grades self-progress; goals influence prompt construction via {{goals.*}} placeholders'; phase4-next-tasks.md:218-220 (cascade + goal.weekly_failed emission + Wave4 acceptance, commits 300d64d/d012ad4/70a0518).
- verify: grep -rln 'corlinman_goals' python/packages/corlinman-server/src | grep -v test → empty. grep -rln 'goals\|reflect' python/packages/corlinman-server/src/corlinman_server/scheduler/builtins → empty. Confirms no gateway-side firing of the reflection job.

### NEW-fhfunc-6 — Medium completeness (NEW, conf=high) — fh-func-deep
- loc: `python/packages/corlinman-identity (resolver) ; only used by routes_admin_a/identity.py (admin CRUD) ; cli/main.py:223 stub ; corlinman-channels/common.py only imports the UserId type`
- problem: Cross-channel identity unification at message ingest is not wired, despite being marked '✅ full stack' with gateway-middleware integration. UserIdentityResolver / SqliteIdentityStore resolve+link+verify logic is complete and exposed through an admin REST surface (routes_admin_a/identity.py), but the core promise — resolving (channel, sender_id) -> canonical user_id on inbound messages and attributing it to the agent — has no caller. inbox.py and agent_servicer.py do zero identity resolution; corlinman_channels/common.py imports only the UserId TYPE (line 29), never the resolver; the CLI 'identity resolve' command is an explicit todo_stub (cli/main.py:230-232).
- evidence: grep -rn 'corlinman_identity' python/packages/corlinman-server/src (non-test, excl home_channel comment) → only routes_admin_a/identity.py and cli/main.py. inbox.py / agent_servicer.py grep for identity → empty. cli/main.py:223 group help: 'Channel-alias resolver inspect (corlinman_identity). STUB — not yet ported.' Docs claim done: phase4-next-tasks.md:32 'B2 Cross-channel identity (4-2B) | ✅ full stack', :91 lists 'iters 5-8 integration (gateway middleware + admin REST + admin UI + HookEvent attribution)' as complete. The middleware/ingest integration (iter 5) and HookEvent attribution are not present.
- verify: grep -rn 'identity\|canonical_user\|UserIdentityResolver' python/packages/corlinman-server/src/corlinman_server/inbox.py python/packages/corlinman-server/src/corlinman_server/agent_servicer.py → empty. grep -rn 'corlinman_identity' python/packages/corlinman-channels/src → only `from corlinman_identity import UserId` (type alias). Send the same human from two channels and check whether their messages resolve to one user_id at ingest → they do not.

### NEW-fresh-bug-server-2 — Medium completeness (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/auto_resume.py:76 (_CHANNEL_NEEDS_BOOT_REPLAY) + 121-166 (BootReplayDispatcher.replay); drain side: corlinman-channels/service.py:80,672`
- problem: auto-resume synthesizes a 'pending' inbox row for telegram/discord/slack/feishu and counts it as resumed (resumed+=1, logs agent.resume.replay_enqueued), but NO dispatch loop drains those channels' inbox, so the resume row is orphaned forever and the scan reports a false-positive success.
- evidence: auto_resume.py:76 `_CHANNEL_NEEDS_BOOT_REPLAY = frozenset({"telegram", "discord", "slack", "feishu"})`; replay() (121-166) enqueues a `resume:{turn_id}` row and returns True. In corlinman-channels/service.py the ONLY inbox drain is `inbox.list_pending(channel="qq", limit=20)` (line 80) feeding `_qq_dispatch_loop` (line 672); the second loop (handle_one_qq_official @3614) is also QQ-only. grep for list_pending / channel="telegram"|"discord"|"slack"|"feishu" returns no non-QQ drain. No _telegram/_discord/_slack/_feishu dispatch loop exists.
- verify: Journal an in_progress turn channel='telegram', run run_boot_auto_resume with a real Inbox; report.resumed==1 but no telegram path drains the row. The 'resumed' count is a false positive.

### NEW-fresh-quality-7 — Medium completeness (CONFIRMED, conf=high) — fh-undercovered-B
- loc: `python/packages/corlinman-evolution-engine/src/corlinman_evolution_engine/{agent_card.py:13-14,110; prompt_template.py:11-12,107; tool_policy.py:12-13,154; gepa.py:32-33; engine.py:89-91,149} vs corlinman-shadow-tester (no caller)`
- problem: evolution-engine docstrings repeatedly and falsely claim the ShadowTester 'already routes high-risk kinds through the shadow stage', but ShadowRunner is never imported or invoked outside tests - shadow-tester is fully unwired in production. (Confirms the R5 NEW-fresh-quality-7 lead.)
- evidence: agent_card.py:13-14 and prompt_template.py:11-12 state 'The Phase 3 W1-A ShadowTester already routes high-risk kinds through the shadow stage'; tool_policy.py:12-13 similar; engine.py:89-91 comment 'high-risk kinds gated by the docker shadow sandbox ... The ShadowTester decides whether they reach the operator queue'. But grep shows zero 'import ... shadow_tester' / 'from corlinman_shadow_tester' anywhere outside the shadow-tester package, ShadowRunner( appears ONLY in shadow-tester's own tests (test_runner.py, test_integration_real_fixtures.py), and there is no cli.py/__main__/server wiring for shadow-tester (no entry in any other pyproject.toml). engine.py's own top docstring (line 13) even admits 'No shadow testing.' So proposals flow pending -> (operator/auto-approval) without ever passing through the claimed shadow gate. Not currently tracked in audit/ARCH_DEBT.md (grep -i shadow returns nothing).
- verify: grep -rn 'corlinman_shadow_tester' python/ --include=*.py | grep -v /corlinman-shadow-tester/ -> empty; grep -rn 'ShadowRunner(' python/ -> only tests.

### NEW-fh-sec-deep-1 — Medium security (NEW, conf=medium) — fh-sec-deep
- loc: `python/packages/corlinman-mcp-server/src/corlinman_mcp_server/resources.py:219-348 (list_resources / read_resource); SessionContext.tenant_id in adapters.py:38; build_dispatcher in gateway/mcp/server.py:144-167`
- problem: MCP per-token cross-tenant memory scoping is documented but NOT enforced — a latent cross-tenant IDOR. The gateway docstring (mcp/server.py:26-30, build_dispatcher:150-156) and the SessionContext docstring (adapters.py:38-40) both assert that cross-tenant memory scoping 'is enforced by the SessionContext.tenant_id the adapter consults'. It is not: ResourcesAdapter.list_resources iterates ALL self._memory_hosts (resources.py:229) and read_resource resolves a host purely from the URI <host> segment (self._memory_hosts.get(parsed.host), resources.py:325) — neither path ever reads ctx.tenant_id. TokenAcl.effective_tenant()/DEFAULT_TENANT_ID is computed and stamped onto the SessionContext (auth.py:64-80) but the value is dead. build_dispatcher passes the same memory_hosts dict to the single shared dispatcher for every token, so any token whose resources_allowed includes 'memory' (or '*') can list+read EVERY tenant's memory host by name, regardless of its own tenant_id. The DEFAULT_TENANT_ID fallback question is moot because tenant filtering is absent entirely.
- evidence: Grep across corlinman_mcp_server adapters (resources.py/tools.py/prompts.py/dispatch.py) finds ZERO reads of ctx.tenant_id / SessionContext.tenant_id — the only references are the dataclass field decl + the auth-layer that sets it. resources.py:325 keys memory reads on the attacker-supplied URI host; resources.py:229 enumerates the whole host map. This is gated behind C4-MCP-SERVER (server not bound today, ARCH_DEBT.md:34-37), so it is not currently reachable — but the bind/mount plan in ARCH_DEBT does NOT mention the missing scoping, so the implementer who lands the /mcp route per that plan will ship an unenforced cross-tenant ACL while the code comments claim it is enforced.
- verify: Construct ResourcesAdapter({'tenant_a': hostA, 'tenant_b': hostB}, skills); call read_resource(ResourcesReadParams(uri='corlinman://memory/tenant_b/<id>'), ctx=SessionContext(resources_allowed=['memory'], tenant_id='tenant_a')) and observe it returns tenant_b's content — tenant_id is never checked.

### NEW-fhA-2 — Medium security (NEW, conf=high) — fh-undercovered-A
- loc: `python/packages/corlinman-wstool/src/corlinman_wstool/file_fetcher.py:192-206 (_fetch_http)`
- problem: _fetch_http enforces the max_bytes cap only AFTER the full response body has been downloaded and materialized: `resp = await self.http_client.get(uri)` followed by `body = resp.content` (which buffers the entire body) and only then `if len(body) > self.max_bytes: raise SizeLimit`. With DEFAULT_MAX_BYTES = 100 MiB (and ws-tool callers passing this as the runner-side cap too), a malicious or compromised HTTP origin can stream an arbitrarily large response and the gateway holds it all in RAM before rejecting it — an unbounded-memory DoS. The module docstring claims http fetches are 'subject to the max_bytes cap', implying a streaming/early cap that is not implemented.
- evidence: Reproduced with httpx.MockTransport returning 10_000 bytes against max_bytes=100: 'SizeLimit raised AFTER full buffering: size limit exceeded: 10000 > 100' — the SizeLimit fires only after the entire body was buffered into resp.content. No Content-Length pre-check and no streamed download with a running-total cap.
- verify: Stream a response larger than max_bytes via client.stream() and abort once the running total exceeds max_bytes, instead of get()+resp.content; confirm peak memory stays bounded.

### NEW-fresh-sec-gateway-2 — Medium security (RESOLVED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_voice/persistence.py:244-304 (audio_path_for @275, tts_audio_path_for @292)`
- problem: Previously: audio_path_for/tts_audio_path_for interpolated client-controlled tenant_id verbatim into a filesystem path, allowing path-traversal-on-write when audio retention is enabled. Now FIXED (R5-S2): both helpers validate every segment.
- evidence: New VoicePathError + _validate_path_segment (persistence.py:244) reject empty/whitespace, '.'/'..', any path separator (os.sep/os.altsep/'/'/'\\'/NUL), and absolute/drive-anchored/multi-part values. audio_path_for (L287-288) and tts_audio_path_for (L302-303) now call `_validate_path_segment` on tenant_id and session_id before building the Path, raising VoicePathError on traversal. The verbatim-interpolation line cited in recon (persistence.py:242) no longer exists.
- verify: Call audio_path_for(Path('/data'), '../../etc', 'sid') -> now raises VoicePathError instead of returning an escaped path. Matches the assignment note that this was bundled into and closed by R5-S2.

### NEW-reg-r5-voice-deploy-ui-3 — Medium security (NEW, conf=medium) — reg-r5-voice-deploy-ui
- loc: `deploy/install.sh:165-174 (secure_root_executed_scripts) vs deploy/corlinman-upgrader.sh:225-231`
- problem: R5-S3's `secure_root_executed_scripts` re-locks only two root-executed files (`corlinman-upgrader.sh`, `install.sh`) back to root:root after the recursive `chown -R $(id -u):$(id -g) $PREFIX`. But the root-running upgrader ALSO executes `${INSTALL_PREFIX}/repo/.venv/bin/python` (corlinman-upgrader.sh:225/231, for the no-downgrade semver check) and re-reads the whole `$PREFIX/repo` tree via `bash $INSTALL_SH`. The recursive chown hands `.venv/bin/python` (and all of repo/) to the unprivileged install user; secure_root_executed_scripts does NOT re-lock the venv interpreter. In the non-root-sudo install flow that S5-S3 explicitly targets ('the unprivileged install user', e.g. the `ubuntu` user per install.sh:197), that user can replace `.venv/bin/python` with a malicious binary, which the root upgrader then executes on the next one-click upgrade → root code-exec. This is the exact LPE class S3 claims to close, left incompletely fixed. (Lower severity than a fresh LPE because the documented default flow is root-install, where chown root:root is a no-op.)
- evidence: install.sh:165-174 loops over only `$PREFIX/repo/deploy/corlinman-upgrader.sh` and `$PREFIX/repo/deploy/install.sh`. corlinman-upgrader.sh:225 `PYTHON_BIN="${INSTALL_PREFIX}/repo/.venv/bin/python"`, :231 `CURRENT_VERSION=$("$PYTHON_BIN" - <<'PYEOF' ...` — root executes it. The upgrader unit is `User=root` (install.sh:827). The recursive chown at install.sh:530/735 covers all of $PREFIX including .venv.
- verify: As the unprivileged install user (non-root sudo flow), confirm write access to `$PREFIX/repo/.venv/bin/python` after install (it is install-user-owned), then trace that corlinman-upgrader.sh:231 invokes it as root on upgrade. No re-chown of .venv exists in install.sh.

### SEC-008 — Medium security (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_b/providers.py:615-626 (helper _query_provider_models @551, reached via _query_provider_models_with_retry @491 and the GET /admin/providers/{name}/models route)`
- problem: Admin-configured provider base_url is dialed with no SSRF guard. _query_provider_models builds the probe URL from operator-supplied base_url and issues a GET with the configured API key in an Authorization: Bearer header to an unvalidated host (no scheme/IP/host check like the agent web-fetch is_safe_host).
- evidence: L615 `raw_base = entry_dict.get("base_url") or "https://api.openai.com"`; L618 `url = base_url.rstrip("/") + "/v1/models"`; L620-621 sets `Authorization: Bearer {api_key}`; L625-626 `async with _httpx.AsyncClient(timeout=10.0) as client: resp = await client.get(url, headers=headers)`. grep of providers.py for safe_host/ipaddress/getaddrinfo/169.254/private returns ZERO hits — no SSRF policy. Route reachable: providers.py:15 documents GET /admin/providers/{name}/models -> _query_provider_models_with_retry(491) -> _query_provider_models(551).
- verify: Configure a provider with base_url=http://169.254.169.254/latest/meta-data (or http://127.0.0.1:<port>), call GET /admin/providers/<name>/models; server dials the internal address (latency/HTTP leaks back) and the API key is sent as a bearer to the attacker host. Add an is_safe_host-style guard rejecting loopback/private/metadata before client.get.

### SEC-012 — Medium security (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-agent/src/corlinman_agent/web/_common.py:322-361 (is_safe_host DNS path) + web/fetch.py:160-170,206-207`
- problem: DNS-rebind TOCTOU in the web-fetch SSRF guard. is_safe_host resolves+classifies the host's IPs but does NOT pin them; client.stream('GET', current_url) then dials by hostname, triggering an independent second DNS resolution. A rebinding attacker can answer the guard's lookup with a public IP and the connect's lookup with an internal IP, bypassing the guard. Same gap on every redirect hop.
- evidence: _common.py L326 `infos = socket.getaddrinfo(host, None)` — validates each resolved IP but the function returns None (no pinning, no resolver override, no connect-by-IP). fetch.py L160 `is_safe_host(url)` then L166-170 `async with make_client(...follow_redirects=False) as client: ... async with client.stream("GET", current_url) as response:` re-resolves the hostname inside httpx. Redirect re-check at fetch.py L207 `is_safe_host(next_url)` has the identical re-dial gap. The validated IP is never used to pin the connection.
- verify: Stand up a DNS name whose first A answer is public and second is 127.0.0.1/169.254.169.254 with low TTL; call web_fetch and confirm the dial reaches the internal IP despite is_safe_host passing. Fix: resolve once and pin (custom transport/resolver, or connect to the validated IP with an explicit Host header).

### BUG-006 — Medium bug (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-providers/src/corlinman_providers/openai_provider.py:222-240`
- problem: Streamed OpenAI tool-call whose first delta arrives without an id is opened with a synthetic f'call_{idx}' id AND a hardcoded finalized=True flag. The late-id promotion branch is therefore dead, so a real server-issued tool_call_id arriving in a later delta is silently dropped and the synthetic id is what gets sent back to the model, breaking tool-result correlation for servers that issue ids late.
- evidence: L223-224 `call_id = tc_id or f"call_{idx}"; open_calls[idx] = (call_id, True)` — flag is True even when tc_id was None. L230 `elif tc_id and not open_calls[idx][1]:` — open_calls[idx][1] is always True, so `not True` == False ⇒ branch unreachable. L240 `call_id = open_calls[idx][0]` keeps the synthetic id. /tmp/repro_bug006.py reproduces: first delta id=None -> 'call_0' opened; later delta id='call_real_123' emits nothing and final id stays 'call_0'. Fix: store flag = bool(tc_id) at open time. Line numbers unchanged from recon.
- verify: Drive chat_stream with a fake stream whose first tool_calls delta has id=None and a later delta supplies id='call_real_123'; assert emitted start/delta/end use 'call_real_123' not 'call_0' (currently fails).

### BUG-010 — Medium bug (CHANGED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/agent_journal_backend.py:828-858 (recon cited 812-840; code drifted ~16 lines, bug identical)`
- problem: begin_turn derives turn_id from wall-clock ms and retries on PK collision for offset in range(0,20). On 20 same-ms collisions it falls through to `return ts` — returning ts+0, the exact id that already collided on the first iteration, WITHOUT inserting any row. Caller gets a turn_id mapping to a different pre-existing turn's row; subsequent complete_turn/error_turn/message-append mutate the wrong turn (cross-turn corruption) and the new turn has no row.
- evidence: L828 `ts = int(time.time() * 1000)`; L830 `for offset in range(0, 20):`; L831 `tid = ts + offset`; L849-850 success commit+return tid; L851 `except aiosqlite.IntegrityError: ... continue`; L857-858 `# Vanishingly unlikely; fall through with a tagged turn_id.` / `return ts`. Since offset 0 (=ts) already raised IntegrityError, returning ts hands back a known-colliding id. /tmp/repro_bug010.py confirms: with ts..ts+19 pre-seeded, begin_turn returns 1000 (=ts), an existing foreign turn's id, with no new row. Status CHANGED only because the recon line range (812-840) drifted to 828-858; the defect is byte-identical.
- verify: Pre-seed turns for turn_id ts..ts+19, mock time to ts, call begin_turn; assert it raises/returns None instead of returning ts. Fix: raise (or widen the window) instead of `return ts` on exhaustion.

### NEW-fresh-ui-3 — Medium bug (CONFIRMED, conf=medium) — reverify-r5-sec-bug
- loc: `ui/components/sessions/cost-footer.tsx:34-41 and ui/components/sessions/session-cost-cells.tsx:37-44`
- problem: Two inline cost fetchers omit the GATEWAY_BASE_URL prefix, so the sessions cost column (every list row) and the session-detail cost footer silently fail (cells -> '—') whenever NEXT_PUBLIC_GATEWAY_URL is set (UI on a different origin than the gateway).
- evidence: Both files define _loadCostInline as `fetch(\`/admin/sessions/${encodeURIComponent(key)}/cost\`, {credentials:'include'})` with a RELATIVE path (cost-footer.tsx:35-37, session-cost-cells.tsx:38-40). Proof of intended pattern: ui/lib/api.ts:17 exports `GATEWAY_BASE_URL = process.env.NEXT_PUBLIC_GATEWAY_URL ?? ''` and apiFetch (api.ts:46) plus every streamX EventSource (api.ts:670,679,2699) prefix `${GATEWAY_BASE_URL}`. With the var set, the relative fetch hits the UI origin -> 404, and the catch branch renders '—'.
- verify: Set NEXT_PUBLIC_GATEWAY_URL to a non-empty origin distinct from the UI host, open /admin/sessions; cost/avg/last-tool cells show '—' (network tab shows the request hitting the UI origin). Fix: prefix `${GATEWAY_BASE_URL}` in both _loadCostInline functions.

### NEW-fresh-ui-4 — Medium bug (CONFIRMED, conf=medium) — reverify-r5-sec-bug
- loc: `ui/lib/api.ts:2528-2537 (streamUpgradeEvents)`
- problem: streamUpgradeEvents builds its EventSource URL without the GATEWAY_BASE_URL prefix, so the system-upgrade live-progress SSE fails to connect when NEXT_PUBLIC_GATEWAY_URL is set; the operator watches a stalled progress panel.
- evidence: L2535 `const url = \`/admin/system/upgrade/${encodeURIComponent(request_id)}/events${suffix}\`;` then L2536 `new EventSource(url, ...)` — no `${GATEWAY_BASE_URL}`. Sibling factories DO prefix it: streamSubagentEvents (api.ts:670), streamSubagentsOverview (679), streamHubInstallEvents (2699). Consumed by components/system/upgrade-progress.tsx. Line range matches recon.
- verify: With NEXT_PUBLIC_GATEWAY_URL set to the gateway origin (distinct from UI), trigger a system upgrade and open the progress panel; SSE 'status' frames never arrive. Fix: `const url = \`${GATEWAY_BASE_URL}/admin/system/upgrade/...\``.

### NEW-fresh-ui-5 — Medium bug (CONFIRMED, conf=medium) — reverify-r5-sec-bug
- loc: `ui/lib/chat/event-merger.ts:9-16 (docstring) + 329-345 (EventDedupSet); ui/lib/chat/use-chat-stream.ts:99-100 (applyEvent text-delta) + 274-287 (live onEvent)`
- problem: Assistant text can be double-rendered. The merger docstring promises dedup between the token stream and journal TextDelta events ('mark it as already-rendered to avoid double-emit') but no such mechanism exists, so if /events/live emits TextDelta during the same turn the token stream is rendering, draft.content gets each fragment appended twice.
- evidence: event-merger.ts docstring L11-14 promises the mark-already-rendered dedup. EventDedupSet.shouldEmit (L334-339) keys only on `${turnId}:${sequence}` and L335 `if (sequence < 0) return true` lets ALL synthetic token-stream deltas (sequence -1, set at L39/47/64/74/90/96) through; journal TextDelta carries a real sequence (L194-196) and is emitted once. There is NO content-based dedup linking the two. Both feed applyEvent's `case 'text-delta': draft.content += ev.text` (use-chat-stream.ts:99-100). The chat e2e only avoids doubling because its live-SSE stub omits TextDelta. Confidence medium: depends on the gateway emitting TextDelta on the live channel concurrently with the OpenAI-compat token stream.
- verify: Unit test: drive useChatStream so BOTH a token chunk (delta.content:'Hi') and a journal TextDelta(text:'Hi', sequence>=0, same turn_id) reach reduceEvent; assert pendingMessage.content === 'Hi' (today it doubles to 'HiHi').

### NEW-providers-3 — Medium bug (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-providers/src/corlinman_providers/google_provider.py:96-102`
- problem: GoogleProvider serialises message content with f-string interpolation of the raw Python object. When a turn carries OpenAI content-parts (list form, produced by reasoning_loop._inject_attachments for any image/file attachment), f"{role}: {content}" embeds the Python repr of the list into the Gemini prompt. The image is never sent as a real image part, so every vision/attachment request via Gemini is garbled; tool-result rounds are similarly flattened ('tool: ...').
- evidence: L97-101 `for m in messages: role = _get(m,'role') or 'user'; content = _get(m,'content') or ''; if content: prompt_parts.append(f"{role}: {content}")`. A user content-parts list (e.g. [{'type':'text',...},{'type':'image_url','image_url':{'url':...}}]) is repr'd verbatim into the flat prompt. The TODO comment at L94-95 admits 'Multi-turn + roles are in the TODO above', confirming the text-only flattening. Reachable via the channel image path (_inject_attachments). Line range matches recon (96-102).
- verify: Call GoogleProvider.chat_stream with a user message whose content is a content-parts list (text + image_url); assert the SDK receives a structured multimodal contents payload (image part) rather than a flat prompt string containing the list repr.

### NEW-fhperf-2 — Medium performance (NEW, conf=high) — fh-perf-deep
- loc: `python/packages/corlinman-memory-host/src/corlinman_memory_host/local_sqlite.py:601-644 (recent) + 260-270 (filter_chunk_ids_by_namespace)`
- problem: Per-chat-turn conversational recall scans and materializes the ENTIRE namespace's chunk-id list just to take the last `limit` (default 8). _recall_memory() runs on every chat turn (agent_servicer.py:1114 await self._recall_memory) and calls host.recent(session_key, 8). recent() calls filter_chunk_ids_by_namespace([namespace]) → `SELECT id FROM chunks WHERE namespace IN (?) ORDER BY id ASC` (no LIMIT), pulls every id for the session into Python, then slices `ids[-limit:]`. Since every completed turn also upserts a row (_store_memory → upsert), a long-running session namespace grows without bound and recall cost grows O(N) in stored turns per recall — all rows fetched/transferred from sqlite each turn even though only 8 are kept. The same all-ids query also runs under the store's shared asyncio.Lock for writes (insert paths) so it adds connection contention. A `WHERE namespace=? ORDER BY id DESC LIMIT ?` served directly by the existing idx_chunks_namespace(namespace,id) index would be O(limit).
- evidence: recent(): line 614 `ids = await self._store.filter_chunk_ids_by_namespace([namespace])` then 622 `recent_ids = list(reversed(ids[-limit:]))`. filter SQL line 264-266: `SELECT id FROM chunks WHERE namespace IN ({placeholders}) ORDER BY id ASC` (returns ALL ids). Called every turn: agent_servicer.py:2396 `hits = await recent_fn(start.session_key, 8)` from _recall_memory invoked at 1114. Index exists: schema line 88 `CREATE INDEX ... idx_chunks_namespace ON chunks(namespace, id)`.
- verify: Upsert e.g. 5000 docs into one namespace via LocalSqliteHost, time recent(ns, 8); compare against an equivalent `SELECT id FROM chunks WHERE namespace=? ORDER BY id DESC LIMIT 8`. Observe recent() row count fetched == namespace size, growing linearly, vs constant 8.

### NEW-fresh-perf-2 — Medium performance (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-server/src/corlinman_server/agent_servicer.py:1915-1924; corlinman-agent coding/search.py:125,153,179`
- problem: Builtin coding tools (read/write/edit/list/search file) still execute synchronous, potentially heavy filesystem work directly on the gRPC servicer's async event loop, blocking all concurrent chat streams for the duration. dispatch_search_files rglobs the workspace and read_text()s up to 5000 files synchronously on the loop.
- evidence: In `async def _dispatch_builtin`, agent_servicer.py:1916-1924 call dispatch_read_file/write/edit/list/search_file with NO await and NO asyncio.to_thread (plain sync functions), e.g. line 1924 `return dispatch_search_files(args_json=event.args_json)`. By contrast the sibling I/O tools ARE offloaded/awaited: line 1926 `await dispatch_run_shell(...)` (async subprocess), 1909/1911 `await dispatch_web_fetch/web_search(...)`. dispatch_search_files is a plain `def` (search.py:125), iterates `scope.rglob(pattern)` (search.py:153) bounded by `_MAX_FILES_SCANNED=5000` (search.py:38) and does `path.read_text(...)` per file (search.py:179) — all synchronous disk I/O. The codebase already knows the fix: corlinman-wstool/file_fetcher.py:419 `return await asyncio.to_thread(_do)`.
- verify: Start two concurrent chat streams; have one invoke search_files over a workspace with thousands of files; assert the second stream's token latency does not spike during the scan. Or unit: monkeypatch dispatch_search_files to sleep(1) sync and assert a concurrent coroutine is blocked.

### PERF-008 — Medium performance (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-episodes/src/corlinman_episodes/store.py:388 (insert_episode); caller python/packages/corlinman-episodes/src/corlinman_episodes/runner.py:349`
- problem: Episode persistence still does one commit (one fsync round-trip) per row. insert_episode ends with a per-row commit, and the distillation runner calls it one-at-a-time inside its per-bundle loop. No batch/executemany API exists.
- evidence: store.py:361-387 is a single-row INSERT INTO episodes followed by `await self.conn.commit()` at line 388. runner.py:349 calls `await store.insert_episode(episode)` then `written += 1` inside the per-bundle loop (one transaction/commit per bundle). grep confirms no executemany/batch insert path. Impact bounded — store docstring (354-359) notes rowcount stays low (a few thousand/tenant/year), background distillation path, hence Medium.
- verify: Insert 1000 episodes via insert_episode vs one transaction wrapping 1000 inserts; compare wall time (each commit is an fsync on default SQLite journal mode).

### PERF-010 — Medium performance (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `ui/lib/chat/use-chat-stream.ts:238-249 (reduceEvent)`
- problem: reduceEvent still rebuilds the entire pendingMessage object — spreading prev and shallow-copying toolCalls, subagents (incl. nested events arrays), approvals, and usage — on every single SSE/journal event, including pure token deltas that touch only content. R5-B2 edited this file (edit-and-rerun) but left reduceEvent's per-event clone untouched.
- evidence: At HEAD lines 240-246: `const next = { ...prev, toolCalls: prev.toolCalls ? prev.toolCalls.map((t)=>({...t})) : undefined, subagents: prev.subagents ? prev.subagents.map((s)=>({...s, events: s.events ? [...s.events] : []})) : undefined, approvals: prev.approvals ? prev.approvals.map((a)=>({...a})) : undefined, usage: prev.usage ? {...prev.usage} : undefined }` then `applyEvent(next, ev)`. Every event allocates fresh copies of all sub-arrays regardless of which field is mutated; a text-token delta still clones every tool-call and every subagent event array → O(toolCalls + Σ subagent.events) allocations per delta.
- verify: Mock a turn with 20 toolCalls + a subagent carrying 200 events; stream 500 token deltas through reduceEvent and count allocations vs a structural-sharing reducer.

### PERF-012 — Medium performance (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `ui/components/chat/chat-model-picker.tsx:93-100`
- problem: Opening the model picker still fans out one upstream model-probe HTTP request per enabled provider in parallel via useQueries, hitting every configured provider's /models endpoint at once. R5-C2 only wired the open handler (chat/page.tsx); chat-model-picker.tsx was not touched in the R5 range.
- evidence: At HEAD lines 93-100: `const probeQueries = useQueries({ queries: enabledProviders.map((p) => ({ queryKey:['chat-picker','provider-models',p.name], queryFn:()=>getProviderModels(p.name), staleTime:120_000, enabled:open }))})`. enabledProviders (84-91) = all enabled providers (image-capable subset for kind==='image'). With N enabled providers, every picker open schedules N concurrent getProviderModels calls; first open (and any open after 120s staleness) fans out N live upstream probes. `git diff 0b8befa..HEAD -- ui/components/chat/chat-model-picker.tsx` is empty.
- verify: Configure 8 enabled providers, open the picker, assert 8 concurrent getProviderModels network calls fire; consider lazy/on-hover probing or a server-side aggregate endpoint.

### NEW-fh-test-deep-2 — Medium test (NEW, conf=high) — fh-test-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/services/direct_backend.py:274-342 (_translate stream tool-call assembly)`
- problem: The direct backend's streaming tool-call assembler is explicitly written to support MULTIPLE concurrent/parallel tool calls (open_calls is a dict keyed by tool_call_id with call_order list) and to flush still-open calls when a 'done' chunk arrives WITHOUT a preceding tool_call_end (lines 332-342). Both paths are untested: every test uses only a single tool_call_id ('c1') always terminated by an explicit tool_call_end. OpenAI/Anthropic routinely emit parallel tool calls and some providers end the stream without a per-call end marker; a regression that mis-keys interleaved deltas (cross-contaminating arg JSON between c1/c2) or drops the done-flush would silently corrupt or lose tool calls in the most common agentic path, with no test catching it.
- evidence: grep -oE 'tool_call_id="[^"]*"' tests/gateway/services/test_direct_backend.py yields only 'c1' (single id) across all 4 tool tests. No test interleaves tool_call_delta for two distinct ids, and no test sends 'done' while a call is still open (every test emits tool_call_end before done). The R5-P1 perf test (test_many_arg_deltas, test_arg_assembler_is_linear_not_quadratic) only stresses one call's fragment count, not parallelism. Impl branches at direct_backend.py:296 (new vs already-open), :313-316 (delta arriving before start), and :335-341 (done-flush of open_calls) are uncovered.
- verify: grep -c 'tool_call_id="c2"' python/packages/corlinman-server/tests/gateway/services/test_direct_backend.py -> 0. Add a test feeding interleaved start/delta for c1 and c2 (deltas arriving out of order) and assert two ToolCall frames with byte-correct, non-cross-contaminated args_json; add a test that ends the stream with 'done' while c1 is still open (no tool_call_end) and assert the flush emits the call.

### NEW-fh-test-deep-3 — Medium test (NEW, conf=medium) — fh-test-deep
- loc: `python/packages/corlinman-providers/src/corlinman_providers/anthropic_provider.py:504-537 (_tool_calls_to_anthropic_blocks) + bedrock_provider.py equivalent`
- problem: The R5-B1 fix translates assistant tool_calls into Anthropic tool_use blocks via a loop over the tool_calls list, so it is designed to emit N parallel tool_use blocks in one assistant turn. All B1 regression tests pass exactly ONE tool_call. A multi-tool assistant turn (two tool_use blocks) plus the two matching role=tool results -> two tool_result blocks (tool_use_id correlation) is never asserted. Since Anthropic rejects a turn where tool_use ids and tool_result tool_use_ids don't line up, a regression in ordering/correlation would break every parallel-tool-call turn after the first — the exact class of bug B1 was filed to fix — with no test catching the multi-call case.
- evidence: In tests/test_anthropic_provider.py the B1 tests (test_split_system_translates_tool_calls_to_anthropic_blocks:292, test_split_system_tool_call_with_text_keeps_both_blocks:338, test_split_system_tool_call_malformed_arguments_falls_back_to_empty_input:363, test_chat_stream_tool_round_sends_anthropic_tool_blocks:387) each assert a single block list like [{type:tool_use, id:call_1,...}]. grep for two distinct call ids (call_1 + call_2 in one turn) or len(tool_use)==2 returns nothing. _tool_calls_to_anthropic_blocks (line 517 'for tc in tool_calls') and the tool_result emission (anthropic_provider.py:476) are only ever hit with a 1-element list.
- verify: Add a test passing an assistant message with tool_calls=[call_1, call_2] followed by two role=tool messages (tool_call_id call_1, call_2); assert the assistant content has two tool_use blocks in order and the user turn carries two tool_result blocks with matching tool_use_id values. Repeat the same shape against bedrock_provider._split_system.

### NEW-fresh-test-3 — Medium test (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/api_keys.py:240-266 (revoke_key) + tenancy/admin_schema.py:600 (revoke_api_key)`
- problem: DELETE /admin/api_keys/{key_id} is untested at the route level; its cross-tenant behavior (revoke is keyed on key_id ALONE, the resolved tenant is discarded) is unasserted. If multi-tenant is enabled, an admin scoped to tenant A can revoke tenant B's key by id, and no test would flag it.
- evidence: revoke_key (api_keys.py:249) `_ = _resolve_tenant(state, tenant)` discards the tenant, then L251 `revoked = await db.revoke_api_key(key_id)`. The DB method (admin_schema.py:600) runs `UPDATE tenant_api_keys SET revoked_at_ms = ? WHERE key_id = ? AND revoked_at_ms IS NULL` — NO tenant_id predicate. grep of corlinman-server/tests for mint_key/revoke_key/api_keys router finds ONLY test_must_change_blocks_admin_routes.py, which mounts the router solely to assert the must_change_password 403 gate — it never asserts mint/list/revoke or cross-tenant scoping.
- verify: Add tests/gateway/routes_admin_a/test_api_keys.py covering: mint+list tenant isolation (?tenant=acme vs ?tenant=other), blank scope -> 400, unknown key_id -> 404, idempotent double-revoke -> 404, and a cross-tenant guard (mint under acme, DELETE ?tenant=other) pinning the currently-silent global-revoke behavior.

### QUAL-007 — Medium quality (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/corlinman-agent/src/corlinman_agent/session_query.py:1-237 (re-exported by corlinman_agent/__init__.py:29,42)`
- problem: 237-line SessionQueryClient/SessionMessage/SessionQueryError module is dead on HEAD. It reads the Rust gateway's <data_dir>/sessions.sqlite `sessions` table (per its docstring), which the active Rust→Python migration is deleting; the Python plane now persists sessions via agent_journal_backend. No production code consumes it.
- evidence: grep for `SessionQueryClient` / `session_query` across all non-test src (excluding the module itself and its re-export in corlinman_agent/__init__.py) returns ZERO hits. The only references are the module file (237 lines), its docstring, and the __init__.py re-export at lines 29 and 42. SQL at the module SELECTs `FROM sessions` (the going-away Rust table).
- verify: `grep -rn 'SessionQueryClient(' python/packages/*/src` returns nothing → no runtime consumer; deleting session_query.py + its __init__ re-export should leave pytest green except its own removed test.

### QUAL-010 — Medium quality (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `python/packages/ — 29 `def _now_ms` src definitions (+3 `def now_ms`)`
- problem: Still 29 separate `def _now_ms()` definitions in production src (32 counting the 3 `def now_ms` variants), each effectively `return int(time.time() * 1000)`. No shared now_ms helper exists anywhere.
- evidence: `grep -rln 'def _now_ms' python/packages | grep -v /tests/ | grep -v /.venv/ | wc -l` → 29. Adding `def now_ms` (auto_rollback/monitor.py:67, gateway/evolution/observer.py:412, gateway/evolution/applier.py:488) → 32 total. No common timeutil/now_ms util module — the 3 `now_ms` defs are themselves duplicates, not a shared import.
- verify: `grep -rln 'def _now_ms' python/packages | grep -v /tests | grep -v /.venv` lists 29 files; consolidating into one helper removes 28+ copies.

### QUAL-011 — Medium quality (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `corlinman-agent/.../session_query.py:214,232; corlinman-identity/.../resolver.py:143,148,160; corlinman-replay/.../session_store.py:159,173; corlinman-server/.../routes_admin_a/identity.py:124 (+agents.py:135); corlinman-user-model/.../cli.py:280`
- problem: 5+ packages still each reimplement an RFC3339 formatter and/or parser (several ship both format+parse halves) with the same Z<->+00:00 normalization. No shared RFC3339 util.
- evidence: `grep -rn 'def .*rfc3339' python/packages | grep -v /tests` at HEAD: session_query.py:214 `_rfc3339` + :232 `_parse_rfc3339`; resolver.py:143 `_now_utc_rfc3339`, :148 `_to_rfc3339`, :160 `_parse_rfc3339`; session_store.py:159 `_format_rfc3339`, :173 `_parse_rfc3339`; routes_admin_a/identity.py:124 `_to_rfc3339_z`; agents.py:135 `_system_time_to_rfc3339`; user_model/cli.py:280 `_rfc3339_to_ms`. All hand-roll `.replace('Z','+00:00')` / `.isoformat().replace('+00:00','Z')`.
- verify: `grep -rn 'def .*rfc3339' python/packages/*/src` enumerates the duplicate format+parse pairs.

### QUAL-013 — Medium quality (CONFIRMED, conf=high) — reverify-r5-perf-completeness
- loc: `corlinman-identity/.../verification.py:45; corlinman-server/.../routes_admin_b/credentials.py:42 + providers.py:44; routes_admin_a/personas.py:350; gateway/grpc/agent_server.py:289`
- problem: Multiple sites still cross-import leading-underscore private symbols across module/package boundaries, violating the underscore convention systematically.
- evidence: At HEAD: verification.py:45 `from corlinman_identity.resolver import _now_utc_rfc3339, _parse_rfc3339, _to_rfc3339`; credentials.py:42 & providers.py:44 `from corlinman_server.gateway.routes_admin_b.onboard import _write_config_atomic`; personas.py:350 `from corlinman_server.persona.store import _now_ms  # type: ignore[attr-defined]`; agent_server.py:289 `from corlinman_server.main import _run_boot_auto_resume`. (Plus QUAL-012's journal._load_messages dotted private access, separately tracked.)
- verify: `grep -rn 'import _' python/packages/*/src | grep 'from corlinman'` lists the cross-package private imports.

### NEW-reg-r5-providers-2 — Low regression (NEW, conf=medium) — reg-r5-providers
- loc: `python/packages/corlinman-providers/src/corlinman_providers/anthropic_provider.py:484-492 and bedrock_provider.py:448-455; _tool_calls_to_anthropic_blocks anthropic_provider.py:514-537 / bedrock_provider.py:472-495`
- problem: An assistant turn whose tool_calls is truthy but yields ZERO tool_use blocks produces {"role":"assistant","content":[]} (an empty content array). This happens when tool_calls is a list of all non-dict entries (each skipped by 'if not isinstance(tc, dict): continue'), or when content is empty AND every tool_call is filtered out. Anthropic rejects empty content arrays — the file itself acknowledges this at anthropic_provider.py:596-599 ('Anthropic rejects empty content arrays') for the _parts_to_anthropic_blocks path, but the new tool_calls branch has no equivalent guard. Reachability is low because the main loop's _extend_with_tool_round (reasoning_loop.py:1578) always builds well-formed dict tool_calls, but DB/session-restored or malformed tool_calls_json (session_query.py:196 keeps only dicts, but a list of zero dicts is possible) could trip it.
- evidence: /tmp/repro_b1.py 'ZERO-BLOCK' case: input assistant content="" with tool_calls=["garbage", 123] -> output {"role":"assistant","content":[]}. _tool_calls_to_anthropic_blocks returns [] (both entries skipped as non-dict), content="" yields no text block, so blocks stays []. Same for both providers. Contrast: the pre-existing _parts_to_anthropic_blocks guards against this exact condition (lines 596-599) but the R5 tool_calls branch does not.
- verify: uv run python /tmp/repro_b1.py (see 'ZERO-BLOCK anthropic'/'ZERO-BLOCK bedrock': content == []). Fix: after blocks.extend(...), if not blocks, append a fallback text block (mirroring lines 596-599) or skip the turn.

### NEW-reg-r5-providers-3 — Low regression (NEW, conf=low) — reg-r5-providers
- loc: `python/packages/corlinman-providers/src/corlinman_providers/anthropic_provider.py:476,532 and bedrock_provider.py:440,490 (the 'or ""' fallbacks for tool_use id / tool_use_id)`
- problem: A tool_call with missing/None id is silently coerced to id="" (tc.get('id') or ""), and a role="tool" message with no tool_call_id becomes tool_use_id="". Anthropic requires tool_use.id and tool_result.tool_use_id to be non-empty and to MATCH. Two such blocks would both carry "" and 'match' only by accident; more importantly an empty id is invalid to Anthropic and would 400. Low reachability: the live loop always supplies real call_ids (reasoning_loop.py:1580 c.call_id, 1598 r.call_id), so this only bites on malformed/externally-injected histories — but the new code degrades silently instead of being explicit.
- evidence: /tmp/repro_b1.py 'NO-ID anthropic' case: assistant tool_call lacking 'id' -> {"type":"tool_use","id":"",...}; role="tool" lacking tool_call_id -> {"type":"tool_result","tool_use_id":"","content":"r"}. Both empty strings.
- verify: uv run python /tmp/repro_b1.py (see 'NO-ID anthropic'). This is a robustness/edge gap, not a live-path break — flagging low.

### NEW-reg-r5-providers-4 — Low regression (NEW, conf=low) — reg-r5-providers
- loc: `python/packages/corlinman-providers/src/corlinman_providers/anthropic_provider.py:484-492 (assistant tool_calls branch uses _content_to_text on list content)`
- problem: When an assistant turn has BOTH list content containing an image_url part AND tool_calls, the image is silently dropped. Pre-R5, assistant list content went through _parts_to_anthropic_blocks (line 495-497 path) which preserves image_url -> Anthropic image blocks. The new tool_calls branch instead flattens content via _content_to_text (line 487), which only extracts text parts (anthropic_provider.py:548-555 ignores image_url), so images are lost on a tool-calling assistant turn. NOT reachable in the current code paths: the live loop sets assistant tool_calls content="" (reasoning_loop.py:1577) and session restore stores content as a plain string column (session_query.py:202), so assistant+tool_calls turns never carry list/image content today. Flagging as latent/low.
- evidence: /tmp/repro_b1.py 'IMG+TOOLCALL anthropic' case: assistant content=[text 'see image', image_url https://x/y.png] with a tool_call -> output blocks = [{text 'see image'}, {tool_use ...}] — the image_url block is gone. The bedrock branch is even narrower (only keeps content when isinstance(content,str), bedrock_provider.py:451), so any list content there is dropped entirely.
- verify: uv run python /tmp/repro_b1.py (see 'IMG+TOOLCALL anthropic'). Confirmed not exercised by live loop (reasoning_loop.py:1577 content="") — hence low confidence/low severity, latent only.

### NEW-reg-r5-server-1 — Low regression (NEW, conf=medium) — reg-r5-server
- loc: `python/packages/corlinman-server/src/corlinman_server/system/subagent/store.py:287-296 (update->_prune_terminal_locked, set_killed->_prune_terminal_locked)`
- problem: The just-completed terminal row CAN be evicted by its own prune call under same-millisecond churn, despite the inline comment claiming 'the just-updated row has the freshest finished_at so it is never the one evicted'. The comment's invariant is only true when finished_at values are distinct.
- evidence: Prune sorts terminal rows by key=(finished_at or 0, rid) and evicts the lowest-sorting `len-cap` entries (lines 287-294). The dispatcher stamps finished_at=_now_ms() (millisecond resolution) on every terminal update (dispatcher.py:407,457). Under high spawn churn many rows complete within the same millisecond, so they tie on finished_at and fall back to the rid tiebreak. If the just-completed row's request_id sorts low among same-ms peers, it is evicted even though it is the newest by wall-clock — contradicting the comment at store.py:361-363. Impact is limited: update()/set_killed snapshot the row BEFORE prune and return that snapshot regardless of eviction (lines 364-367, 477-480), so the caller's return value is always correct; the only effect is that the freshly-finished row may be absent from the retained operator-history window (cap=512) a tick early. No KeyError, no in-flight loss, no count corruption. Cosmetic precision of the retention window only.
- verify: Inspect store.py:287-296 sort key vs the 'never evicted' claim at 361-363; construct >512 terminal rows sharing one _now_ms() value with the target rid lexicographically smallest and observe it pruned despite being newest. Snapshot return remains correct in all cases.

### R5-B2 — Low regression (RESOLVED, conf=high) — reg-r5-voice-deploy-ui
- loc: `ui/lib/chat/use-chat-stream.ts:251-322 (runTurn baseHistory), :423-441 (retryLast), :407-421 (sendMessage), :452-477 (editAndRerun)`
- problem: Verified the edit-rerun baseHistory threading does NOT regress retryLast or sendMessage. runTurn gained an optional `baseHistory?: ChatMessage[]` arg; the history loop reads `baseHistory ?? messages` (:317). retryLast (:440) and sendMessage (:418) call runTurn WITHOUT baseHistory, so they fall back to the closure `messages` exactly as before — behavior unchanged. runTurn's dep array lists `messages` (:404) so the closure is fresh per render. editAndRerun now computes truncatedHistory synchronously from `messages` and passes it explicitly; dep array correctly `[messages, runTurn]` (:477). The signature change is backward-compatible — all external consumers (chat-area.tsx:86/93/213) use the unchanged public sendMessage/retryLast/editAndRerun API; runTurn is internal with no other callers. No dangling refs.
- evidence: vitest `lib/chat/__tests__/use-chat-stream-edit-rerun.test.ts` passes (1 test). grep confirms only chat-area.tsx consumes these via chat.* public API; no direct runTurn callers outside the hook.
- verify: pnpm -C ui exec vitest run lib/chat/__tests__/use-chat-stream-edit-rerun.test.ts

### R5-C2 — Low regression (RESOLVED, conf=high) — reg-r5-voice-deploy-ui
- loc: `ui/app/(admin)/chat/page.tsx:308,318-324; ui/components/chat/chat-area.tsx:51,242; ui/components/chat/composer.tsx:61,124,385`
- problem: Verified the model-picker wiring passes the correct pickerOpen value and forwards cleanly. `onOpenModelPicker={() => setPickerOpen('llm')}` (page.tsx:308) sets a valid `ModelPickerKind` ('llm'|'image', chat-model-picker.tsx:34). The picker opens via `open={pickerOpen !== null}` (:319) with `kind={pickerOpen ?? 'llm'}` (:321). The composer model pill (composer.tsx:385 onClick) and the /model slash command (composer.tsx:124) both fire onOpenModelPicker, which ChatArea (:51,242) and Composer (:61) forward correctly. The image picker is opened via a separate path, unaffected. No regression.
- evidence: vitest `app/(admin)/chat/page.test.tsx` passes (3 tests). Type-checked: setPickerOpen('llm') matches React.useState<ModelPickerKind|null>.
- verify: pnpm -C ui exec vitest run 'app/(admin)/chat/page.test.tsx'

### R5-C3 — Low regression (RESOLVED, conf=high) — reg-r5-voice-deploy-ui
- loc: `ui/app/(admin)/nodes/page.tsx (rewritten); ui/components/nodes/offline-block.tsx:66-103; ui/lib/mocks/nodes.ts`
- problem: Verified the nodes page renders without the removed query and has no dangling imports/refs. The page now imports only React/motion/useTranslation/useMotion/NotImplementedBlock — all used (the React import remains but the project's own `next lint` passes with no warnings, so it is not flagged). The removed useQuery/GlassPanel/TopologyGraph/fetchRunnersMock etc. are no longer referenced in the page; the `nodes.tp.heroTitle` i18n key exists (lib/locales/en.ts:1522). NotImplementedBlock is a clean static panel. mocks/nodes.ts retains the Runner type + summariseRunners (still used by @/components/nodes/*) so no broken type refs. The 5s poll is correctly removed.
- evidence: vitest `app/(admin)/nodes/page.test.tsx` passes (2 tests). `next lint --file app/(admin)/nodes/page.tsx` → 'No ESLint warnings or errors'. grep confirms heroTitle key present and no residual references to removed symbols in page.tsx.
- verify: pnpm -C ui exec vitest run 'app/(admin)/nodes/page.test.tsx' && pnpm -C ui exec next lint --file 'app/(admin)/nodes/page.tsx'

### NEW-fhfunc-7 — Low completeness (NEW, conf=medium) — fh-func-deep
- loc: `python/packages/corlinman-nodebridge/src/corlinman_nodebridge/server.py (NodeBridgeServer never instantiated) ; config_watcher.py:63,495 (nodebridge listed restart-required but never parsed/acted on)`
- problem: The NodeBridge WebSocket server is built but never bound, and the [nodebridge] config section is in the restart-required list but is parsed/acted-upon nowhere. NodeBridgeServer has no production caller (grep across the server finds no NodeBridgeServer( and no nodebridge config read; the only reference is config_watcher's restart-required-sections set). So ws://host/nodebridge/connect never exists at runtime. This is the same class as the known/spec'd 'MCP /mcp server not bound' gap. Severity is Low because docs are honest (architecture.md:350 calls it a 'stub crate ... no native device client'), but the [nodebridge] config key is effectively dead.
- evidence: grep -rn 'NodeBridgeServer(' python/packages | grep -v test → none. grep -rn 'nodebridge' python/packages/corlinman-server/src | grep -v 'test|config_watcher' → empty (no config parsing, no entrypoint import). config_watcher.py:63 RESTART-required set includes 'nodebridge'; :495 lists it again — but nothing constructs the server when that section changes. architecture.md:348-352 documents 'NodeBridge v1 — WebSocket + JSON at config.nodebridge.listen ... Implemented by the stub crate corlinman-nodebridge'.
- verify: grep -rn 'NodeBridgeServer\|nodebridge/connect\|NodeBridgeConfig' python/packages/corlinman-server/src | grep -v test → empty. Set [nodebridge] listen in config, boot the gateway, dial ws://host/nodebridge/connect → connection refused (route never mounted).

### NEW-fh-sec-deep-2 — Low security (NEW, conf=medium) — fh-sec-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes/canvas.py:223-248 (_create_session), :340-418 (_post_frame / _stream_events); registered at canvas.py:495-508 as /v1/canvas/* and /canvas/*`
- problem: Canvas sessions carry NO tenant binding, so the SSE event stream and frame-post endpoints are a cross-tenant IDOR by session_id. _create_session stores _Session keyed only by session_id in the process-global state._sessions dict and never records the requesting tenant (no request.state.tenant read). _post_frame (canvas.py:257) and _stream_events (canvas.py:382, session = state._sessions.get(id)) look the session up purely by id. The ApiKeyAuthMiddleware authenticates the caller and binds a tenant to the key, but the canvas handlers ignore that tenant entirely — any authenticated tenant who obtains another tenant's canvas session_id (e.g. via a leaked URL, referrer header, or log line) can subscribe to its live operator output or inject frames. Real-world impact is bounded because session_id = 'cs_' + secrets.token_urlsafe(24) (canvas.py:79, ~192 bits, guarded by test_canvas_session_id_entropy.py) so blind guessing is infeasible, and canvas is opt-in (mounted only when state.canvas is not None, register.py:109).
- evidence: canvas.py:235-240 stores the session with no tenant field; canvas.py:382-391 and :257 resolve by id only; no occurrence of request.state.tenant / tenant_id anywhere in canvas.py. The R2-001 fix added the /canvas/ prefix to the api-key middleware (authentication) but did not add per-tenant authorization on the in-memory session map.
- verify: With two valid tenant API keys, tenant A POSTs /v1/canvas/session (gets cs_<id>); tenant B then GETs /v1/canvas/session/cs_<id>/events with B's key and successfully attaches to A's stream — the only barrier is knowing the high-entropy id.

### NEW-fhA-3 — Low security (NEW, conf=medium) — fh-undercovered-A
- loc: `python/packages/corlinman-wstool/src/corlinman_wstool/server.py:196 (_process_request) + python/packages/corlinman-wstool/src/corlinman_wstool/types.py:162 (WsToolConfig.auth_token default)`
- problem: WsToolConfig.auth_token defaults to the empty string. The pre-upgrade auth check is `if token != cfg.auth_token: return 401`. A client that omits the auth_token query param gets `params.get('auth_token','') == ''`; if the server was constructed with the default empty token, the comparison `'' != ''` is False so the connection is accepted with NO authentication. Additionally the check is a plain non-constant-time string equality (timing side channel). There is no guard rejecting an empty configured auth_token at construction time.
- evidence: types.py:162 `auth_token: str = ""`; server.py:193-196 `token = params.get("auth_token", "")` then `if token != cfg.auth_token:`. With cfg.auth_token=='' any tokenless dial passes. WsToolServer is not currently instantiated anywhere in python/packages outside its own package/tests, so this is latent until the gateway wires it.
- verify: Construct WsToolServer with WsToolConfig() (default empty token), bind, then dial /wstool/connect with no auth_token query param and confirm the upgrade succeeds (no 401).

### NEW-fhA-4 — Low security (NEW, conf=medium) — fh-undercovered-A
- loc: `python/packages/corlinman-nodebridge/src/corlinman_nodebridge/server.py:396-455 (_connection_loop) and :118-144 (NodeBridgeServerConfig); protocol.py:103 (Register.auth_token)`
- problem: The Register frame carries an auth_token field (protocol.py:103) and the client sends it (client.py:114), but the server never reads or validates it. NodeBridgeServerConfig has no auth_token field at all, and _connection_loop only enforces the path and the signature/accept_unsigned gate (which defaults to accept_unsigned=True). Any client that reaches /nodebridge/connect can register a node, advertise capabilities, and receive dispatched jobs (whose params may be sensitive) with no token authentication.
- evidence: grep over the nodebridge src shows auth_token only in protocol.py (field), client.py (sent) — never referenced in server.py. _connection_loop builds and registers the session purely from register.node_id/node_type/capabilities with no token check. NOTE: the Rust crate (rust/crates/corlinman-nodebridge/src) likewise has zero auth_token references, so this is a faithful port of a pre-existing Rust design gap, not a port regression; and the /nodes runner endpoint that would bind this server is a known-deferred item.
- verify: Open a NodeBridgeServer, dial /nodebridge/connect, send Register with an arbitrary/garbage auth_token and signature=None (accept_unsigned default True) and confirm a Registered ack is returned.

### NEW-fhB-6 — Low security (NEW, conf=medium) — fh-undercovered-B
- loc: `python/packages/corlinman-agent-brain/src/corlinman_agent_brain/vault_writer.py:90-91 (_resolve_vault_path)`
- problem: node.frontmatter.agent_id is used as an unsanitized path component when building the vault directory, so an agent_id containing '../' or an absolute path can write KnowledgeNode markdown outside vault_root (path traversal). Titles ARE sanitized via _safe_filename, but agent_id is not.
- evidence: vault_writer.py:90-91: agent_id = node.frontmatter.agent_id or 'unknown-agent'; return vault_root / 'agents' / agent_id / kind_folder. _safe_filename (lines 50-74) is applied only to the title->filename, never to agent_id. agent_id originates from SessionBundle.agent_id (models.py:105) / candidate.agent_id (runner.py:295), tracing back to session metadata; if any upstream lets the value carry '..' or a leading '/', pathlib '/' with an absolute string resets to that absolute path and escapes the vault. No validation of agent_id anywhere in agent-brain.
- verify: Construct a KnowledgeNode with frontmatter.agent_id='../../tmp/evil' and call VaultWriter.write_node(dry_run=False); observe target path resolves outside vault_root. Confirm grep shows no sanitization of agent_id before the path join.

### NEW-fresh-sec-channels-deploy-6 — Low security (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-channels/src/corlinman_channels/commands.py:867-882`
- problem: is_command_admin() is default-open: when CORLINMAN_COMMAND_ADMINS is unset/blank it returns True for every caller, so any inbound channel user is treated as a command admin. Gates nothing today (no built-in CommandSpec sets admin_only=True) but is a fail-open authorization footgun for any deployment that registers an admin_only runtime command.
- evidence: commands.py:877-879 `raw = os.environ.get(_COMMAND_ADMINS_ENV, "").strip(); if not raw: return True`. Enforcement keys solely on `spec.admin_only and not ctx.is_admin` and is_admin flows from is_command_admin(req.binding). Line range matches recon (867-882).
- verify: Register CommandSpec(admin_only=True) at runtime with CORLINMAN_COMMAND_ADMINS unset and send as a non-listed user — it executes. Decide whether to default-deny when no allowlist is configured.

### SEC-009 — Low security (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:150-157 (emitted at :272)`
- problem: Admin session cookie is set without the Secure flag, so over a plaintext-HTTP hop the session token can be sniffed. Documented design choice (TLS terminates upstream) but still a weakened cookie.
- evidence: L150-157 `_set_cookie_header` returns `f"{SESSION_COOKIE_NAME}={token}; HttpOnly; SameSite=Strict; Path=/; Max-Age={max_age_seconds}"` with no Secure; docstring L153 literally says 'no Secure flag (TLS terminates upstream)'. Used at L272 in the login handler. _clear_cookie_header (L160-165) likewise omits Secure. Unchanged from recon.
- verify: POST /admin/login and assert the Set-Cookie header lacks Secure. Make Secure conditional on X-Forwarded-Proto=https.

### SEC-010 — Low security (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes/wechat_webhook.py:92-99 (route handlers @101-109)`
- problem: bot_name path parameter is reflected unvalidated into the 404 body. Reflection is real but PlainTextResponse is Content-Type text/plain, so no HTML/JS executes — cosmetic / information-reflection only, NOT exploitable XSS.
- evidence: L95-98 `return PlainTextResponse(f"no wechat bot registered as {bot_name!r}", status_code=404)`; reached via @api.get('/{bot_name}') (L101) and @api.post('/{bot_name}') (L106) -> _dispatch (L92). Recon cited L95-98; current is L95-98 — unchanged.
- verify: GET /wechat/<script>alert(1)</script> on an unregistered name; response is text/plain so the browser does not execute. To remove reflection entirely, drop bot_name from the body.

### SEC-011 — Low security (CONFIRMED, conf=high) — reverify-r5-sec-bug
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/routes_admin_a/auth.py:263 + _auth_shim.py:192`
- problem: Admin username compared with non-constant-time != at both auth sites, creating a username timing oracle: a wrong username short-circuits before the slow constant-time argon2 path, making username correctness measurable.
- evidence: auth.py L263-265 `if body.username != state.admin_username or not argon2_verify(...)`; _auth_shim.py L192-194 `if username != admin_username or not _verify_password(...)`. Neither uses hmac.compare_digest; the `or` short-circuits on a username mismatch so argon2 never runs. Line numbers match recon exactly.
- verify: Wrap both username compares in hmac.compare_digest and always run argon2 against a dummy hash on username mismatch so total time is username-independent.

### NEW-fhA-5 — Low bug (NEW, conf=low) — fh-undercovered-A
- loc: `python/packages/corlinman-replay/src/corlinman_replay/session_store.py:384-388 (list_sessions ORDER BY MAX(ts) DESC)`
- problem: list_sessions orders by `MAX(ts) DESC` where ts is RFC-3339 TEXT compared lexicographically. The store's own writers normalise to a 'Z' suffix (via _format_rfc3339), but if a sessions.sqlite contains a mix of 'Z'-suffixed and '+00:00'-suffixed timestamps (e.g. rows written by an external/legacy tool, or sub-second-precision variance), lexicographic ordering is not chronological ('Z' (0x5A) sorts after digits and '+' (0x2B)). The admin sessions roster ('most recent first') could then show rows out of order. Low-confidence because all in-repo writers (Python and Rust) emit the canonical 'Z' form, so the skew only manifests on externally-written data.
- evidence: session_store.py:385-388 `SELECT session_key, MAX(ts) ... ORDER BY MAX(ts) DESC` with ts stored as RFC-3339 text; _format_rfc3339 (line 159-170) canonicalises to 'Z' but _parse_rfc3339 accepts both 'Z' and '+00:00', and the ALTER/legacy path makes no guarantee about pre-existing ts formatting.
- verify: Seed a sessions.sqlite with one row ts '2026-01-01T00:00:00+00:00' and another '2026-01-01T00:00:01Z', call list_sessions, and confirm whether the ordering matches true chronological order.

### NEW-fhB-3 — Low bug (NEW, conf=high) — fh-undercovered-B
- loc: `python/packages/corlinman-user-model/src/corlinman_user_model/distiller.py:539,552 + cli.py:85,106,149,179,313`
- problem: The --llm-model CLI flag / DistillerConfig.llm_model is silently ignored on the default path: default_llm_caller hardcodes 'deepseek-chat' for both resolve() and chat_stream(model=...), and never receives config.llm_model.
- evidence: cli.py adds --llm-model (lines 85,106), stores it into config.llm_model (lines 149,179), but the actual call uses default_llm_caller (cli.py:313) which takes only (system_prompt, transcript) and hardcodes provider = resolve('deepseek-chat') (distiller.py:539) and provider.chat_stream(model='deepseek-chat', ...) (distiller.py:552). config.llm_model is plumbed nowhere into the caller. An operator passing --llm-model gpt-4o silently still hits deepseek-chat. DistillerConfig docstring (lines 52-53) claims 'operators can swap via --llm-model on the CLI'.
- verify: Inspect distiller.py:523-556 (default_llm_caller signature has no config param, model literal 'deepseek-chat') and cli.py:304-313 (default caller chosen when no custom llm_caller passed). config.llm_model unreferenced by the caller.

### NEW-fhB-5 — Low bug (NEW, conf=medium) — fh-undercovered-B
- loc: `python/packages/corlinman-evolution-store/src/corlinman_evolution_store/repo.py:594-614 (mark_shadow_done)`
- problem: mark_shadow_done transitions a proposal to shadow_done with WHERE id = ? and no status guard, unlike the sibling state-transition methods (claim_for_shadow, mark_auto_rolled_back, _restore_proposal) which all guard on the expected current status - a stale/racing writer can clobber a row that has since moved out of shadow_running.
- evidence: claim_for_shadow uses 'WHERE id = ? AND status = pending' (repo.py:555), mark_auto_rolled_back uses 'WHERE id = ? AND status = applied' (line 489), auto-rollback applier _restore_proposal uses 'WHERE id = ? AND status = applied' (applier.py:432). But mark_shadow_done (repo.py:606-612) UPDATEs status='shadow_done' WHERE id = ? with NO 'AND status = shadow_running' clause, overwriting whatever status the row currently holds. Lower severity because the claiming ShadowRunner is normally exactly-one and shadow-tester is itself unwired (NEW-fresh-quality-7), but it is an inconsistent/unsafe transition.
- verify: Inspect repo.py:606-618; compare WHERE clauses against claim_for_shadow (555) and mark_auto_rolled_back (489).

### NEW-fhconcurrency-2 — Low bug (NEW, conf=high) — fh-concurrency-data
- loc: `python/packages/corlinman-replay/src/corlinman_replay/session_store.py:419-486 (append uses BEGIN IMMEDIATE+commit at :428/:470; delete uses bare commit at :484), shared _conn at :194, no lock`
- problem: SqliteSessionStore.append() runs an explicit BEGIN IMMEDIATE..commit() envelope while delete() issues a bare autocommit commit() on the SAME shared _conn (:276 _c) with NO write lock. This is the R5-B3 mix: a concurrent delete()'s connection-global commit() would flush an in-flight append() transaction. Mitigating factors keep severity Low/latent: (1) the write methods are documented test-fixture helpers (header comment :415-417 'Writes (test-fixture helpers; replay itself is read-only)'); (2) the only production caller (gateway/routes_admin_a/sessions.py:246,260) opens a fresh per-request store instance and calls only read methods load()/list_sessions(); so no two concurrent writers share one instance today. The defect is latent — if a future feature reuses a long-lived store and exercises append()+delete() concurrently, it inherits the B3 race.
- evidence: session_store.py:428 `await conn.execute("BEGIN IMMEDIATE")` then :470 `await conn.commit()` inside append(); :484 `await self._c.commit()` inside delete() with no surrounding lock; class has no asyncio.Lock (only _conn at :194). Production read-only usage confirmed at routes_admin_a/sessions.py:248 (store.list_sessions) and :262 (store.load), each on a per-request `SqliteSessionStore.open(path)` closed in finally.
- verify: grep -nE "asyncio.Lock|_write_lock|tx_lock" session_store.py returns nothing (no serialisation). Confirm no production concurrent writer: grep callers of `.append(`/`.delete(` on SqliteSessionStore outside /tests/ — none found. If a writer is ever wired, add an asyncio.Lock around append/delete envelopes mirroring agent_journal_backend.py:689.

### NEW-fhperf-3 — Low performance (NEW, conf=medium) — fh-perf-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/services/chat_service.py:245-249 (_run_chat) and 292`
- problem: Two minor per-frame hot-loop costs in the gRPC channel chat path. (1) _run_chat creates a fresh asyncio Task and a 2-element asyncio.wait set on EVERY inbound frame, including every token frame: `next_task = asyncio.create_task(_next_frame(rx)); done,_ = await asyncio.wait({next_task, cancel_task}, ...)`. For a token-heavy stream this is one Task allocation + wait-set construction + cancel/cleanup per token, a measurable scheduler overhead vs awaiting rx with a single long-lived cancel race. (2) line 292 uses `__import__("json").loads(...)` instead of a module-level `import json`, re-resolving the json module via the import machinery on each _builtin_done frame (fires once per in-process builtin tool completion). The json module is already imported elsewhere in the package; this is needless per-call import overhead in a tool-heavy turn.
- evidence: chat_service.py:245-258 per-iteration `next_task = asyncio.create_task(_next_frame(rx))` + `asyncio.wait({next_task, cancel_task}, return_when=FIRST_COMPLETED)` inside the `while True:` frame loop that also handles `kind == 'token'` (line 275). Line 292: `__import__('json').loads(bytes(tc.args_json).decode('utf-8'))`. Module has no top-level `import json`.
- verify: grep -n 'import json' chat_service.py (absent at module scope) — confirms __import__ workaround. For the task churn, profile a 500-token stream through GrpcAgentChatBackend and count Task creations (~1 per frame).

### NEW-fhperf-4 — Low performance (NEW, conf=medium) — fh-perf-deep
- loc: `python/packages/corlinman-providers/src/corlinman_providers/openai_provider.py:118-132 (_make_client) called from chat_stream:184`
- problem: Each provider round constructs a brand-new AsyncOpenAI client (and thus a fresh httpx connection pool) inside chat_stream, then closes it in the finally (lines 184, 262-269). A multi-round agent turn (the ReasoningLoop drives up to _MAX_ROUNDS=60 provider calls per turn) therefore creates and tears down a TCP/TLS connection pool on every single round, paying connection-setup + TLS-handshake latency per round instead of reusing keep-alive connections. The per-call construction is deliberate (the R1-003 pool-leak fix + 401-recovery rebuild rely on close-on-finally), but a cached/reused client for the steady-state non-error path would avoid repeated pool setup. Same pattern in codex_provider.py:351 (_make_client per chat_stream).
- evidence: openai_provider.py:184 `client_ = self._make_client()` inside _open() called per chat_stream; 268-269 `if client is not None: await _safe_close(client)` in finally closes the pool every call. _make_client builds `AsyncOpenAI(**client_kwargs)` (127-132) which opens a new httpx.AsyncClient pool. ReasoningLoop calls provider.chat_stream once per round (reasoning_loop.py:1265), up to _MAX_ROUNDS=60 (reasoning_loop.py:188).
- verify: Wrap _make_client to count instantiations; run a 5-tool-round agent turn against a mock provider and observe 6+ AsyncOpenAI constructions/closes (one per round) instead of one reused client.

### NEW-fhperf-5 — Low performance (NEW, conf=medium) — fh-perf-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/agent_servicer.py:977 (_refresh_skill_registry) → skills/registry.py:354-532 (refresh)`
- problem: The per-turn skill-registry reconcile runs a synchronous root.rglob('*.md') plus a stat() per tracked file directly on the asyncio event loop inside the async Chat handler (it is a sync method awaited-adjacent, not offloaded to a thread). When the 30s debounce window has elapsed (skills/registry.py:396-403) the next chat turn does blocking filesystem I/O on the loop thread — rglob over the skills dir + read_text on any changed file — stalling the event loop (and thus all other concurrent sessions' streaming) for the scan duration. Impact is bounded by the 30s debounce and the small (~16) bundled skill set, so it is normally a few ms, but on a cold/slow/networked filesystem or a large skills tree it blocks the loop. (Corroborates the spirit of the already-filed NEW-fresh-perf-2 sync-FS-on-async-loop class.)
- evidence: agent_servicer.py:977 `self._refresh_skill_registry()` is a plain sync call inside `async def Chat`; _refresh_skill_registry (2342-2343) calls `delta = refresh()`. registry.refresh() does `for path in sorted(root.rglob('*.md'))` (registry.py:440) + `path.stat()` (445) + `path.read_text()` (467) — all synchronous, no asyncio.to_thread/run_in_executor. Debounce limits cadence to 30s (43, 396-403) but the scan itself runs on the loop thread.
- verify: Point CORLINMAN_DATA_DIR/skills at a directory with many .md files (or a slow mount), set CORLINMAN_SKILL_REFRESH_INTERVAL_MS=0, and time how long the event loop is blocked during refresh() inside a Chat turn (e.g. measure delay on a concurrent task).

### NEW-fh-sec-deep-3 — Low quality (NEW, conf=low) — fh-sec-deep
- loc: `python/packages/corlinman-server/src/corlinman_server/gateway/lifecycle/entrypoint.py:1381-1395 (admin seed wiring); routes_admin_b/state.py:286-300 (require_admin); routes_admin_a/_auth_shim.py:148-210`
- problem: Admin-B route authentication only works by accident via a lazy fallback, not by its primary path. The boot lifespan populates admin_a_state.admin_username / admin_password_hash / session_store (entrypoint.py:1382-1384 + the session store created in auth.py) but for admin_b_state it sets ONLY must_change_password (entrypoint.py:1393-1395) — never admin_username, admin_password_hash, or session_store. Consequently routes_admin_b.state.require_admin's primary call authenticate_admin_request(request, admin_b_state) always raises 'admin_not_configured' (the admin_b cookie path is skipped because session_store is None, and the Basic path bails at _auth_shim.py:180), and admin-B auth survives only because require_admin catches that specific reason and re-runs require_admin_dependency(request) against the admin-A module-global state (state.py:296-298). This is fragile: any change that makes admin_b's state report a username/hash (e.g. a future boot tweak that mirrors creds onto admin_b like it mirrors must_change_password) would short-circuit the fallback and run the admin_b cookie/Basic path against a state whose session_store is None — silently breaking cookie-based admin-B sessions — and the dual-state design means the SEC-007 must_change gate for admin_b is also only enforced through the admin_a fallback. Auth is currently intact; this is latent fragility, not an active bypass.
- evidence: entrypoint.py:1381-1385 sets four fields on admin_a_state; the parallel admin_b block at 1393-1395 sets only must_change_password (guarded by hasattr). state.py:286-300 shows require_admin depends on the admin_not_configured exception to trigger the admin_a fallback. _auth_shim.py:165-181 shows admin_b's session_store=None disables the cookie path and the missing username forces the admin_not_configured raise.
- verify: Mount build_router() from routes_admin_b with set_admin_state(AdminState(must_change_password=...)) but no admin_username/session_store, and a separately-seeded admin_a state; confirm admin-B routes authenticate only because state.py:296 falls back to admin_a — and that an admin_b state carrying a username but no session_store would make cookie auth fail.

### NEW-fhB-4 — Low quality (NEW, conf=high) — fh-undercovered-B
- loc: `python/packages/corlinman-memory-host/src/corlinman_memory_host/read_only.py:43-46,55-59`
- problem: ReadOnlyMemoryHost.name() returns 'ro:<inner>' but query() delegates straight to the inner host, so returned MemoryHit.source carries the INNER host's name, not the 'ro:' prefix - contradicting the docstring's stated attribution guarantee.
- evidence: read_only.py:53 caches 'ro:{inner.name()}' and name() returns it (line 56); the docstring (lines 43-46) claims this keeps 'attribution in MemoryHit.source correct'. But query() (lines 58-59) returns await self._inner.query(req) unchanged; LocalSqliteHost/RemoteHttpHost set MemoryHit.source=self._name (the inner name) so hits are never re-stamped with 'ro:'. Read-only enforcement itself (upsert/delete refusal, lines 61-73) is correct - only the attribution claim is wrong. Low impact (telemetry/source labeling).
- verify: Wrap a LocalSqliteHost named 'kb' in ReadOnlyMemoryHost; ro.name()=='ro:kb' but ro.query(...)[0].source=='kb'.
