# corlinman · ARCH_DEBT.md

> Issues that are architectural / cross-service / require product decision.
> Not fixed inside the perpetual loop. Surfaced here for user review.

- **R3-003 follow-up (operator action):** reserve the abandoned `ymylive` namespace on github.com and ghcr.io to prevent a third party from re-registering it and serving malicious install scripts / container images from URLs still referenced in external docs. Procedure: `audit/evidence/round-3/R3-003/SQUAT_RESERVE.md`.

- **R4-F3 (feature — background subagent dispatch):** `subagent.spawn run_in_background:true` returns `run_in_background_not_implemented` (safe, honest). Docs aligned to reality in Round 4 (`docs/multi-agent.md` "Background dispatch"); the safe rejection is kept. A real implementation is a scoped multi-part feature (deliberately NOT shipped as a misleading pure-LLM MVP — analysis in `audit/evidence/round-4/F3-bg-subagent/analysis.md`, contract research in workflow `wf_6f13daa8-3f5`). Required pieces:
  1. **`SubagentRequest` schema** (`system/subagent/store.py:87`): add `tool_schemas`/allowlist snapshot, `model`, `parent_depth`, `max_wall_seconds` — captured at `_dispatch_via_background` time (`tool_wrapper.py:846`). Without the parent tool snapshot a background child can only run pure-LLM (forwarding a `tool_allowlist` with `parent_tools=None` auto-rejects as escalation, `runner.py:167-176`). **Product decision required**: what tool allowlist may an autonomous *background* agent use (esp. `run_shell` / fs-write)?
  2. **Real `run_child_factory`** (`entrypoint.py` ~1671, replace `_unwired_run_child_factory`): a LAZY closure over `state` + `resolved_data_dir` (NOT value-capture — `state.provider_registry` is still None at construction; the providers bootstrap runs later). At invocation: `card = registry.get_or_default(req.subagent_type)` (→ REJECTED if None); `model = card.model or state.config['models']['default']` (never `''` → KeyError); `provider = state.provider_registry.resolve(model, aliases=...)` (NOT the specs-less module `resolve`); reconstruct `ParentContext` from req; `run_child(..., parent_tools=<per policy>)` wrapped in `asyncio.wait_for(ceiling)` + try/except → ERROR `TaskResult`.
  3. **Thread the dispatcher** into `dispatch_subagent_spawn` at `agent_servicer.py:1865` (else background stays at the sentinel even with a real factory).
  4. **Journal notification**: add `start_turn_for_subagent_notification` to the real `AgentJournal` backends (missing everywhere → the dispatcher's terminal "synthetic user msg" is a no-op, so the parent never sees the result). Without this the feature is near-useless.
  5. **Supervisor routing** so all three R3-004 caps (depth / per-parent concurrency / tenant) apply — calling `run_child` directly bypasses two of them.
  6. **Boot reconciliation**: flip pre-existing `running` rows to `stalled` on startup (no sweep exists → orphaned rows after restart permanently consume the tenant cap, `store.py:74`).

- **R5 — /nodes runner registry (feature — no backend exists):** the `/nodes` admin page is a UI-only prototype. There is NO `GET /wstool/runners` (or any runner-registry) HTTP route in the gateway — `grep -rn "wstool/runners" python/` is empty. The page used to poll `fetchRunnersMock()` (`ui/lib/mocks/nodes.ts` → `return []`) every 5s and render the generic "No runners registered" empty state, falsely implying a working-but-idle registry; the detail-drawer "Reconnect" was an inert no-op (`ui/components/nodes/node-detail-drawer.tsx:196`). Honest-aligned in R5: the page now renders an explicit "not yet available (no backend endpoint)" panel and polls nothing (`ui/app/(admin)/nodes/page.tsx` + `NotImplementedBlock` in `ui/components/nodes/offline-block.tsx`). The full topology / side-rail / detail-drawer UI and the `Runner` type/`summariseRunners` mock are retained so they can be re-wired with no UI rework. Required pieces for the real fix:
  1. **Gateway HTTP route** — expose `corlinman-wstool`'s in-memory registry. The data already exists on `ServerState` in `python/packages/corlinman-wstool/src/corlinman_wstool/registry.py`: `runner_count()` (`registry.py:132`) and `self.runners` (`runner_id -> ConnHandle`, each with `.tools`); `advertised_tools()` (`registry.py:128`) gives the tool→runner index. Add a read-only endpoint e.g. `GET /v1/wstool/runners` returning a serialised snapshot (per-runner: id, advertised tool names, connection age, last-ping/health if tracked) plus an aggregate count. Map the existing `ConnHandle` fields onto the UI `Runner` shape (`ui/lib/mocks/nodes.ts`: id/hostname/ring/health/latencyMs/toolCount/connectedForSec/lastPingMs/errorRate/tools) — several UI fields (ring/slot/latency history/errorRate) have no server-side source yet, so either add lightweight server-side bookkeeping or drop those UI affordances.
  2. **Auth + wiring in entrypoint** — register the route behind the same API-key dependency the other gateway routes use, and pass it the live `ServerState`/registry handle wired at WS-tool-server bootstrap (the route must read the SAME registry instance the WS server mutates, not a fresh one). Add to the gateway's route table in `entrypoint.py` next to the other `/v1/...` mounts.
  3. **UI: point the query at the real endpoint** — in `ui/app/(admin)/nodes/page.tsx` restore a `useQuery<Runner[]>` whose `queryFn` calls `apiFetch("/v1/wstool/runners")` (replacing the deleted `fetchRunnersMock` poll), normalising the server payload to `Runner[]`. Re-mount the retained `PageHero`/`StatsRow`/`CapabilityFilter`/`TopologyGraph`/`NodeSideRail`/`NodeDetailDrawer`/`EmptyBlock`/`OfflineBlock` (the loading/empty/error branch logic from the pre-R5 version, preserved in git history) and remove `NotImplementedBlock` from this page. Decide on refresh cadence (poll interval or SSE — `registry.py` would need a change/notify hook for true streaming).
  4. **Reconnect action** — implement `onReconnect` (currently `undefined`, so the button is disabled via `disabled={!onReconnect}` at `node-detail-drawer.tsx:197`): a gateway POST that signals the WS-tool server to drop+await re-handshake for a given `runner_id`, or remove the affordance if out of scope. Update the now-stale hint `nodes.tp.actionReconnectHint` ("ships in B4-BE3") accordingly.
  5. **i18n strings** — the R5 honest panel copy is intentionally inlined in `NotImplementedBlock` (no existing translation string truthfully says "no backend endpoint"); when the real feature lands, fold any retained copy back into `ui/lib/locales/en.ts` + `zh-CN.ts` under the existing `nodes.tp.*` namespace.

- **C4-EMB (feature — embeddings / dense retrieval is DEAD):** the `[embedding]` config block is parsed, seeded by onboarding, and surfaced via `/admin/embedding`, but nothing constructs an embedder at runtime — so semantic/dense retrieval does **not** work. Docs aligned to reality in Round C4 (`docs/providers.md` §1 "Not wired yet" note + the three recipe captions). Evidence: every provider `.embed()` raises `NotImplementedError` (`openai_provider.py:281`, `anthropic_provider.py:405`, `google_provider.py:178`); episodes retrieval is recency + BM25 only and the stored embedding BLOB is never read for a cosine path; the `corlinman_embedding.EmbeddingRouter` the bindings nominally target does not exist (no `python/packages/corlinman-embedding/` package). Required pieces:
  1. **A real `embed()` impl** for at least one provider kind (OpenAI `/v1/embeddings` is the obvious first — replace the `NotImplementedError` at `openai_provider.py:281`; batch + dimension from `[embedding].model`/`dimension`).
  2. **Wire `register_embedding_provider_factory`** (already exported by `corlinman_episodes.cli`, `cli.py:127`) in gateway boot (`lifecycle/entrypoint.py`): construct an `EmbeddingRouter`-shaped callable from the resolved `[embedding]` binding (`provider` → `ProviderRegistry` entry, must be embedding-capable) and hand its bound `embed` to the episodes CLI/resolver. Today the factory is never registered, so the embed seam stays `None`.
  3. **Episodes resolver cosine path**: read the stored embedding BLOB and add a cosine (or RRF-with-dense) branch in the episodes retrieval ranker, gated on the registered embedder being present; fall back to BM25 + recency when absent. Without this the embedding column written at ingest is dead weight.
  4. **Provider capability gating**: keep the `kind`-level embedding capability flags honest so `/admin/embedding` + `<DynamicParamsForm>` only offer embedding-capable providers once (2) lands.

- **C4-PLUGIN-ASYNC (feature — plugin async/deferred callback is a permanent no-op):** `plugin_type = "async"` accepts a `task_id` but the deferred-result path is not connected. Docs aligned to reality in Round C4 (`docs/plugin-authoring.md` §5 banner + type table + timeout FAQ). Evidence: `POST /v1/plugins/callback/{task_id}` returns 501 unconditionally because `GatewayState.plugin_async_tasks` is never assigned anywhere (no `plugin_async_tasks =` in src or tests; the route's `None` guard at `routes/plugin_callback.py:45` always fires); `plugin_invoker` never parks `accepted_for_later` on a registry — it just surfaces the `task_id` verbatim to the model (`grpc/plugin_invoker.py:198`). Required wiring:
  1. **Construct + assign the registry**: build an `AsyncTaskRegistry` (`corlinman_providers.plugins.async_task:42`) in `lifecycle/entrypoint.py` and assign it to `GatewayState.plugin_async_tasks` (the slot at `routes/register.py:78`); `register.py:123-132` already mounts the callback router only when that slot is a real `AsyncTaskRegistry`.
  2. **Park on accept**: in `plugin_invoker` `accepted_for_later` branch (`plugin_invoker.py:198`), call `registry.register(task_id)` to create the awaitable future *before* returning to the loop, and have the reasoning loop await it (with the manifest `timeout_ms` ceiling + cancellation) instead of treating `accepted` as a terminal string.
  3. **Resolve on callback**: the `/v1/plugins/callback/{task_id}` route resolves the parked future from the registry with the posted `{status,result}` payload (one-shot, then evict); unknown/expired `task_id` → 404/410, not 501. Keep `task_id` as the one-time bearer-less credential the docs describe.

- **C4-MCP-SERVER (feature — `/mcp` WebSocket server built but never bound):** `build_mcp_server` exists (`corlinman_server.gateway.mcp.server:170`) but has **zero non-test callers** (only the module docstring/`__all__` re-export reference it); the gateway entrypoint wires only the MCP *client* (`McpClientManager`, `lifecycle/entrypoint.py:1944-1948`), so no `/mcp` route is mounted and Claude Desktop cannot connect. Docs aligned to reality in Round C4 (`docs/design/phase4-w3-c1-design.md` reality-check banner + present-tense "is mounted" / "point Desktop at" sentences corrected). Required bind/mount wiring:
  1. **Construct the server in boot**: in `lifecycle/entrypoint.py`, after the registries exist, call `build_mcp_server(cfg, plugins, skills, hosts, runtime)` with the live `PluginRegistry` / `SkillRegistry` / memory hosts (the same args the docstring example at `mcp/server.py:185` shows).
  2. **Mount the WebSocket route** at `GET /mcp` on the gateway app (pre-upgrade bearer/token check → 401 before the WS upgrade, per the design's auth gate), wired into the route registration alongside the other routers.
  3. **Config + restart gating**: add an `[mcp]` enable/bind block (mirroring `[wstool]`), gate construction on it (degrade to "not mounted" when absent — do not crash), and add `mcp` to the restart-required sections so a config flip rebinds. Until (1)-(3) land, keep the honest "not bound" docs.

- **R5-Q1 (CI gate — `py-ruff`/`py-mypy` red + grandfathered layering violations):** R5 fixed the *structural* CI breakage (import-linter phantom `corlinman_embedding` package removed so the contract evaluates again; `boundary-check` added to the `gate` `needs`; ruff `external` config so the ~510 `# noqa: BLE001`/`PLC0415`/… directives for non-selected rules stop tripping `RUF100`; `ci.yml` mypy scoped to `python/packages/`; the stale/false `docs/ci-status.md` numbers corrected). But the required `gate` check is still **red** because two real debt piles remain — deliberately deferred (a mechanical sweep is a large, audit-unrelated blast radius, out of R5 scope per the user):
  1. **ruff: 1153 genuine errors** (down from 1663). Mostly auto-fixable import churn — I001 (202), F401 (95), UP017/UP037 (130), RUF022 (50) — `ruff check --fix` clears 566 but touches hundreds of files. Plus a **policy call**: RUF001/2/3 ambiguous-unicode (~165) are false-positives on CJK prompt/docstring content (repo already per-file-ignores RUF001 for `distiller.py`) — decide ignore-plane-wide vs per-file. Plus SIM105 (115) / E402 (95) need case-by-case review (legit lazy/conditional imports). Recommend one dedicated "lint greening" PR, package-by-package with full tests between.
  2. **mypy: 156 genuine type errors in 73 files** (scoping does not reduce them). Concentrated in `gateway/routes_admin_a/*` (`Returning Any`), `cli/init.py` (`no-redef`), provider error-construction kwargs. Not auto-fixable; needs an incremental type-correctness pass.
  3. **3 grandfathered layering violations** (`.importlinter` `ignore_imports`): `corlinman_agent.persona.dispatch -> corlinman_server.persona`, `corlinman_agent.subagent.tool_wrapper -> corlinman_server.system.subagent`, and `-> corlinman_server.gateway.observability.emitter`. Upward (agent→server) imports that violate the layered plane (were masked by the phantom-package abort). Proper fix: extract the shared symbols into a lower layer or invert via a protocol/registration seam, then drop the `ignore_imports` entries. The guard is now live and blocks any *new* upward import.

- **NEW-fhB-1 / NEW-fhfunc-3 (feature — self-evolution APPLY is bookkeeping-only + AUTO-ROLLBACK chain is dead; HIGH-RISK, needs product decision):** `POST /admin/evolution/{id}/apply` (`routes_admin_b/evolution.py:769`) drives `corlinman_auto_rollback.EvolutionApplier` (`python/packages/corlinman-auto-rollback/src/corlinman_auto_rollback/applier.py:189`). That applier **records an intent-log row + an `evolution_history` row and flips the proposal to `applied`, but NEVER mutates any skill/prompt file or `kb.sqlite`** — its own docstring says so (`applier.py:5-9` "The kb / filesystem mutators stay a follow-up"); `_commit_apply` (`applier.py:258-297`) only writes audit rows. The gateway-side applier (`gateway/evolution/applier.py:329-344`) does call `handler.apply(proposal)` but writes the same **empty `metrics_baseline={}`** (`applier.py:344`; the auto-rollback copy hardcodes it at `applier.py:279`). That empty baseline kills auto-rollback: `AutoRollbackMonitor.run_once` (`monitor.py:123`) feeds `history.metrics_baseline` into `MetricSnapshot.from_dict` (`monitor.py:176`), and `from_dict({})` raises `ValueError` on the missing `target` field (`metrics.py:67-83`) → the monitor's fail-safe branch logs "malformed metrics_baseline JSON; skipping" and `continue`s (`monitor.py:179-186`). So even if a monitor ran, every applied proposal is skipped. AND there is **no runtime driver** for the monitor in the gateway: the only CLI path is a STUB (`cli/main.py:191` `rollback run-once` → `todo_stub`), `AutoRollbackMonitor` is not registered in any scheduler job and not constructed in `gateway/lifecycle/entrypoint.py`. The whole apply→monitor→rollback chain is therefore dead. ShadowTester docstrings overstate too: `corlinman_evolution_engine` handler docstrings (`engine.py:89-91`, `agent_card.py:13-14`/`:110`, `prompt_template.py:11-12`/`:107`, `tool_policy.py:12-13`/`:154`, `gepa.py:32`) claim the ShadowTester "already routes high-risk kinds through the shadow stage", but `corlinman_shadow_tester.ShadowRunner` (`runner.py:87`) is **never imported or called** by the engine, has no console-script entrypoint, and is not registered as a scheduler job (docstrings are code, not edited here — noted as overstatement only). **HIGH-RISK capability** (an agent self-mutating its own skill/prompt files) — requires a product decision before wiring. Required pieces:
  1. **Apply mutates the target artifact transactionally + records a real baseline.** Per `kind`, give the applier a real mutator that writes the proposal diff to the target skill/prompt file or `kb.sqlite` chunk inside the same transaction as the audit row, capturing the pre-apply content into `inverse_diff` (the `_build_inverse_diff` seam at `applier.py:140` already exists for the revert payload). Thread the kb/fs handles through `AdminState` (the docstring at `applier.py:5-9` names exactly this gap). On the same path, replace `metrics_baseline={}` (`auto-rollback applier.py:279`, gateway `applier.py:344`) with a real `MetricSnapshot` captured at apply time via `corlinman_auto_rollback.metrics.capture_snapshot` (already called for the *current* snapshot at `monitor.py:189`) keyed on `proposal.target` + the kind's `watched_event_kinds`.
  2. **Wire a monitor driver into the gateway.** Construct an `AutoRollbackMonitor` (`monitor.py:91`) over the live `EvolutionStore` connection in `gateway/lifecycle/entrypoint.py` and register a periodic `run_once` (`monitor.py:123`) — either a scheduler job (mirror the `evolution_engine`/`shadow_tester` subprocess jobs in `docs/evolution-loop.md`, or an in-process lifespan task) — replacing the `cli/main.py:191` STUB. The monitor reverts via `EvolutionApplier.revert` (`applier.py:311`), so the manual `POST /admin/evolution/{id}/rollback` route (`routes_admin_b/evolution.py:826`) and the auto path share one code path.
  3. **Rollback reverts the artifact.** `revert` today replays the captured prior status from `inverse_diff` (`applier.py:339`) but, symmetric to (1), must also re-write the target skill/prompt/kb artifact from the inverse payload, not just flip the status row.
  4. **Shadow stage (optional, gated by (1)):** if the shadow flow is wanted, invoke `ShadowRunner.run_once` (`runner.py:116`) — either inside `corlinman_evolution_engine.engine` for `risk = medium|high` proposals or as a scheduled job — and add a `[project.scripts]` entrypoint to `corlinman-shadow-tester`. Until then the handler docstrings (above) overstate; correct or guard them when this lands. The `docs/evolution-loop.md` "Shadow gating" section + `docs/multi-agent-release-plan.md` §2.2 carry reality-check banners pointing here.

- **NEW-fhfunc-5 (feature — GOALS package has no runtime driver/wiring in the gateway):** `corlinman-goals` is fully built (`GoalsResolver` `placeholders.py:117`, `GoalStore.open_or_create` `store.py:187`, `reflect_once` `reflection.py:288`) but is **not imported by `corlinman-server`** anywhere (`grep -rn "corlinman_goals" python/packages/corlinman-server/src/` is empty) — no runtime driver, no admin route, no agent wiring. The `{{goals.*}}` placeholder namespace seam already exists and is anticipated (`gateway/grpc/placeholder.py:519`, `:702`) but is gated on a `state.goals_resolver` attribute that nothing ever publishes, so `build_default_engine` logs "goals_resolver_unavailable reason=no_resolver_on_state … namespace deferred" (`placeholder.py:699-712`) and `{{goals.weekly}}` echoes back unresolved. Docs aligned this round (`docs/design/phase4-next-tasks.md` Section-D note + D2 table row; the design doc `phase4-w4-d2-design.md` stays a `Status: Design` intent doc). Required wiring (mirror the R4-F2 episodes precedent):
  1. **Open the store + build the resolver at boot.** In `gateway/lifecycle/entrypoint.py` (async lifespan, where `data_dir` is resolved) call `await GoalStore.open_or_create(<data_dir>/tenants/<t>/agent_goals.sqlite)` (path per `phase4-w4-d2-design.md:65`) and construct `GoalsResolver(store, tenant_id=...)` (`placeholders.py:126`). This is the "new async plumbing through entrypoint/app-state" the placeholder builder comment flags (`placeholder.py:690-691`) — `build_default_engine` is synchronous and cannot open the store itself.
  2. **Publish the resolver on AppState.** Set `state.goals_resolver = <resolver>` so `build_default_engine` (`placeholder.py:704`, probed via `_resolve_state_attr`) picks it up through the already-wired `_CtxIdResolverAdapter` (`placeholder.py:524`, keyed on `AGENT_ID_METADATA_KEY`) — the namespace then lights up with **no change to `placeholder.py`**. Confirm the `{{goals.weekly}}` 4-item acceptance (`phase4-next-tasks.md:21`) round-trips against a seeded store, the same way R4-F2 verified `{{episodes.recent}}`.
  3. **Reflection driver (optional but needed for live goal scoring).** Register `reflect_once` (`reflection.py:288`) per tier as a scheduler job (the design routes it through `corlinman-scheduler` `JobAction::Subprocess`, `phase4-w4-d2-design.md:155`) and wire a real `corlinman-providers`-backed `Grader` via `register_grader_factory` (`cli.py:109`) — without a grader, reflection writes no scores. Admin goal-setting REST/UI is a further follow-up (carried in `phase4-next-tasks.md:276`).

- **NEW-fhfunc-6 (feature — cross-channel IDENTITY ingest unification is not wired):** `corlinman-identity`'s `UserIdentityResolver` (`user_identity_resolver.py:47`, with `resolve` = resolve-or-create at `:111`) is built and the admin REST/UI ship, but at **message ingest no identity unification runs** — `UserIdentityResolver`/`identity_store` is consumed **only** by the admin route (`gateway/routes_admin_a/identity.py:154`) reading `AdminState.identity_store` (`routes_admin_a/state.py:132`). The Rust "iter 5 chat-path resolution" (`handle_chat` calling `resolve_or_create`, design `phase4-w2-b2-design.md:289-291`) was **not ported** to the Python gateway; the channel `InboundEvent.user_id` seam is left `None` for adapters that "don't perform resolution" (`corlinman-channels/common.py:214-218`) and nothing fills it. Docs aligned this round (`phase4-next-tasks.md` B2 rows + `phase4-w2-b2-design.md` iter-5 + status snapshot; the `…/cli/main.py:223` "Channel-alias resolver inspect — STUB" already honest). Required wiring:
  1. **Open the resolver for chat, not just admin.** Make the per-tenant `UserIdentityResolver` (`await UserIdentityResolver.open(...)`, `user_identity_resolver.py:70`) reachable from the chat/ingest path (today only `AdminState.identity_store` holds one; the chat path runs off a different state). Either publish it on the chat-side `ChatState`/`AppState` at boot in `gateway/lifecycle/entrypoint.py`, or share the admin-opened handle.
  2. **Resolve at ingest.** In the inbound message path (where `session_key` / channel + channel-user-id are first known), call `resolver.resolve(channel, channel_user_id, display_name_hint=...)` (`user_identity_resolver.py:111`) to mint/look up the canonical `UserId`, and stamp it onto `InboundEvent.user_id` (`corlinman-channels/common.py:214`) so the canonical id flows downstream. This is the gap that makes "same human across QQ + Telegram + iOS = same `user_id`" real.
  3. **Propagate `user_id` to hooks/persona.** Thread the resolved `user_id` into `HookEvent` / `EvolutionSignal` (design iter 8, `phase4-w2-b2-design.md:301-304`) and into the placeholder render ctx under `USER_ID_METADATA_KEY` (`placeholder.py:511`) so `{{user.*}}` / per-user trait attribution works. Verification-phrase issue/redeem (`verify_issue`/`verify_redeem`, `user_identity_resolver.py:161`/`:184`) already exist for the operator-driven cross-channel merge.

- **NEW-fhfunc-4 (feature — VOICE persistence + transcript→chat bridge never run):** the `/voice` route mounts (`gateway/lifecycle/entrypoint.py:1077-1078`) and `build_voice_state_from_app` (`routes_voice/mod.py:400`) builds a `VoiceState`, but it constructs it **without `session_store` / `transcript_sink`** (`mod.py:426-430`), leaving both at their `None` defaults (`mod.py:250-251`). There is **no `SqliteVoiceSessionStore`** — only `MemoryVoiceSessionStore` (`routes_voice/persistence.py:170`); the Sqlite store is explicitly "left as a follow-up" (`persistence.py:16-18`, mirroring the Rust `SqliteVoiceSessionStore`). Net effect: `voice_sessions` rows are never durably written and the transcript→chat-session bridge (which fires `transcript_sink` at `mod.py:747`/`:761`) never runs — so voice turns are not persisted and do not merge into the chat `sessions` table. Docs aligned this round (`phase4-next-tasks.md` Section-D note + voice iter-6 row; the design doc `phase4-w4-d4-design.md` stays a `Status: Design (pre-implementation)` intent doc). Required wiring:
  1. **Implement `SqliteVoiceSessionStore`** in `routes_voice/persistence.py` satisfying the `VoiceSessionStore` Protocol (`persistence.py:153`) over a `voice_sessions.sqlite` (schema: `voice_sessions` row with `audio_path IS NULL` when `retain_audio=false`, per `phase4-w4-d4-design.md:283`). Mirror the `MemoryVoiceSessionStore` (`persistence.py:170`) method surface.
  2. **Build a real transcript sink** that writes the voice turns' `user`/`assistant` rows into the existing chat `sessions` table under the same `session_key` (the `transcript_persisted_to_chat_session` acceptance, `phase4-w4-d4-design.md:282`).
  3. **Wire both into `build_voice_state_from_app`** (`mod.py:400`): open the Sqlite store + transcript sink from the gateway `data_dir`/config and pass them to the `VoiceState(...)` constructor (`mod.py:426`) instead of leaving them `None`, so `mod.py:698` (`if state.session_store is not None`) and the `transcript_sink` calls at `mod.py:747`/`:761` actually fire. Gate on a `[voice].persist` (or reuse the existing `[voice]` enable) flag and degrade to the current no-persist behaviour when absent. PCM byte-stream writer for `retain_audio=true` + a `voice_spend` Sqlite table remain separate follow-ups (`phase4-next-tasks.md:278`).

- **R6-G8 (feature — `{{persona.*}}`/`{{user.*}}`/`{{goals.*}}` placeholder activation; seam shipped, entrypoint plumbing pending):** R6 (#R6-G8) shipped the *seam*: `build_default_engine` (`gateway/grpc/placeholder.py`) now adds a `_CtxIdResolverAdapter` and conditionally `register_namespace`s `persona`/`user`/`goals` whenever a `persona_resolver`/`user_model_resolver`/`goals_resolver` is published on `AppState` (it logs `<ns>_resolver_unavailable … namespace deferred` once at boot otherwise). This is tested + harmless (production behaviour unchanged — those tokens still echo verbatim today). It is NOT yet active because the entrypoint never publishes the resolvers, and (unlike the lazy data-dir `EpisodesResolver`) the persona/user/goals stores are single-tenant single-connection. Required plumbing to light it up: (1) in `gateway/lifecycle/entrypoint.py` async boot, open `corlinman_persona.PersonaStore`/`corlinman_user_model.UserModelStore`/`corlinman_goals.GoalStore` per the per-tenant data dir and publish `PersonaResolver`/`UserModelResolver`/`GoalsResolver` on `AppState` under `persona_resolver`/`user_model_resolver`/`goals_resolver` — with a per-tenant store cache or a data-dir-lazy resolver (mirror `EpisodesResolver`) since the stores are single-tenant; (2) stamp `agent_id` + `user_id` (and `tenant_id`) onto `PlaceholderCtx.metadata` in the render-call path (no middleware does today — same pre-existing gap that leaves `{{episodes.*}}`/`{{memory.*}}` id-less); (3) add store `.close()` to gateway shutdown. Overlaps the goals wiring in NEW-fhfunc-5 and the identity work in NEW-fhfunc-6.
