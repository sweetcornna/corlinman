"""Admin-A multi-tenant route bundle (identity / tenants / API keys).

Modularization Phase 7b: grouped out of the flat routes_admin_a namespace.
Each module exposes a no-arg ``router()`` consumed by routes_admin_a.__init__.
"""
