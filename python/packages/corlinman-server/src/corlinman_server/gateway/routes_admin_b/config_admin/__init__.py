"""Admin-B configuration route bundle.

Modularization Phase 5b: the config / models / providers / credentials /
onboard / image-provider admin routes grouped here out of the flat
routes_admin_b namespace. Each module exposes a no-arg ``router()`` consumed by
routes_admin_b.__init__.
"""
