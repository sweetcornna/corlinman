"""Small operator/agent-facing CLI tools shipped on the venv PATH.

Currently:

* :mod:`corlinman_server.tools.doc_render` — the ``corlinman-md2pdf``
  console script the ``document-generator`` skill uses to turn Markdown into
  a clean, CJK-correct PDF (the v1.12.3 fix for garbled report output).
"""
